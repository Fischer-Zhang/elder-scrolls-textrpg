# CLAUDE.md — 流亡者 (tesrpg) 開發憲法

上古卷軸風格的**技能驅動沙盒文字 RPG**(**瀏覽器 Web 版**;Python 後端,`rich` 把畫面渲成 HTML)。
單一英雄、learn-by-doing(做什麼練什麼)、跨八省探索鑽地城。
> ⚠ **本專案已 Web-only**:終端機版(`python3 -m tesrpg`)已移除;`tesrpg/ui/console.py` 現為 Web 的渲染/輸入接層(rich→HTML view-model + `_web_prompt`),5 個輸入原語在無 web backend 時直接 raise。

> **本檔是給 Claude 的開發憲法**:每次改動都適用的節奏 + 跨領域紅線 + 提交檢查表 + 子系統鐵律「索引」。
> 子系統鐵律**本體**在 [handoff.md](handoff.md) §3(以 `R##` 標號);完整現況清單見 handoff §1;設計理念見 [DESIGN.md](DESIGN.md);玩家「怎麼玩」見 [README.md](README.md);**全增益效果目錄(含實際數值/疊加規則,按來源層橫切)見 [BUFFS.md](BUFFS.md)**(改增益常數順手更新);**角色構築縱切盤點(按 build 走完所有層+分支取捨)見 [build.md](build.md)**;**全怪物圖鑑(數據+行為+掉落,由 `tools/gen_bestiary.py` 自動生成)見 [BESTIARY.md](BESTIARY.md)**(改 bestiary 順手重生)。

## 怎麼跑 / 測試

```bash
python3 -m tesrpg.web                     # Web 版(唯一進入點)→ http://127.0.0.1:8080
python3 tests/run_all.py                  # 全綠;模組數見結尾「全部通過 (N 個測試模組)」(別在文件硬寫數字)
python3 tests/browser_ui.py               # Playwright/Chromium 桌面 UI 真瀏覽器回歸
bash check.sh                             # ★ 一鍵驗證:編譯 → 單元/流程 → 真瀏覽器 → 條件式 sim
shopt -s globstar; python3 -m py_compile tesrpg/**/*.py tesrpg/*.py tests/*.py   # 純編譯檢查(完整形;少了 tesrpg/*.py 會漏掉 main/state/formulas 等頂層模組)
PYTHONPATH=. python3 sim_assassin.py      # 平衡回歸模擬(改戰鬥常數後必跑)
```
- Python 3.12;遊戲 runtime 的 `rich` 可由系統套件提供(`python3-rich`);dev 工具走 `.[dev]`。sudo 需密碼、無 `jq`(寫 hook 要用 `python3` 解析 stdin)。
- 真瀏覽器測試是 dev-only:安裝 `.[dev]` 後執行 `python3 -m playwright install --with-deps chromium`;遊戲 runtime 仍只有 `rich`。
- 存檔在 `~/.tesrpg/save.json`(repo 外;煙霧測試/實跑後記得清)。

## 開發流程(每個里程碑走完五階段;⚠ 一次只推一個里程碑)

1. **評估**:理解範圍 / 找根因 / 盤點選項(範圍不明就走 plan-mode:Explore→Plan 子代理)。
2. **決定方向**:給選項、讓使用者拍板(功能性 > 數值;不替使用者預設方向)。
3. **實作**(data + systems + main/ui;內容優先純改 JSON)。
4. **驗證**(全綠才算數;一鍵 a–d = `bash check.sh` / `/check`):
   - a. 單元測試 `tests/run_all.py`(**自動探索** `tests/test_*.py`:新模組/新測試函式皆無需登錄;每模組 `run()` 用 `sorted(globals())` 跑全部 `test_*`;須全綠)
   - b. 真瀏覽器回歸 `tests/browser_ui.py`:Chromium 走正式 `index.html`+HTTP/SSE+`/input`,驗桌面版面/互動/持久化;**不可缺依賴就 skip**
   - c. 平衡模擬:改戰鬥常數跑 `sim_assassin.py`,勝率/回合數不退化
   - d. 無頭煙霧:WebBackend 自動作答驅動 `main()`(或 patch `ui.menu`/`Console(file=StringIO())`)抓 traceback
   - e. 對抗審查(Workflow):多維 fan-out → 獨立懷疑者**對抗式驗證**,只留能真實重現的
   - f. 覆核+修正:逐一覆核(有誤報、也有「會引入新 bug 的錯誤修法」)→ 補回歸測試 → 重跑全套
5. **修改文件 + 提交推送**:同步 `handoff.md`(§1 現況 / §3 鐵律 R##)與必要的 `CLAUDE.md` → **驗證綠即自動 `git commit` & `git push origin main`(本專案慣例,見 handoff §3 R22;不需明說。紅燈則不提交、先修)**。

## 鐵律總則(跨領域紅線,全文)

- **現實邏輯優先於平衡**(R138b 使用者鐵令):機制的字面數值關係不得違反物理/主題直覺(如盾防元素>防物理、布甲>鐵甲、火燒治癒);**即使端到端數學能自圓、平衡實測更好看,違反直覺的設計一律推翻或先問使用者**——平衡在現實邏輯的框架內調,不得反過來(先例:重盾元素卸力恆≤物理,知情接受 gauntlet 100→0% 回落;同族:R45/R115 誓福按原典)。
- **成長/夾限只用 `base_skill()/base_attr()`**;裝備/吸血鬼/斯庫瑪/狼人/里程碑/**達貢之力**加成走獨立疊加層,**絕不寫回 base**。
- **任何改動 `char.equipped`(穿/卸/丟/賣)後必 `stats.recompute_max_resources(char, gamedata)`**(務必帶 gamedata,否則 fortify 視為 0)。
- **改任何戰鬥/施法/刺客/附魔常數 → 必跑 `sim_assassin.py`**,守 `SOLO_SNEAK_DAMAGE_CAP_RATIO`(偷襲不秒 solo boss)、群體規模反制、**fear/paralyze solo boss 機率減免**(`SOLO_CONTROL_RESIST_CHANCE`;R44 改為機率抵抗,非完全免疫)等紅線。
- **所有控場(fear/paralyze/stagger/slow/weaken)施加一律走 `magic.apply_control`**(R44 單一決策點:硬控去重 + 玩家 willpower/solo BOSS 機率抵抗 + 軟控照施);**絕不**就地 `active_effects.append({"kind":控場})`。
- **同源多節點 getter 必聚合**(相加/取最/取後),不得 first-wins 遮蔽。
- **存檔向後相容**:新技能 → `progression.ensure_all_skills`;新里程碑欄 → `ensure_mastery_choices`;新欄位 → dataclass 預設值(`from_dict` 走 `cls(**d)`)。
- **內容優先純改 `data/*.json`**(地點/怪/物品/法術/任務/事件/開局/里程碑),不動邏輯。
- **數量別盲信**:文件中的技能/地點/測試數是快照;有疑義以 `run_all.py` 輸出、`len(gamedata.skills)`、JSON 為準並順手更新。

## 子系統鐵律索引(本體見 handoff §3 `R##`;標籤:`re-sim`=重跑 sim、`recompute`、`migrate`=ensure_*、`save`=存檔相容)

| R## | 一行要旨 | 標籤 |
|---|---|---|
| R01 | 碼/變數/key 英文,玩家文字繁中 | |
| R02 | 新增內容只改 JSON、不動邏輯 | |
| R03 | `to_dict/from_dict` 全欄 + `cls(**d)` 預設 + 防禦夾限;`active_effects` 不入檔 | save |
| R04 | `resolve_attack` 通用任意組合;`clamp_resources` 只玩家側 | |
| R05 | `max_health` 是有效上限;改 equipped 後必 recompute(帶 gamedata) | recompute |
| R06 | `level_xp` 餵升級;`from_dict` 自動 `ensure_level_xp` 別繞過 | migrate |
| R07 | 偷襲 `opening` 條件;副手傷害在 `sneak_mult` 之後;隱遁三道煞車缺一不可 | re-sim |
| R08 | 偵查備戰 `prep_budget`;召喚預載 `battle["allies"]`;prep 在 while 前保 opening | |
| R09 | 運動=旅行加速 + 降戰鬥體力;格擋實扣 `BLOCK_FATIGUE_COST` | |
| R10 | `magic.cast` 玩家專用;扣魔再擷體力;力竭 ×0.75 連 summon HP 也乘 | re-sim |
| R11 | 難度靠 `min_level` + `danger`,**不**縮放怪數值;`solo`/`raw` 旗標 | |
| R12 | 地點帶 `biome`、怪帶 `biomes`;無標籤=通用墊底池 | |
| R13 | `events/quests` 可加 `provinces` 做在地化 | |
| R14 | `fire/frost/shock` 吃 `magic` 抗性;`poison/disease` 不吃 | |
| R15 | 附魔載體 `encha/enchj/enchw/enchws`;`synth` 段數相容;麻痺 solo boss **機率減免**(R44 改,原免疫) | recompute, re-sim |
| R16 | 公會規則在 `factions.py`/資料 `factions.json`;分支任務推進務必保留 `branch` | |
| R17 | AoE 每敵各取**獨立** `make_status_effect` dict(勿共用) | |
| R18 | `apply_origin` **只改處境不動屬性/技能**;別起在地城/`danger≥4`;開局 `quest` 欄自動接起手任務(`is_player` 閘);起手任務 `reach` 不指自身起始地、`collect` 不取起始包物、禁 `clear_dungeon` | |
| R19 | `vampirism.update` 每圈頂端;`vampire_*` 獨立層;疾病抗性削感染 | migrate, recompute |
| R20 | `skooma.update` 在 vampirism 後;亢奮**絕不碰 strength/sneak/武傷** | re-sim, migrate |
| R21 | 門檻只認 `base_skill()`;新 kind 三步登錄(`_IMPLEMENTED_KINDS`+getter+呼叫端);溢盾夾總量 cap | re-sim, migrate |
| R23 | 對話條件複用 `events.meets`(無 state 鍵就地加、需 state 走 `meets_dialogue`);hostile=拒談 `topics_for` 回 `[]`;帶持久 effect 話題必標 `once`(`dialogue_done` 去重防零成本刷分);`faction_standing` 互斥+表態一次性 | save |
| R24 | AI 戰爭 `aiwar.update` 在 worldstate 後/tick_tax 前;決定性 `rng`+`sorted`(迭代序不餵 rng);玩家城只削 garrison 不寫 world_faction(三層免疫);改常數必跑 `sim_worldwar`(防雪球**含玩家選邊**、霸權煞車在外交後套) | re-sim, save |
| R25 | 房產 `house_stash` 不計負重(存穿戴擋免漏 recompute);精神飽滿 `well_rested` 快取只乘 xp 不寫 base;坐騎鞍袋走 `max_weight(char,gd)` 非資源不 recompute;衝鋒不走 `sneak_mult`、受獨立 `MOUNTED_CHARGE_DAMAGE_CAP_RATIO` 夾;戰技僅 `mounted` 旗(野外)+ 第一回合;spear archetype 落安全預設 → 改 combat/formulas 必跑 `sim_assassin` | re-sim, recompute, save |
| R26 | 湮滅危機主線=`source:"main"` + `requires_event`/`requires_faction` gate(`available_quests`)+ `expel_faction` 叛離(`accept_quest`);雙結局都打達貢(滿血 `the_deadlands` vs 削弱 `dawn_sanctum`),用 `kills` milestone 互斥、都 `eradicate_faction` 神話黎明(`<fac>_eradicated` 旗標擋再入會);達貢之力=永久獨立層 `dagon_boon.py`(照吸血鬼);**獨立戰爭=危機後第二幕**(`aiwar`+分裂事件 gate 在 `oblivion_crisis_ended`)→ 改 aiwar 必跑 `sim_worldwar`、加永久屬性層/solo boss 必跑 `sim_assassin` | re-sim, recompute, save |
| R27 | **Web-only**:唯一進入點 `python3 -m tesrpg.web`(終端 `__main__.py` 已刪);`console.py` 是 web 渲染/輸入接層(rich→HTML 退路**不可刪**+ `_xxx_view` 原生 view);5 輸入原語無 backend 即 `raise`,**不可重引入終端 stdin/`IntPrompt`**;測試 patch `ui.*` 或用 `WebBackend` 驅動 | |
| R28 | `pos` 座標與 `links` 連線**皆依正典 TES 地理**(UESP;`world["map"]`40×24,省內 lore 鄰接、跨省**只**經邊境、時數由格距離推導,不入檔);無非預期死路、省內子圖連通、危險度分級(`test_world` 守);加城→rulers+跑 `sim_worldwar`,加地城→dungeons+`clear_dungeon` 委託(`test_polish`/`test_detailing` 守);重建工具 `tools/geo_rebuild.py`(GEO/BORDER_LINKS) | re-sim, save |
| R29 | 城鎮服務專精化:訓練師可教技 = `trainers.json`「skills」覆寫→公會推導系(`world._GUILD_SPEC`)→全技後備,再 ∪ 招牌「master」技;宗師破 `TRAINER_CAP=75`(`≤SKILL_CAP`)、`TRAINER_CAP` 唯一數值旋鈕、不動戰鬥**免 sim**;法師公會法術**每省守一學派 + 保底集 9 道**(純改 `world.json spell_stock`,`imperial_city` 通才,無孤兒);零存檔欄位(`test_world` 守) | |
| R30 | 煉金限時增益藥水:`brew` 出強化屬性/技能/抗元素**限時**藥(`synth brewb`);效果 kind **參數內嵌**(`fattr_*`/`fskill_*`/`resist_*`)→ 共有偵測天然要求同參數;走獨立 `potion_*` 層(`potion_buffs` 權威 + 快取,聚合 attr/skill/entity_resist、**絕不寫 base**),`potion_buff.update` 掛 game_loop(skooma 後)、`ensure_potion_fields` 接載入剔過期;疊加**同(kind,param)取最強+取較晚到期非相加**;`use_item` 加 `state`;可釀池**排除 strength+武器技能 → 免 sim**(放開則必跑 `sim_assassin`) | recompute, save |
| R31 | 毒劑深化:五毒型(DoT/麻痺基礎 + **衰毒 weaken**·**遲緩 slow**〔唯一新 combat kind:降 `_speed`+命中〕·**懼毒 fear**);**修塗毒命中路徑 solo 控制免疫缺口**(paralyze/fear 守 `_is_solo`+去重、charge 接觸即耗);brew 優先序 麻痺>懼>遲>衰>DoT,特殊毒型需 `mastery.poison_unlocks` 解鎖否則退回 DoT(特殊材料皆兼具 damage_health);里程碑 `poison_unlock` kind(R21 三步,**保留 opt_id** toxin_master/potent_poison/venom_lord 改功能解鎖);塗層次數依毒型(控制半量);無新存檔欄 → **改 combat/formulas/alchemy 必跑 `sim_assassin`** | re-sim, save |
| R32 | 煉金效果逐步揭露:材料效果預設 `???`,三源揭露(嚐一口 `taste` 決定性消耗 / 煉製成功 `brew` 回 `learn` 鍵由呼叫端 `reveal` / 技能被動 `passive_reveal` 揭露前 `1+base_skill//25` 個);**純資訊層、brew 數學不變**;通用於任何 kind(自動涵蓋 R30/R31);`known_effects`(ing→[kind])存檔欄 + `ensure_known_effects` 防呆/清陳舊;UI 走 `ui.menu/message`(R27 安全);無戰鬥改動 → 免 sim | save |
| R34 | **技能里程碑深化:冷技能身份化(檔A)**:4 冷戰技補功能 loop(改 9 節點選項,仍 88/22)—— marksman 控場(`weapon_mod` slow〔**必帶 magnitude** 否則 `slow_factor` 硬下標崩 initiative〕+ 傷害線)、block 盾擊宗師(`block_riposte` getter float→dict 聚合 stagger/weaken/counter)、light_armor/acrobatics 閃身反打(**新 kind `on_evade`** R21 三步,閃過敵攻反擊/回體)。**🔴 on_evade 每回合至多一次**(`_evade_counter_used`,回合頂重置)防反制隨敵數線性放大(鏡像 `EVASION_BONUS_CAP`);改 opt_id 走 `ensure_mastery_choices` 退 pending(零存檔欄);動 combat → 跑 `sim_assassin`。**🅑 砍法系無腦選**:alt/myst_50→護盾/結界威力·conj_50→護甲·illusion 50/100→懾心(`fear_on_hit` 單源→聚合夾0.30);**補 `magic.cast` fear/rout/mass_paralysis 對 solo BOSS 免疫**(R31 法術路徑缺口)。**🅒 頂點 apex 化**:6 stat-dump capstone 弱邊升二 apex(passive_armor×3·potion_potency 聚合·新 kind `combat_regen`/`armor_stagger`〔∵玩家不會被控〕);審查修「防守側 stagger turns:1 死時序→2〔含既有 shield_bash〕」+「combat_regen 復活死人→補 is_alive 守」 | re-sim, save |
| R33 | **裝備耐久/修理整套移除**(Skyrim 式;純雜務稅→零決策):刪 `weapon_condition`/`armor_condition`+折損+`_cond_mult`+三修理路徑+`repair_hammer`+`repair_fee`+ mastery `repair_floor`/`combat_repair`;**「armorer」服務(鐵匠站)≠ 技能**——技能 armorer 整條刪(+4 里程碑節點),服務保留(閘 craft/temper/meltdown,不動 world.json/`_SERVICE_CN`);`effective_armor_rating`併入`worn_armor_rating`、`_armor_display`單值;`smithing_50` 換 `thrifty_forge`+`smith_arm`、`forgemaster`→heavy_armor;戰士公會 `repair_discount`→`armory_discount`(`buy_price` 對 `damage`/`armor_rating` 物品折扣,cap 0.35 非免費守不套利);warrior/archer 主修 + nord/orsimer 種族 armorer→smithing/athletics;`from_dict` 剝舊欄+armorer 鍵、`ensure_mastery_choices` 自動剔陳舊選擇;**動 combat → 必跑 `sim_assassin`** | re-sim, save |
| R35 | **技能里程碑深化:去冗餘/修 no-brainer 波次**:4 個二選一弱邊的「死填充/自我重複」純改 `mastery.json`(零新 kind、零新存檔欄、複用既有 kind,仍 88/22)—— acrobatics_100 `deft_roll`(vanish_floor 與 75 tumble 同值被 MAX 遮蔽)→ `wind_step`(`evasion_bonus`,閃避流);light_armor_50/100 `passive_armor` 填充 → `evasion_bonus`(輕甲=閃避非吸收,反擊流 vs 閃避流);scout_50 `skill_fortify` → `threat_read`(`recon_resist_read` 同 mercantile getter)。**🔴 對抗審查自我糾錯**:初版 `whirl_riposte`(on_evade-counter)**換軸重演隱形遮蔽**(被 light_armor storm_dance 0.6 MAX 遮蔽,雜技+輕甲正是最常見閃避 build)→ 審查「改 SUM」**駁回**(破 counter-MAX 防群戰疊加紅線)→ 正解換 SUM-capped `evasion_bonus`。**鐵則:MAX 聚合軸(counter/vanish_floor)上做二選一弱邊極易製隱形 no-brainer → 弱邊優先 SUM-capped 軸(evasion/restamina)。** 改 opt_id 走 `ensure_mastery_choices` 退 pending;動 combat → 跑 `sim_assassin`(綠) | re-sim, save |
| R36 | **security 功能化 + 核心 loop 修復(混合身份:地城入侵+盜賊)**:評估揪出 security 核心 loop 冷(撬鎖只首通、失敗/避陷不給 xp、行竊練 sneak)→ 使用者拍板放寬。**Part1 練功**:`SECURITY_FAIL_XP_FRAC=0.3`,撬鎖失敗也給少量 xp(`dungeon.pick_lock`)、解陷阱避陷 full/觸發 0.3×(`_resolve_trap`,**補 `is_alive` 守:致死不給 xp**=鏡像 combat_regen)。**Part2 寶箱刷新**:CONTAINER 格去 `first_clear` 閘 → 一般寶箱隨機刷新可重撈(=可再生練功);BOSS 首領寶藏維持首通限定。🟡 經濟放寬(逆反「重訪刷寶」),自然閘=開鎖器金幣+時間+游蕩怪。**Part3 perks**:`security_50`→`light_fingers`(**新 kind `theft_skill`**:steal+0.15/賞金×0.5,`crime.steal_chance` 加 gamedata);`security_100`→`thiefs_eye`(**新 kind `dungeon_casing`** 布林:`case_layer` 進層揭全層陷阱/寶箱,UI 靠 console 既有 `^/$` 零工)。`dungeon_casing` 刻意**布林軸**(R35:不落 lock_floor MAX 軸被遮蔽)、與 scout `has_recon_perk` 互補(全層機關 vs 四鄰任意,不揭怪/不發 scout xp)。⚠ **by-design**:theft 掛 security 樹但練 sneak(跨技能盜賊 synergy,使用者已知拍板);零新存檔欄(地城 explored 局部、opt_id 換走 `ensure_mastery_choices`);**security 不碰戰鬥 → 免 sim** | save |
| R37 | **smithing 功能化身份(混合:工匠·省料 vs 鋒銳·淬鍊威力)**:4× temper_cost_free + 2 死填充 → 零身份。**只 1 新 kind `temper_power`**(50+100 sum 聚合);改 50/100 死填充側 → `temper_edge`(0.10)/`temper_mastery`(0.15),25/75 不動(75 = `efficient` 省料 vs `master_temper` cap = 既有健康工匠-vs-鋒銳)。套進 `smithing.weapon/armor_temper_bonus`(**簽名加 gamedata**)= `int(flat×(1+power))`,4 callsite(combat/console)傳 gamedata。**棄案**:野地鐵匠 field_smithing(58 處 armorer 城鎮密度太高 → 邊際無感)、回收 crafting_yield(`int(base*0.5)` floor 吃 factor + craft↔melt 刷 XP 套利)。**R35 軸**:省料(max-float 時序成長線,同 lock_floor 階梯)/ temper_power(float-factor sum)/ cap(int)各獨立。**🔴 必跑 `sim_assassin`**(temper flat 進偷襲倍率前):apex_temper(cap6+power0.25→武器+15)→ solo boss 0%(cap 夾)、精英 oneshot Δ0%、4 敵死亡率隔離邊際 −0.2pp。對抗審查駁 2 誤報(省料時序線非遮蔽、75 cap 是鋒銳側)、補 4 敵群戰隔離測試;Finding(雙持副手淬鍊不套倍率)=既有行為非本輪。零新存檔欄 | re-sim, save |
| R38 | **speechcraft 功能化身份(混合:社交問題解決者 + 戰場號令)**:最冷線(25/50/75 全議價/fortify 影子 mercantile)→ 混合。**2 新 kind `talk_down_lever`/`rally`**,複用 intimidate_floor/skill_fortify/guaranteed_persuade。50 `war_cry`(intimidate_floor 0.30 威嚇喝退)vs `silver_voiced`(skill_fortify 廣抬社交 odds);75 `silver_pardon`(talk_down_lever:衛兵說退賞金上限 120→200+floor)vs `iron_presence`(intimidate_floor 0.45,與 50 取最=lock_floor 式成長線);100 `charm`(guaranteed_persuade)vs `rally`(戰陣號令)。**`rally` 仿騎士戰旗**(main.py `_has_rally`/gate/action/回合末維護,複用 empower 管線零改 combat.py):speechcraft-gate、純耗體 12、empower 固定 0.15(< 戰旗 0.20、MAX 聚合不疊)、**自身零益**(`not _is_player`;單挑死 perk by-design)。`intimidate_floor` `_param`→MAX 聚合;`INTIMIDATABLE` 擴 {bandit,rogue_thief,city_guard}(主要對盜匪);`talk_down_chance` 加 gamedata=None(back-compat)。**社交免 sim**;rally 無 sim 網(solo 刺客無盟友)→ 對標戰旗上界手動論證、sim_assassin byte-identical。對抗審查駁 2 誤報(intimidate MAX=時序成長線非遮蔽、rally 單挑死=by-design 混合)、修 1 誤註;cap+80 由「一次性說退閘」(失敗即 talked=True 無重試)中和。零新存檔欄。**R34–R38 冷線功能化告一段落** | save |
| R39 | **使用者點名兩改:block 盾反 + empower 遞減疊加曲線**:① `block_50` 死填充「盾陣」(block_deflect)→「盾反」(**新 kind `block_reflect`** reflect 0.20/fatigue 6;combat 反震區複用 armor_reflect 路徑,**與重甲反震疊加=32%**〔使用者拍板可疊,自限:反彈隨受傷縮放+體力閘〕)。opt_id 保留 `shieldwall`(內部 id;~10 測試 fixture 沿用)。② empower 多源由 **MAX→降序加權和** `Σ mag×0.7^i`(`formulas.EMPOWER_STACK_RATIO=0.7`,上限≈3.33×最強;戰旗 0.20+號令 0.15=0.305;**單道 byte-identical**),戰旗+號令同時生效、防 SUM 暴衝,順帶修 R38「號令對騎士 redundant」。**🔴 流程教訓**(`ask-before-balance-tradeoffs` 記憶):審查建議「32%→MAX 封 20%」我**一度自行套用**,使用者糾正「平衡取捨必須先問」→ 還原疊加。審查「盾反打遠程」=誤報(bestiary attack 無 ranged 旗標,物理敵攻皆近戰等價)。`sim_assassin` byte-identical(刺客無盾無盟友)。動 combat → re-sim | re-sim |
| R40 | **弓手散兵戰技:由「裝弓即免費」改為 marksman 里程碑解鎖**(使用者點名;里程碑分流身份化,貼 R34–R38)。三式塞進既有二選一(使用者指定映射):`marksman_50` 散兵走位(取代 tracker)vs 牽制射(取代 harrying_shot slow 線)、`marksman_75` 瞄準射(取代 piercing_volley)對 疾矢。**新 kind `bow_technique`**(R21 三步:`_IMPLEMENTED_KINDS`+`has_bow_technique(char,gd,technique)` getter+main.py 呼叫端),option 帶 `technique` 參數;比照 deathmark/rally(戰鬥動作走里程碑)。main.py 把 `archetype=="bow"` 免費閘改成每式各查 `has_bow_technique`;**戰技數學全不動 → sim byte-identical**(sim 直驅 `resolve_attack` 不經選單)。**🔴 skirmish 解鎖依賴**(使用者拍板):散兵走位複用 vanish,**拔掉 `can_vanish`(sneak 25)閘** → 選了 marksman_50 即可用、保留 `vanish_used<vcap` 上限;成功率仍走 `vanish_chance`(sneak+acro 成長/>3 敵陡降/每場 cap)=三道煞車不變,純弓手能用只是成功率低(by-design 跨技能 synergy)。移除 harrying_shot(slow 控場,與牽制射 niche 重疊)/tracker(recon,scout/mercantile 仍供)/piercing_volley(+8%傷)。改 opt_id → `ensure_mastery_choices` 退 pending(零新存檔欄)。marksman 不碰戰鬥常數 → 免 sim(仍跑確認) | save |
| R41 | **雙手武器握法系統 + 鈍器 mace/axe 分流**(使用者點名;三段提交)。① `blunt` archetype 拆 `mace`(控制流·新 `_ARCHETYPE_BUILTIN_STATUS` 命中擊暈 stagger 0.20/1t,守 `not _is_solo`)/`axe`(破甲流·0.30 pen 由 blunt 移此),**skill 全維持 `blunt`**(含 2H 戰錘/戰斧亦算鈍器;mastery 樹不動);1H 戰斧→短斧、daedric_mace 名→魔族釘錘、wuuthrad arch war_axe→axe(全保 id 零存檔破壞)。② **雙手武器**(`two_handed` 旗,戰錘 mace/戰斧 axe 各 5 階 +45~55%傷·wuuthrad 轉 2H):`inventory.is_two_handed`+`equip_weapon` 裝 2H 自動卸盾/副手+`equip_offhand`/`equip_armor`/`is_dual_wielding` 主手 2H 擋+**新 `ensure_grip`**(載入正規化,零新欄);main 抑制格擋/盾牆、mounts 排除衝鋒。③ **雙手重盾**(`great_shield`+`two_handed`+`bash_damage`+`mitigation`,5 階+crusaders_ward 轉重盾):`combat._weapon_profile` 加盾擊分支(bash·block 技·完全取代武器,守同 beast/bound)、`great` 旗 → `archetype=None`、新 `_great_shield_mitigation_factor`(套 `_shield_wall_factor` 後·乘性·僅物理·`not _is_beast`)、`is_great_shield` None-safe;仍可格擋/盾牆。**🔴 三提交每個跑 `sim_assassin` byte-identical**(刺客匕首非 mace/axe、不持 2H/重盾 → 全 no-op);solo 夾/85% 甲夾/麻痺免疫全守。recipes/world/dungeons 補取得(skill_req 守分級、值守無套利)。`run_all` 64 模組(+test_two_handed/test_great_shield) | re-sim, save |
| R42 | **反傷流功能化:反傷吃 raw(解耦護甲)+ 荊棘附魔身份**(使用者點名+逐項拍板參數)。反傷由吃 `dmg_done`(耦合·護甲越高反越少)改吃**「攻方完整物理輸出(連格擋前)」`raw/block_factor`**(解耦護甲/盾牆/重盾/格擋)→ 龜也能反出有意義傷;物理限定 + player-only(直接 `_set_hp` 非遞迴 → 無 A→B→A 環)。三源相加單次結算:重甲反震 0.12→**0.06** + 盾反 0.20/6→**0.10/10體**(力竭不計)+ **新 `thorns` 荊棘護甲附魔**(盔/胸/手/靴/盾各一條·反傷%=靈魂石階×1%·不吃 myst/potency·`inventory.thorns_reflect` 聚合 5 槽 /100)→ max **0.41 of raw**。**🔴 不夾(使用者拍板)**:紅線靠①物理限定(元素 solo boss〔湮滅系全元素〕反傷流完全不反=天然剋星)②盾反體力閘③物理敵 raw 上限小(最強 raw41×0.41=17<物理 boss HP120+→永不一擊反殺)自然守;反傷=反應式非偷襲→不碰 solo cap。touch:mastery.json/combat.py(反傷區)/enchanting+synth(thorns kind)/inventory getter/main UI。`sim_assassin` byte-identical(反傷只防守側·刺客無投入)。**前瞻**:無夾,日後若加高物理 raw 敵(>~300)再評估加夾 | re-sim |
| R43 | **怪物攻擊多樣化:多攻擊模式曲目 + on_hit 控場放寬(全控)**(使用者點名「多種攻擊模式·元素怪也要有物理手段」+ 拍板全控 fear/paralyze + 雙軌防禦)。① **攻擊曲目**:bestiary 可選 `attacks`(現有 attack 形狀 list + `weight`/`when`〔hp 階段閘〕/`cooldown`),保留 `attack` 後備;`Creature.attacks` 欄(**戰鬥暫態→零存檔遷移**);新 `combat.choose_attack(creature,rng)` 加權+階段+蓄力選招(cooldown 靠 active_effects `atk_cooldown` 標記 + 既有 `tick_effects` 遞減,未知 kind 靜默);`spawn_boss` 縮放套全曲目;呼叫端 main(敵/同伴)、`auto_resolve`、`sim._round` 走 choose_attack。② **resolve_attack 加 `attack=None`**:`atk=(attack or attacker.attack) if not player else None`,5 讀取點(_weapon_profile/element/on_hit/infect×2)改讀 atk,**玩家路徑不變→sim byte-identical**;事件加 `attack_name`(玩家 None)。③ **on_hit dispatch 放寬**:依 `oh["status"]` 分派 dot→`_apply_dot_capped`/控場(weaken/slow/stagger/fear/paralyze)→`make_status_effect`+**控場去重**防連鎖(**零新 kind/getter**,複用 R31 中立 kind)。④ **🔴 雙軌防禦**:on_hit 在 `if hit:` 內→**落空連控帶傷全免**(閃躲第一道·通用);命中後 fear/paralyze 吃 willpower `resisted_mind` 第二道·軟控命中即中。**兩功能修復**:main 回合頂 `is_incapacitated(player)` 閘(麻痺/恐懼跳玩家行動,否則硬控 no-op)+ `weaken_factor` 去 `not _is_player` 守改**對稱**(無耗弱×1 byte-identical,否則怪→玩家 weaken 失效)。⑤ **內容紀律**:控場 chance≤0.30·硬控 turns1 去重·軟控 turns2;**硬控只放中/高危**,早期 trash(rat/wolf/skeleton/bandit)不放控(守 sim 群戰乾淨);curated 19 隻→**後續補 d5 全 12 + d4 全 16(主題分流:法系 fear、純野獸軟控)+ d3 全 24(更輕;3 隻 sim 樣本怪〔強盜/骷髏/吸血鬼新血〕配曲目=唯一破 byte-identical→傷害多樣無控、群戰死亡率僅+~1pp、solo/oneshot 仍 byte-identical;改其曲目必重跑 sim)。共 71 有曲目、15 缺〔d1-d2 trash 留單招〕;見 `BESTIARY.md`**。`run_all` 65(+test_monster_attacks);`sim_assassin` **byte-identical**(diff HEAD 證:玩家攻擊未動 + sim 群戰怪未配曲目 + solo 段為玩家偷襲)。⚠ **CC-on-player 平衡非由 sim 量**→保守內容+去重+雙軌+煙霧守 | re-sim, save |
| R44 | **BOSS 控場「機率減免」+ 控場施加集中化(收斂 stagger 不一致)**(使用者點名:fear/paralyze 對 solo 由「完全免疫」改機率抵抗 + 把所有控場集中成一個 helper)。**新 `magic.apply_control(target,kind,gd,rng,*,magnitude,turns,source)`** = 唯一決策點,回 `applied/resisted/blocked`:硬控(fear/paralyze)去重 + 玩家 `resisted_mind`(willpower)/ solo BOSS `rng.chance(formulas.SOLO_CONTROL_RESIST_CHANCE=0.65)` 抵抗;軟控(stagger/slow/weaken)照施(無 solo 免疫)+ 可選 `source` 同源去重。**~18 施加點全路由到 helper**:combat(塗毒/附魔麻痺/chill·jolt rider/on_hit_status/**鈍器內建 stagger〔收斂點:移除 mace-only `_is_solo` 免疫,boss 比照其餘 stagger 照吃〕**/盾反/fear_on_hit/armor_stagger/on_evade/偷襲殘響/R43 怪→玩家)、magic(cast fear·weaken/AoE/**潛在缺口 damage_status·spell_on_hit·apply_status 閉合**)、`powers.paralyze_touch`(**戀人座破口閉合**:對 solo 由保證 3 回合鎖改機率減免)、`lycanthropy.howl`、main 牽制射。dot/soul_trap/deathmark/反傷扣血**不**走 helper。**🔴 改 R31/R15「solo 完全免疫」→ 機率減免**(更新 5 舊免疫測試為機率模型 + 新 `test_solo_control`)。`run_all` 66;`sim_assassin` **byte-identical**(刺客無 fear/paralyze/stagger 投入·偷襲傷害夾與控場無關→solo 秒殺仍 0%)。⚠ 機率減免後長時程控場(戀人座 3 回合)35% 命中仍鎖 3 回合 → 過強再加「solo 控場時程上限」或調 `SOLO_CONTROL_RESIST_CHANCE` | re-sim, save |
| R45 | **後期內容:戴德拉誓福引擎 + 神殿任務(首位親王 阿祖拉)**(使用者兩道拍板:方向=戴德拉親王神殿任務+神器/誓福線;節奏=一次一位親王·任務做深·reward=神器或永久 Buff)。共用底座建一次 + 首位親王深線。**通用誓福層(approach A:平行附加層,刻意不動 `dagon_boon.py` 零遷移)**:Character 加 `boons`(權威 list)+ `boon_attr/skill_bonus·boon_resist·boon_magic_bonus`(推導快取);`attr()/skill()/magic.entity_resist()/stats.max_magicka` 各加**一項** boon_* 聚合(**每個 dagon_* 點皆有對位·parity 已審**)、**成長/夾限只用 base_***、絕不寫 base。新 `systems/boons.py`(`grant/apply_to_character〔冪等·末尾必 recompute·未知 id 略過〕/ensure_boon_fields`)+ `data/boons.json` 登錄表 + `state.py` 載入鉤。`quests._complete` 的 `grant_boon` 改派發(`"dagon"`→legacy·其餘 ∈ gamedata.boons→`boons.grant`)。**神殿機制**:地點帶 `"shrine":"<prince>"`(零遷移)+ `main.action_shrine`(供奉→`available_quests("daedric")` 篩 shrine→複用 `_accept_and_brief`)+ `available_quests` 加 `requires_level/requires_fame` 向後相容門檻。**阿祖拉(Skyrim「黑星」)**:復用 `azuras_coast`+新 degree-2 地城 `azura_defiled_shrine`(d5·海岸環)+新 boss `malyn_varen`(d5·solo·raw·frost·R43 曲目 paralyze chance0.25/turns1 走 R44 減免)+深線 `azura_star`(`source:"daedric"`·`requires_level:15`·分支 淨化→`grant_boon:"azura"`〔晨昏之佑·**守紅線無 sneak/武傷**〕vs 墮化→神器 `black_star_amulet`)。**🔴 誓福不碰 sneak/武傷·strength≤達貢 → `sim_assassin` byte-identical**(stash diff 證·sim fixtures boons=[]);boss solo+raw·硬控≤0.30/turns1。`run_all` 67(+test_daedric)。**每里程碑純 JSON 加一位親王**(✅ 已做 #2 **莫拉格巴爾**〔恐怖之屋·復用 molag_mar·臣服得 mace_of_molag_bal〔吸生命鈍器·走既有 weapon_status absorb_health·solo 受夾〕vs 反抗得 molag_defiance 誓福〕、#3 **海爾辛**〔血月狩獵·瓦倫森林 grahtwood·純野獸 boss 軟控·狩成得 hircine_blessing 誓福 vs 奪皮得 saviors_hide〔輕甲魔抗·artifact 不帶 material〕〕、#4 **波耶西亞**〔陰謀者試煉·天際 falkreath_wood·重甲戰士 boss·試煉得 ebony_mail〔重甲·`thorns` 荊棘反傷 8%·走既有 R42 路徑〕vs 拒血得 boethiah_resolve 誓福〕、#5 **克拉維克斯·瓦爾**〔言靈之犬·高岩 stormhaven·**開社交/口才軸**·法系 boss·獻祭得 masque_of_clavicus_vile〔helmet·`fortify_skill speechcraft 25`·走既有 `_apply_enchant`〕vs 守諾得 clavicus_silver_tongue〔personality/speechcraft 純社交〕誓福〕、#6 **佩雷丽特**〔唯一的解藥·黑沼澤 shadowfen·**盾/防魔軸**·法系瘟疫 boss·役疫得 spellbreaker〔1H heavy 盾·`resist_element magic 45`·非 great_shield〕vs 淨疫得 peryite_ward〔endurance/block/magic+disease〕誓福〕、#7 **梅赫拉**〔低語之門·賽羅迪爾 blackwood·**暗影/魂刃軸**·織影蛛母 boss·弒密得 ebony_blade〔weapon·`weapon_status vampiric 50`·**archetype sword 非 dagger/bow → 零偷襲加成守紅線**〕vs 封密得 mephala_shadow〔agility/**illusion〔魔法學派〕**·非 sneak/武器技能〕誓福〕、#8 **桑格玖完**〔永不散場的醉宴·艾爾斯維爾 rimmen_desert·**召喚師軸**·醉宴主 boss·縱酒得 sanguine_rose〔staff·skill conjuration·`on_hit_self magicka 12`〔命中回魔→召喚續航,走既有 magicka_staff 路徑〕〕vs 清醒得 sanguine_revel〔conjuration+12〔已見於達貢誓福〕/magicka〕誓福·**召喚師軸純 JSON 達成不需新 summon 機制**〕、#9 **馬拉凱斯**〔棄絕者的詛咒·漢默法爾 dragontail_peaks〔**補齊最後一省 → 八省神殿全到齊**〕·**雙手武器軸 R41**·純物理 boss 棄絕者酋長〔solo·raw·無元素·stagger0.30/fear0.25-1·物理反傷流秀場〕·臣服得 volendrung〔沃倫鎚·**two_handed mace·damage38〔2H 之冠〕·`weapon_status absorb_fatigue 15`**〔命中回氣力→2H 續航·走既有 absorb_fatigue 路徑·**blunt 非 dagger/bow 零偷襲守紅線**·treasure 放 ebony_warhammer 非神器〕〕vs 抗咒得 malacath_might〔棄絕者之力·**strength+10〔首個給力量的誓福·≤達貢 18+18〕**/endurance+8/**smithing〔工匠技能非武器〕**/poison+disease〕誓福·`requires_level:18`·與 boethiah_resolve〔heavy_armor〕靠唯一 strength 區隔〕、#10 **梅瑞迪雅**〔破曉之燈·賽羅迪爾 gold_coast〔陽光海岸·賽羅迪爾第二座神殿,比照晨風雙神殿〕·**恢復/聖光 paladin 軸**〔使用者拍板〕·冰霜死靈法師 boss meridia_defiler〔solo·raw·無火抗→破曉者剋之·fear0.25-1〕·執刃得 dawnbreaker〔破曉者·**blade sword·dmg24·`weapon_status burn` 火焰 DoT 10/3t**〔刻意用 burn DoT 而非 dawnfang/skyburner 的 weapon_element·走既有 ench_dot 路徑·**sword 非 dagger/bow 零偷襲守紅線**·treasure 放 generic flame_staff·**非 skyburner**〔dragon_lair 唯一戰利·test_oblivion 守·初版誤用已修〕〕〕vs 守光得 meridia_light〔光之佑·personality+8/willpower+6/**restoration+10/light_armor+6**/fire+magic/magicka+15·無 strength/sneak/武器技能〕誓福·`requires_level:18`〕、#11 **瓦瑪納**〔甦醒的夢魘·天際 the_pale_tundra〔蒼原·晨星城旁=夜喚神殿正典地·天際第二座神殿〕·**純元素破壞法系軸**〔使用者拍板·最大未覆蓋戰鬥軸〕·電擊/冰霜夢魘術士 boss dream_devourer〔solo·raw·仿 wish_eaten 法系·fear0.25-1〕·執顱得 skull_of_corruption〔腐敗之顱·**staff·destruction·`weapon_element shock 32`**〔走既有 staff 路徑·同 staff_of_magnus/daedric_staff·dmg11·**staff 非 dagger/bow 零偷襲守紅線**·treasure 放 generic daedric_staff 非神器〕〕vs 破夢得 vaermina_nightmare〔夢魘之佑·**intelligence+8/willpower+6/destruction+10**〔**首個給 destruction 的誓福**·達貢 legacy 除外〕/shock+15/magic+10/magicka+20·無 strength/sneak/武器技能〕誓福·`requires_level:18`〕、#12 **娜米拉**〔饕餮之宴·瓦倫森林 malabal_tor〔綠約食人習俗=娜米拉正典題·瓦倫森林第二座神殿〕·**煉金/毒藥匠軸**〔使用者拍板〕·物理/毒食人 boss carrion_priest〔solo·raw·resist poison/disease40·fear0.25-1〕·赴宴得 ring_of_namira〔娜米拉之戒·**items.json jewelry·slot ring·`fortify_skill alchemy 25`**〔走既有飾品 fortify_skill 路徑·同 black_star_amulet/skeleton_key·**jewelry 非武器零偷襲**·treasure 放 generic glass_dagger 非神器〕·比照 Clavicus 範式〕vs 淨化得 namira_decay〔腐朽之佑·**agility+8/endurance+6/alchemy+12**〔**首個給 alchemy 的誓福**·工藝技能非武器〕/poison+20/disease+15·無 strength/sneak/武器技能〕誓福·`requires_level:18`·**🔴 alchemy 不餵偷襲倍率**〔毒=獨立 DoT 受 R31 夾·偷襲秒殺受 SOLO_SNEAK_DAMAGE_CAP_RATIO 夾,與 alchemy 無關〕→ byte-identical〕、#13 **謝歐格拉斯**〔癲狂之心·黑沼澤 hist_grove〔希斯特致幻=瘋神正典題·黑沼澤第二座神殿〕·**瘋神/混沌軸·R46 新機制**〔**第一個非純 JSON 親王**〕·混雜 fire/frost/shock boss madness_avatar〔solo·raw·fear0.25-1〕·執杖得 wabbajack〔瓦巴賈克·**staff·`weapon_status "wabbajack"` 命中隨機混沌六效果**〔走既有 helper:burst solo 夾/control 走 apply_control/回火非遞迴〕·**staff 零偷襲·玩家不可鍛造**·treasure 放 generic daedric_staff〕vs 守心得 sheogorath_madness〔瘋癲之佑·**alteration+12〔首個 alteration 誓福·補最後一個未覆蓋魔法學派〕**/willpower/intelligence/magic/magicka·無 strength/sneak/武器技能〕誓福·`requires_level:18`〕;**翻轉道德軸**·**八省全覆蓋** 法/戰近/遊俠/重甲反傷/社交/盾防/暗影魂刃/召喚師/雙手武器/恢復聖光/元素破壞/煉金毒藥/瘋神混沌/盜賊 security/**秩序偵查** build〔#14 見 R47·#15 見 R48〕;⚠ **流程教訓**:審查驗證代理在共享工作樹跑 `git stash` 競態誤 revert 資料檔險些提交 RED → 自此 byte-identical 改用隔離 worktree、審查 prompt 禁用 mutating git。**末位 Hermaeus Mora 已做(R49·Oghma 無限祕典三徑書)→ 17 親王神殿線全集完成**〔#14 見 R47·#15 見 R48·#16 見 R49〕 | recompute, save |
| R47 | **灰狐面具 `talk_down_cap` + 盜賊神器換手(使用者點名:盜賊公會神器改灰狐面具〔提高可協商罰金〕·骷髏鑰改 Nocturnal 神器)+ 第十四位親王 Nocturnal**(補**最後一個未覆蓋軸:盜賊/security**·非戰鬥**免 sim**)。**新機制=衛兵說退賞金上限多一個裝備來源**:`mastery.talk_down_mod`(R38 唯一聚合點,被 `main.py` cap 公式 + `dialogue.talk_down_chance` floor 讀)改聚合**里程碑 `talk_down_lever`(silver_pardon)+ 裝備 `enchant.kind=="talk_down_cap"`**(cap_bonus 相加·floor 取最)·**callsite 零改**·新 kind 不入 `_apply_enchant`(走 talk_down_mod 專屬掃 `char.equipped`)·**🔴 back-compat 無源回 `{}`**。`gray_fox_mask`(armor.json helmet·`talk_down_cap cap_bonus 120/floor 0.25`)→ tg5 兩分支 reward〔test_guild_depth arts 改 tg5→gray_fox_mask〕。**`skeleton_key` 定義零改**(amulet·security 20·`skeleton_key:true` 旗標→`dungeon.has_skeleton_key` 認旗標非來源·撬鎖必成機制不變·test_dungeon 續綠)·只換發放來源(tg5→Nocturnal `twilight_sepulcher` 竊鑰分支)·仍單一來源不成孤兒。Nocturnal:`tenmar_forest`(艾爾斯維爾)加 `shrine`+新地城 `twilight_crypt`(d5·pos[22,22])+boss `twilight_sentinel`〔solo·raw·敏捷物理暗影遊俠·fear0.25-1〕;**竊鑰**得 skeleton_key+惡名 vs **守鑰**得 `nocturnal_shadow`〔夜影之佑·agility+8/speed+6/**security+12〔首個 security 誓福·盜賊技能非武器/sneak〕**/magic+10·無 strength〕誓福·`requires_level:18`。`sim_assassin` **byte-identical**(security/talk-down 皆非戰鬥·誓福 fixtures boons=[]·隔離 worktree 證);`run_all` 67(test_speechcraft +1·test_guild_depth 改 1·test_daedric +5) | save |
| R48 | **秩序之劍 `order` 附魔:移除傷害變異(第十五位親王 Jyggalag 神器·反 Wabbajack)**(使用者選 Jyggalag〔秩序〕= Sheogorath〔混沌〕對立面;碰 `combat.py`/`formulas.py` 極小單點)。`formulas.py` 抽 `DAMAGE_ROLL_LO=0.85`/`DAMAGE_ROLL_HI=1.15`(原 `rng.roll` 引數同值);`combat.py:469` 傷害 roll 改 `if 玩家 and not beast/bound/great and 武器 enchant.kind=="order": roll=DAMAGE_ROLL_HI else rng.roll(LO,HI)`(gate 比照 berserk@530)。**🔴 byte-identical**:非 order 武器走原 `rng.roll`(同值·同一次抽)→ rng 序零變;order 分支不擲 rng 但**僅 order 劍可達**·sim 持匕首走 else;新增 `gamedata.item(...).get` 純 dict 讀無 rng(short-circuit 非玩家/獸形/束縛/重盾不呼叫)。**🔴 紅線**:always-max 流進 attack_damage 在 solo 偷襲夾(combat.py:562)**之前** → solo 仍受夾·sword 偷襲 ×1.0(比匕首弱)→ even maxed 永不秒殺 solo(專測)。`sword_of_jyggalag`(1H sword·dmg26·`enchant {kind:"order"}`·僅任務 reward)·誓福 `jyggalag_order`〔秩序之佑·intelligence+8/willpower+6/**scout+12〔首個 scout 誓福〕**/magic+15/magicka+10·無 strength/sneak/武器〕。always-max ≈ 平均 +15% + 零變異(可靠性換隨機)。`run_all` 67(test_daedric +6);隔離 worktree 法定 byte-identical。⚠ 測 order 零變異須固定體力+技能(攻擊給 blade xp 會讓傷害爬升非 roll 變異) | re-sim |
| R46 | **Wabbajack 命中隨機混沌效果(瘋神謝歐格拉斯神器·第一個 R45「需新機制」親王)**(使用者拍板評估新機制 → 選 Sheogorath;碰 `combat.py`/`formulas.py`,非純 JSON)。**新 `weapon_status` kind `"wabbajack"`**:`formulas.py` `WABBAJACK_*` 常數 + `WABBAJACK_TABLE` 權重表(放 `WEAPON_ABSORB_SOLO_FACTOR` 旁紅線對位);`combat.py` 武器附魔 dispatch 末尾加 `elif st=="wabbajack"`(仍在 `if _is_player and not beast/bound/great` 內 → **怪/獸形/束縛/重盾、與 sim 匕首〔enchant=None〕永不進入**)·累加和 `rng.roll` 抽選**六效果各走既有安全 helper**:burst(隨機 fire/frost/shock·`resist_multiplier`+`_scaled_damage`+`_set_hp`·`is_alive`·solo ×`WABBAJACK_BURST_SOLO_FACTOR=0.5`)/control(隨機 fear/paralyze/stagger→`apply_control(source="wabbajack",turns=1)`·solo 機率抵抗+去重自動)/self_restore(回自身 clamp `max_<stat>`)/weaken(0.20/2t)/backfire_self(`max(1,…)` 不致死)/backfire_enemy(治敵 6 自限)。**🔴 紅線靠「複用 helper + 直接賦值非遞迴」**:爆發非偷襲傷·staff 零 sneak·solo 受夾;硬控走中央 helper;回火**絕不呼叫 `resolve_attack`(無環)**;玩家**不可鍛造**(`"wabbajack"` 不入 `enchanting._WEAPON_STATUSES`,測試鎖)。**回火 16% 權重=自平衡** → 永不嚴格最優。**`sim_assassin` byte-identical**(隔離 worktree 法定·新分支 append-only 且僅 `weapon=="wabbajack"` 可達·sim 持匕首不觸發·不擾既有 rng 序);`run_all` 67(test_daedric +7 測)。⚠ self_restore 刻意**不夾**(不扣敵·非無限回血泵·隨機+回火制衡)→ 過強再評估 solo 夾或調權重 | re-sim |
| R49 | **戴德拉神殿線 末位親王 Hermaeus Mora(禁忌真知)+ Oghma 無限祕典三徑機制(17 親王神殿線全集)**(使用者兩道拍板:① 新機制=Oghma 禁書·三徑選一;② 純多徑選路·無抗拒)。**新機制(碰 `main.py`·非純 JSON)**:新 item `kind:"book"`+`effect.type:"grant_choice"`(`oghma_infinium`·`paths`→3 boon);新 `main._read_book`(比照 `action_shrine`·R27 UI 留 main 不入 systems)→ `ui.menu` 三選一 → `boons.grant`(末尾自帶 recompute)+`inventory.remove_item`(書消耗·Mora 收回);**未選(返回)不消耗**可日後再讀。`action_inventory` 加 `("read","閱讀")`+dispatch(鏡像 use/dose);`inventory.use_item` 零改(`kind!="potion"` 已回 None)。**消耗品→選單→永久誓福**為全新層(原只有限時藥水)。地理:`rivenspire`(高岩裂石郡·**高岩第二座神殿·八省各 2**)加 `shrine:"hermaeus_mora"`+新 d5 地城 `forbidden_archive`(pos[5,3]·雙向 links rivenspire+wendir·守 R28)+boss `apocrypha_seeker`〔solo·raw·215HP·觸鬚物理+shock dot/weaken/fear0.25-1·控場≤0.30/turns1 走 R44〕。三徑誓福(讀書三選一·**皆守紅線無 sneak/武器技/strength**):`hermaeus_might`〔end/heavy_armor/block/fire=坦克鍛造〕/`hermaeus_shadow`〔agi/speed/**acrobatics〔首個〕**/light_armor=機動非潛行〕/`hermaeus_magic`〔int/will/destruction/mysticism/magicka=法系〕。任務 `forbidden_knowledge`(`requires_level:18`·**線性無分支** collect moonstone_ingot×2→reach→clear→reward `items:["oghma_infinium"]`·**無 grant_boon**·誓福改由讀書授予)。**🔴 byte-identical**:未碰 combat/formulas·誓福 fixtures boons=[]·新 item/boss/地城不在 sim 路徑(隔離 worktree 法定)。零新存檔欄(書=物品·誓福=`char.boons` 字串·`ensure_boon_fields` 遷移)。`run_all` 67(test_daedric +5);BESTIARY.md 重生(116 怪) | recompute, save |
| R50 | **血月詛咒深化:吸血鬼/狼人(月相 + 主動獸性 + 識破圍捕)**(戴德拉線完結後評估下一步;使用者選方向=詛咒血脈深化、拍板聚焦=完整血月包三件互扣)。補 handoff §6 列的 4 缺口·**零新存檔欄**·`sim_assassin` **byte-identical**(刺客匕首 build 永不化身詛咒,同 R45 fixtures·隔離 worktree 法定)。**① 月相**:新 `systems/moons.py`(`LUNAR_PERIOD_DAYS=24`·八相·`is_full_moon`/`is_new_moon` 各 3 日窗·純由 `time.absolute_hours()//24` 推導 → 決定性零遷移);`state.GameTime.label()` 附月相(moons 零依賴→無循環 import)。**② 狼人主動變身+月相**:`powers.beast_form` `contexts` 加 `"utility"`→hub 平時 surface 主動獸化(複用 `action_use_power`→`transform`);`powers.available`+`can_transform` 加**滿月免每日冷卻**(比照 `has_hircine_ring` 旁路);`effective_duration`=base+`FULL_MOON_DURATION_BONUS=2`(滿月時程·**只動時程不動戰鬥數值**);hub 加「變回人形」action(複用 `revert`)。**③ 雙血脈識破→衛兵圍捕**:新 `main._curse_manhunt`(複用 guard resist 分支 `spawn_creature("city_guard")`×N+`run_battle`+`crime.add_bounty`+infamy·只城/鎮·互斥):獸形入城 `_BEAST_TOWN_MANHUNT_CHANCE=0.85`、高階吸血鬼 `vampirism.detection_chance`(shunned·base0.25+0.15/階·滿月+0.15·cap0.7);掛抵城(`guard_confrontation` 旁)+城內主動變身後。月相連動:滿月↑識破、新月↓`feed` 被撞見(`NEW_MOON_STEALTH_BONUS=0.10`)。進食壓階/變回人形即規避=可管理 loop。**🔴 平衡**:滿月只給可用性/時程·圍捕用既有 city_guard → byte-identical;curse build 平衡走 by-design+煙霧(同 R43);「滿月強化 beast 戰鬥數值」屬平衡取捨**刻意不做**(留待先問)。`run_all` 68(新 `test_curse_depth`) | re-sim, save |
| R51 | **詛咒巢穴與同類:吸血鬼隱穴 + 狼人巢穴(安全區·同類 NPC·招募 kin)**(R50 讓城鎮危險→使用者選後續=巢穴與同類,R50 的避風港+社群對位)。**極高複用、近純資料·零新存檔欄·`sim_assassin` byte-identical**(solo 刺客無盟友→kin 同伴/巢穴不在 sim 路徑·隔離 worktree 法定)。**巢穴閘**比照 R45 `shrine`→`action_shrine`:地點加 `"lair":"vampire"|"werewolf"` 欄(零遷移)·hub `_player_is_lair_kin` 對應詛咒者才現「進入巢穴」→ 新 `main.action_lair`·凡人/異種只見沉寂(`_living_npcs_at` 在 lair 回 `[]`·lair 抑制探索·danger 1 守 `test_world`)。**`action_lair`** 複用:`休息`(`housing.set_well_rested`)/`密窖`(`housing.stash[巢穴]`+`_stash_transfer`·**不需 owns**)/`與同類交談`(`dialogue.offered_quest`→`_accept_and_brief`);吸血鬼 `安心進食`(`vampirism.feed(safe=True)` 新旗略過 caught+賞金)、狼人 `安心獸化`(`action_use_power`→transform)。**同類 NPC+招募**(純資料):血族族長/頭狼(`npcs.json` `quest` 欄)→ `recruit_blood_thrall`/`recruit_pack_warrior`(`source:"npc"`·`reward.companion`)→ `blood_thrall`/`pack_warrior`(`companions.json`·`cost:0`·`recruit_quest` 回指·數值對齊既有)→ `party` 招募。**地理(R28)**:`bloodmoor_crypt`(高岩·吸血鬼傳說地)/`moonhowl_den`(瓦倫·海爾辛狩獵地)·轉化時加巢穴線索。**🔴 安全區**:巢穴非城/鎮 → `_curse_manhunt` 天然不觸發(零改 R50);**城鎮仍危險**(避風港非城內豁免→R50 loop 不破)。`run_all` 69(新 `test_curse_kin`) | re-sim, save |
| R52 | **血族階級政治 + 巢穴升級:吸血鬼血族 / 狼人獵群階級梯**(R51 巢穴/同類深化成詛咒陣營階級梯;使用者拍板:晉階回報**含詛咒戰鬥強化**·頂階**弒主篡位 vs 忠誠繼承**)。**極高複用**:整套 `factions` 階級系統(`join`/rank/`available_quests("guild")`/`quests._complete` 晉升/`perk_value`+`_best_perk`)·**零新存檔欄**(`char.factions` dict)。**curse-gated 陣營(不改 factions.py)**:`coven_vampire`/`werewolf_pack`·`gate_skills:[]`/`join_skill:0`/`lawful:false`→技能/賞金閘恆過·**curse 閘=巢穴**(`action_lair` 只詛咒者可達)·`hall`=巢穴(無 city service)。**2 新 perk kind(只詛咒模組讀·非會員=0)**:`sun_ward`(`vampirism.expose_to_sun` 灼傷×(1−ward)·cap0.6)·`beast_vigor`(`lycanthropy.effective_duration` 獸形時程+`devour` 回血·cap0.6)·`factions.py` 各一行 accessor。**階級任務**(quests.json·source guild·逐階閘):`coven1/2`·`pack1/2`(血族↔狼人宿仇)+`coven3`/`pack3` **分支頂階**(`faction`+`rank:2` 頂層→晉升不分支·branches 篡位/繼承皆 `kill <master>`)。**頂階決鬥**:`action_lair`「血族/獵群事務」(比照 `action_guild_hall`·頂階已接→`run_battle` vs master→`record_kill`+`check_completion`)·2 新 boss `coven_patriarch`/`dire_alpha`(solo·raw·fear0.25-1)·**不新增地城**。**頂階誓福(R45 boons·守紅線)**:`coven/pack_usurper`(infamy)vs `coven/pack_heir`(fame)·無 sneak/武器技·strength≤8。**巢穴升級(rank-gated)**:rank≥1 血泉/獵壇(即時全回)·rank≥2 補給(治療藥水補至3)。**🔴 sim byte-identical**(perk 只詛咒模組讀·刺客永不入會·隔離 worktree 法定);curse build 平衡 by-design(perk cap0.6·誓福守紅線·boss cc≤0.30)。`run_all` 70(新 `test_curse_rank`) | re-sim, save |
| R53 | **疾病系統 + 統一治癒(含吸血/狼人潛伏期解除)**(評估揪出無普通病 + 潛伏期不可提前治癒;使用者拍板:治癒=藥水+神殿+恢復法術·疾病=更重含惡化/DoT)。**新 `systems/diseases.py`+`data/diseases.json`(仿斯庫瑪戒斷負層)**:6 正典病(rockjoint/bone_break_fever/brain_rot/witbane/ataxia/swamp_rot)·`char.diseases` 權威 + `disease_attr/skill_penalty` 負值快取(**只疊 attr()/skill()·絕不寫 base**)·`contract`/`update`(惡化每N日加深·cap + DoT 每 per_hours 扣血·**clamp≥1 不致死**)/`purify`/`ensure_disease_fields`。Character +3 欄(dataclass 預設·遷移)。**感染**:`combat.py` on-hit `atk["disease"]`(受 `entity_resist` disease 削弱·吸血鬼/狼人 disease:100 免疫)→ `infect_kind="disease"`+`disease_id`→`run_battle` 分派 `diseases.contract`;`disease` 欄**只加 6 非 sim 怪**(giant_rat/swamp_leech/jungle_toad/moor_hound/sand_adder/moor_witch;sim 用 bandit/skeleton/vampire_fledgling/dremora/frost_troll/wolf)。**統一治癒三途**(`diseases.purify`=cure_all + `vampirism/lycanthropy.cure_infection`〔新·只重置 `infected_day`、**不解已轉化詛咒**〕):藥水 `cure_disease_potion`(use_item 新 effect·42 城)/神殿 `action_disease_cure`(免費·比照 skooma_cure)/法術 `cure_disease` 淨疫術(magic.cast 新 kind·7 公會)。**🔴 sim byte-identical**(動 combat/character 仍隔離 worktree 法定逐位元組同:疾病層空→刺客 attr/skill +0·on-hit 只非 sim 怪·DoT/惡化只 game-loop·治癒非戰鬥);**疾病=懲罰層不強化玩家**。零寫 base·新欄 dataclass 預設遷移。`run_all` 71(新 `test_disease`);BESTIARY.md(118 怪)。~~剩餘:疾病可釀~~→ R54 | re-sim, save |
| R54 | **疾病可釀:煉金療疾藥水(R53 收尾)**(承 R53 明列「剩餘」;使用者拍板:形態=**統一淨化**·範圍=**純收尾**)。**極小·純資料+一條 brew 分支·零新存檔欄·`sim_assassin` byte-identical**(非戰鬥·刺客無療疾材料/疾病·隔離 worktree 法定)。療疾材料(`ingredients.json`):garlic/charred_skeever_hide/vampire_dust 各加 `cure_disease`(magnitude:1·二元忽略)=正典 cure-disease reagent。brew(`alchemy.py`):毒藥鏈末、有益 else 前插 `elif "cure_disease" in shared`→`synth.brew_cure_id()`·result_kind `cure`(verb 療疾藥水)·**療疾優先於回復/增益**。🔴 **零回歸**:cure_disease 只在 3 材料·**現有測試無任一「雙療疾」配對**→既有 buff/回復/毒藥交集永不含 cure_disease 不被攔截;3 材料中僅 vampire_dust 帶 harmful(damage_health/fear)而 garlic/charred 無→療疾配對永不共享有害效果·永不落毒藥分支。synth:`brew_cure_id()`=`brewcure\|1`(含 `\|`→is_synth 真)·synthesize 加 `brewcure` 分支→`{potion, effect:cure_disease, value:30}`(同 cure_disease_potion)→use_item 既有 cure_disease 分支走 `diseases.purify`·**零新存檔欄**(藥水=合成 id 由 id 還原)。UI:`main._EFFECT_CN` 加 `cure_disease:療疾`·`alchemy._TASTE_HINT` 加嚐一口提示·R32 揭露/learn 對新 kind 天然通用。`run_all` 71(`test_disease` +4) | re-sim, save |
| R55 | **成就/重玩性:首達通知 + 後期成就(複用 accessor·零存檔欄)**(評估揪出成就兩冷迴圈〔首達無反饋 + 後期內容無成就〕;使用者拍板**零風險集**)。**顯示層原已完整**(`sheet_achievements`/web `renderAchievements`/結算 `legacy_screen`)→ 新成就自動流入不動顯示。**首達通知**(`achievements.py`):新 `seed_seen(char,gd)`+`update(state,gd,seen)→events`(對齊 vampirism/skooma update·純讀零 char 變動);`main.py game_loop` while 前 seed `_ach_seen`、`_drain_mastery_choices`↔`ui.rule()` 間冒泡 `ui.message`(安全互動點非戰鬥)。**去重=session 暫態 `_ach_seen`**(seed=載入當下已達成→同 session 報一次·重載不重報·成就入 seen 即不再報→狀態回退不閃爍)·**零存檔欄**(不需 `achievements_notified`)。**4 新 cond.type**(登錄 `_IMPLEMENTED_TYPES` 否則被 `_defs` 過濾):`boons_count`(`len(char.boons)`)·`faction_rank`(`factions.rank_index>=rank`·非會員 -1 安全·與 guildmaster 互補)·`disease_count`(`len(diseases.active_ids)`)·`soul_gems_filled`(**inventory qty 加總 `kind=="soul_gem"`**·⚠ 不用 `enchanting.filled_soul_gems`〔回每堆一 id·數型別非顆數〕)。頂層 import diseases(不反向 import·無循環)。**~10 新成就**(`achievements.json`·id 無撞):誓福 8/15·血主/頭狼(coven_vampire/werewolf_pack r3)·沉默者(dark_brotherhood r4)·疾病 1/3·魂石 5·kill malyn_varen/apocrypha_seeker。🔴 **零存檔欄·不碰 combat→`sim_assassin` byte-identical**(隔離 worktree 已驗)·成就純讀零 char 變動。`run_all` 71(`test_achievements` +9·擴 `test_shipped_ids_are_legal` 覆蓋 faction_rank) | save |
| R56 | **吸血鬼能力深化:夜視 + 魅惑(戰鬥+社交)+ 偽裝暗影套裝**(詛咒線收尾;使用者拍板:夜視=潛行/偵察〔非夜戰數值·守 R50 延後〕·魅惑=戰鬥+社交·裝備=輕甲四件套裝·**套裝 bonus=偽裝入城**)。**零新存檔欄·`sim_assassin` byte-identical**(vampire-only·刺客非吸血鬼·隔離 worktree 已驗含 combat.py)。**夜視**:`moons.is_night`+`vampirism.night_vision_bonus`(讀快取 `vampire_stage`)/`night_evade`;`combat.stealth_approach_chance` 在 `night` 折入既有 `approach_bonus`(非吸血鬼/白天=0→byte-identical·無 formulas 改);`world.travel` 夜間乘 `(1−night_evade)`。**魅惑凝視**:`main._choose_combat_action` 加 is_vampire+每場一次(`charm_used` 暫態)→ action `vampire_charm`→`run_battle` 扣體力+`apply_control("fear",turns=2,source=vampire_charm)`(R44 solo 抵抗/去重);比照 rally/deathmark 戰鬥動作·不擠占 `power_id`。**社交魅惑**:`vampirism.charm_social_bonus=+0.10`(vampire-only)加進 `dialogue.persuade_chance`/`talk_down_chance`(沿用夾限)。**偽裝套裝**:`armor.json` 4 件 `nightshade`(material·light·`requires_vampire`)+`armor_sets.json` bonus`{illusion 20,**disguise:true**}`(`disguise` 鍵藏 bonus·`_apply_enchant` 安全忽略·比照 `cast_fatigue_factor`·零存檔欄);`vampirism.is_disguised`=is_vampire&active_set_bonus.disguise → 在識破圍捕(`_curse_manhunt`)/城鎮 shun/`dialogue.attitude` 疊 `and not is_disguised`(偽裝可在城正常活動·`is_shunned` 不改簽名只後果端覆寫·保留 `vampire_shunned` req);`inventory.equip_*` 加 `_vampire_locked`(requires_vampire·向後相容);招牌件 nightfang_dagger/bloodkin_amulet;取得=coven1/2 reward + vampire boss loot。**平衡 by-design**:偽裝=全輕甲套裝換社交安全·陽光詛咒仍在·**不做**夜戰數值/抗陽光新 kind。對抗審查修 1 真 bug(`cure` 後 `requires_vampire` 裝殘留→`inventory.shed_vampire_locked` 脫下);餘為 by-design(偽裝無 stage 閘/travel 用出發時對齊 encounter_chance/meets_dialogue 保留 vampire_shunned)。`run_all` 76(新 `test_vampire_abilities`;戰鬥魅惑走煙霧) | re-sim, recompute, save |
| R57 | **血族/獵群任務線深化 + 夜影套裝改高階 capstone**(評估「偽裝太早」→ 升格內容:血族/獵群是最薄公會〔各 3 任務 vs 5-7〕;使用者拍板補齊到公會水準 + 狼人對位=獸形 capstone)。**主純資料·零新存檔欄·`sim_assassin` byte-identical**(刺客非狼人/非會員·隔離 worktree 已驗含 lycanthropy.py)。**兩派各 4→6 階 3→5 任務**(`factions.json` ranks/rank_quests/rank_skill_req 同步;血族+血裔/夜爵·獵群+月牙/血爪):新任務目標**多樣化**(coven_a 清狼窟 moonlit_grove·coven_b collect vampire_dust×4·pack_a 清吸血鬼窟 molag_vault·pack_b collect wolf_pelt×5;宿仇加深)。`available_quests` 用 quest.rank 逐階篩。**頂階決鬥移末位**(coven3/pack3 rank 2→4)→ `_lair_pending_duel` 讀 `rank_quests[-1]` 自動跟上·零 UI 改。**偽裝再 gate**:夜影 4 件重排,**cuirass 移到 coven3 決鬥兩分支** → 偽裝只在擊敗 coven_patriarch 後湊齊(解原始痛點;hood/gloves/boots 在 rank 0-2)。**獸形 capstone**(`lycanthropy.py` 複用既有 pack 誓福):`_pack_capstone`=pack_heir/usurper ∈ char.boons → `claw_bonus` +3、`effective_duration` +2(讀 char.boons 旗標·不新增 boon·零存檔欄·刺客非狼人 byte-identical)。**perk 不動**(per_rank/cap 守 R52);存檔相容自癒(mid-coven 舊存檔爬新階後舊 coven3 自然生效)。`run_all` 77(改 `test_curse_rank` rank 3→5·新 `test_curse_quest_depth`) | re-sim, save |
| R58 | **Web 故事日誌:敘事 log 區持久化(分區捲動)**(評估「改善 UI/UX」→ Web 其實近乎全原生〔27 view + HUD + 互動地圖,等寬退路只剩 3 個終端死碼〕,最大真缺口=`renderScreen` 每幀全清→*文字* RPG 一行動就抹掉上回合敘事;使用者拍板 方向=敘事捲動歷史·feel=分區持久日誌)。**純前端 `index.html` 一檔·零 Python·零存檔·不碰 combat/formulas**。機制靠**既有 block kind**:`render(frame)` 分流 **view/html→`#screen`(替換,只在有 state block 時換→純敘事幀保前畫面),log→常駐 `#log` 故事日誌(append·cap 240·`.jturn` 分隔每幀·auto-scroll·`aria-live`·`prefers-reduced-motion`)**。去重雙保險:**seq gate**(重連重送 last_frame 同 seq 不重記)+**內容簽名 `lastLogSig` 僅對 backend `resend` 幀**(int 越界重送;ultracode 15-agent 對抗審查抓到「初版無條件簽名去重會靜默吞掉合法重複敘事〔連休/連 miss/空拾取/stale view 幀〕」**major bug** → 改後端標 `resend` 旗、前端只對 resend 幀內容去重,`test_int_revalidate` 守 resend 旗+blocks 同一);session 暫態不入檔(比照 active_effects/R55 `_ach_seen`)。scope v1 只 `kind:"log"` 敘事入日誌(combat_event/loot/message/ally/tick);敘事型一次性 view(event/discovery/encounter)維持 `#screen`。契約「敘事→`kind:"log"`」由 `test_blocks_protocol` 守。驗證:`node --check` JS 合法 + `run_all` 77 綠(未碰 Python)+ 伺服器啟動 serves `#log`。🔴 **鐵律**:狀態快照走 view block(替換)·敘事/旁白走 log block(入日誌)·**絕不**把敘事塞進 view(否則缺席日誌且一行動即失);改日誌呈現動 `render`/`appendJournal`/`#log` CSS | |
| R59 | **角色卡里程碑頁面 redesign:扁平 154 卡 → 系統/技能折疊手風琴 + 配對卡 + 三態**(評估「里程碑頁面太雜亂」;ultracode workflow 出 5 方案×對抗驗證→使用者選「手風琴+配對+三態」完整版)。**根因**:web 分支走 `mastery._defs`(option 攤平)→ 154 卡一次平鋪(新角 147 張未解鎖)、二選一拆兩卡(失真+翻倍)、狀態壓二分(漏「待選」)、缺技能成長線。**這頁唯讀參考**(真抉擇在 `_drain_mastery_choices`)→ 純 UI/唯讀/零存檔。**做法**:新 `console._masteries_view`(node 層聚合:系統`skills_by_spec`→技能→節點;三態 chosen✦/pending◆/future○ + single + foregone + remaining + has_pending)取代攤平;前端 `renderMasteries` 重寫手風琴(系統小標→技能折疊列〔門檻點+計數〕→展開 `.msk-node` 卡:A\|B 並列+三態著色),**折疊純前端、預設只展開 has_pending 技能** → 154 卡壓成 ~22 可掃列。終端退路保留(R27)。**對抗審查 10-agent:0 真功能 bug、1 minor cosmetic(已修)**:新卡誤用既有死 class `.mnode`(map 改 `.mmark` 後成死 CSS)致 border-left/margin 洩入→改名 `.msk-node`。`run_all` 77(`test_web` 改驗 grouped 形狀+三態)。🔴 改顯示動 `_masteries_view`+`renderMasteries`+`.msk-*` CSS;不碰 mastery 邏輯/opt_id(R34)、零存檔欄;新前端 class 用 `.msk-*` 命名空間避碰 | |
| R60 | **遊戲內指南/圖鑑(codex):深沙盒 how-to 入口**(評估「下一步」→ 使用者選遊戲內指南/圖鑑;survey 揪出深沙盒〔吸血鬼/狼人/煉金/附魔/公會/17 親王/疾病/88 里程碑〕零遊戲內說明)。**純 UI/內容向·唯讀·零存檔·免 sim·前端零改動**(複用 `panel` 原生 view)。新 `data/codex.json` 13 質化分類(section `{h}|{p,c?}|{kv}|{li,c?}`·著色限 TONE·`order` 排序·`_` 鍵不渲染·**質化抗陳舊**);`disease_temple` 帶 `"dynamic":"shrines"` → 末追加 `_shrine_index_rows`(掃 world `shrine` 地點,隨新增親王更新)。接線:`gamedata.codex=_load(...)`;`console.py` `codex_index`/`_codex_rows`/`codex_panel`(web `_emit_panel`+終端 fallback)/`_shrine_index_rows`;`main.py` `action_codex`(比照 `action_character_sheet`)+人物組 `("guide","指南/圖鑑 📖")`+elif。`run_all` 78(新 `test_codex`:schema lint+渲染煙霧+action 唯讀)。🔴 加/改分類**純改 `data/codex.json`**(R02·section h/p/kv/li·id 穩定·內容質化);需清單用動態鉤子(`entry.dynamic`+`_xxx_index_rows`)非硬寫;codex 唯讀(讀 gamedata 不碰 char/state) | |
| R61 | **種族深化:戰系主動威能(第二威能槽)+ 其餘被動天賦(混合身份)**(評估揪出種族 10 族全冷〔僅創角一次性加值+resist·零主動威能/零跑時分支·正典招牌能力全是 flavor〕;星座多已暖故留後續;使用者拍板混合)。**架構=雙不相交威能槽**:`powers.power_id()` 一字不動,新增平行 `racial_power_id()`+`RACIAL_POWERS`,冷卻**複用 `char.power_last_day`**(種族 pid 不撞星座 pid)→ **零新存檔欄**;vampire/werewolf override 只蓋星座槽、獸形關種族槽。**6 主動**(每日一次·無體力·比照星座 cadence):獸人 `orc_berserk`/紅衛 `adrenaline_rush`(自身 `berserk_buff` 增傷 ≤0.20·combat `_self_empower` 加在 `power_bonus`〔偷襲倍率**後**相加·流進 dmg 受 solo 夾〕)、木精靈 `command_beast`〔野獸〕/帝國 `imperial_voice`〔人形〕/諾德 `battle_cry`〔AoE〕(控場**全走 `magic.apply_control`** R44·turns=1·solo 機率減免+去重·類別閘 `_control_class_ok` 讀 bestiary `sentient`)、暗精靈 `ancestor_guardian`(召喚 `ancestral_ghost`·走 `spawn_creature`+`battle["allies"]`)。**4 被動**(新 `systems/race_ability.py` 純 read helper·即時讀 `races[race]["passive"]`·非對應族回中性·**零快取/零存檔/零遷移**·比照星座 resist):虎人 `night_eye`(掛 `stealth_approach_chance` 的 `if night:`)+`claw_bonus`(**僅 `_weapon_profile` hand_to_hand 側**·🔴 絕不進匕首/blade·不餵偷襲)、亞龍 `histskin`(rest HP×1.5)、高精靈 `magicka_regen`(回魔×1.5·rest+combat)、布萊頓 `spell_absorb`(0.25 吸魔·combat 元素吸收 elif·**短路於 `>0` 前→非布萊頓不擲 rng**)。**🔴 sim byte-identical**(動 combat 強制·隔離 worktree 法定:刺客=Khajiit/Shadow/匕首·4 處插入皆可證 no-op:claws gate hand_to_hand/night-eye gate night/dragonskin 短路/`_self_empower` 無 buff=0)。`run_all` 80(新 `test_race_abilities` 23 測)+ 4 主動真實 `run_battle` 煙霧。加戰系威能→`RACIAL_POWERS`+races.json `racial_power`(控場必走 apply_control);加被動→helper+races.json `passive`(絕不寫 base·利爪只徒手);**星座深化**(5 冷座)留後續 | re-sim |
| R62 | **職業深化:偏好屬性功能化(創角 +5)+ 專精跑時加速(同系技能 +20% XP)**(評估「職業的作用」→ 4 定義物只 `major_skills` 有持續作用〔起始+20·升技×1.5 等級 XP〕;`specialization` 只創角 +5〔跑時純顯示〕;`favored_attributes` **完全空殼**〔連正典 +5 都沒有·跑時從不讀·卻掛金 ★ 誤導〕;使用者拍板兩個都功能化)。**favored +5**(`formulas.FAVORED_ATTR_BONUS=5`):`creation.build_character` 屬性迴圈 `if attr in favored: val+=5`(夾 cap·正典原意·custom 適用·**創角限定·舊存檔不回溯·無遷移**)。**spec +20% XP**(`SPEC_SKILL_XP_MULT=1.2`):`progression.use_skill` well_rested 後 `if skills[sid].spec==char.specialization: xp*=1.2`(跑時 learn-by-doing·非專精 ×1·只玩家·與 major×1.5 等級 XP 獨立池不雙算)。**🔴 平衡(非 byte-identical·必跑 sim)**:刺客偏好 speed/agility +5 → 隔離 worktree diff:**solo 秒殺仍全 0%**(SOLO_SNEAK cap 與屬性無關)、**精英 oneshot 不變**(爆發不吃 speed/agility)、**群戰死亡率 ↓3-5pp**(27.2→22.6%·偏好閃避·apex 仍真實風險→守)。`run_all` 80(`test_favored_attributes_give_creation_bonus`+`test_spec_skill_trains_faster`+warrior_nord 55→60·`test_practice_cost._char` 設 `specialization=""` 隔離 spec 層)。調 `FAVORED_ATTR_BONUS`/`SPEC_SKILL_XP_MULT`→必跑 sim;測技能 XP 精確值用 `specialization=""` 隔離 | re-sim |
| R63 | **屬性深化:讓每個屬性過 200 仍有意義(漸進曲線 + 第二段效果)**(評估屬性上限擴張→揪出更根本:8 屬性只力量〔`0.75+str/160` 無上限〕真吃頂,其餘 6 個在 100~158 因硬上限或「生命只創角結算」失效·而 effective 靠誓福/裝備/詛咒疊加現就 ~190–285;使用者三拍板:機制=漸進曲線+第二段·耐力=生命跑時隨耐力長·膨脹=接受終局強度)。**兩 helper**`_soft_cap`(斜率接·0→cap)/`_soft_ceiling`(緩肩 width·硬上限替換)·**knee 以下原值=改前逐位元組同**。**5 條硬上限改漸進**(`_soft_ceiling` width):mind_resist 0.75→0.90·luck_loot 1.5→1.75·luck_fortune 0.20→0.30·rest_regen 2.5→3.2·flee 0.90→0.95;`persuade_curve` 0.90→0.95 套 dialogue(recruit/talk_down 不套);`*_CAP`→`*_KNEE`+`*_ASYMPTOTE`/`*_WIDTH`。**🔴 `hit_chance` 完全不動**(刺客 agi55 撞 0.95 舊夾·改它即破 byte-identical→敏捷改走第二段)。**第二段**(`_soft_cap` knee=0·閾下中性):智力`intelligence_spell_potency`〔R63 原:過100 漸近+25%·**R67 已大改見下列**〕·意志`willpower_cost_factor`〔過115 漸近−15%省魔·`effective_cost`乘·`max(1)`地板〕+休息回魔漸進〔**戰鬥每回合回魔維持整數硬頂5不漸進**·避分數魔力〕·敏捷`agility_evasion`〔過100 漸近+0.12·命中下夾0.05仍在〕·速度`speed_extra_action_chance`〔過100 漸近30%·`run_battle`追擊一記·**強制`sneak_attack=False`普通擊·不碰SOLO_SNEAK夾·耗體力**〕。**耐力→跑時生命**:`endurance_health`〔≤cap×2==舊`base_max_health`·>cap×1遞減無上限〕·`stats.recompute_max_resources` 改讀 live·**絕不寫`base_max_health`**〔冗餘保留欄〕·live 耦合無時機陷阱〔R06 反轉〕·副作用 intended=任何耐力加成源〔達貢/誓福/裝備/狼人獸形+40〕現也加血。**🔴 byte-identical**(隔離 worktree·非 stash):刺客屬性皆 ≤閾或在 knee 線性段·hit 未動·sim 不施法/逃跑/說服·追擊只 run_battle〔sim 走 `_round`〕→ HEAD sim 腳本對改前後逐位元組同;**動 combat/formulas 仍必跑 sim**:solo 全 0% 存活·新「R63 追擊紅線」場景〔偷襲+普通追擊〕solo 0%·精英 oneshot Δ0%·4-bandit 22.6%。`run_all` 80(test_attributes 漸進+第二段·test_m15 反轉 `test_health_couples_to_endurance`·test_progression/test_lycanthropy 耐力增量·test_solo_control applied(255)<0.5)。調任何 `*_ASYMPTOTE`/`*_WIDTH`/第二段 cap/`ENDURANCE_HEALTH_*`/`SPEED_EXTRA_ACTION_*`→必跑 sim;軟上限一律漸近非硬夾;hit/整數回魔刻意不漸進;耐力生命只在 recompute 經 `attr()` live 算·絕不寫 base;速度追擊必 `sneak_attack=False` | re-sim, recompute, save |
| R64 | **升級資源精簡:移除三選一資源 + 屬性點 4→5(資源全屬性驅動)**(承 R63 評估「調整升級資源」→ R63 後三資源皆屬性驅動·三選一變對稱 no-brainer〔健康偏差失依據·聚焦 build 與屬性雙重疊加〕;使用者拍板**取消三選一 + 補償=屬性點 +1**)。`LEVELUP_ATTRIBUTE_POINTS` **4→5**(吸收原三選一價值);移除 `LEVELUP_RESOURCE_GAIN` 常數 + `apply_level_up` 的 `resource_choice` 參數/邏輯/回傳 + `main.action_level_up` 三選一選單與結算訊息。資源純屬性驅動(endurance→生命·int→魔力·str/wil/agi/end→體力)+ 創角/裝備。**🔴 存檔相容**:`char.resource_levels` 欄位**保留**、`stats.recompute_max_resources` **續讀**(舊存檔已累積的資源加成續生效·只是不再新增)→ 零遷移、零欄位刪除(R03)。升級仍=分配 5 點 + 三圍回滿 + 里程碑二選一(非空洞)。**平衡**:取消三選一是淨資源 nerf(squishy/法系最吟)·+1 屬性點僅部分補償〔1 點=+2 資源 vs 原 +13〕=溫和 nerf + 分配彈性·by-design。**🔴 sim byte-identical**(sim 不升級→不觸 `LEVELUP_*`/三選一;隔離 worktree diff vs R63 commit 證);**不碰 combat/formulas 戰鬥常數**。`run_all` 80(test_progression 改 `test_apply_level_up_allocates_attributes` 去資源斷言·test_m14/test_m15/test_equipment 去 `resource_choice` 引數·capping 測改 `ATTRIBUTE_CAP`/`LEVELUP_ATTRIBUTE_POINTS` 常數化)。加回升級獎勵→改 `apply_level_up`+`action_level_up`;`resource_levels` 是 read-only 遺留層勿再寫 | save |
| R65 | **魔抗再分配:意志 + 大法師套裝給魔抗 + 布萊頓魔抗下修**(承 sim_builds 評估「法師對元素 boss 太脆」→ 魔抗減 fire/frost/shock〔R14〕卻只來自種族/誓福/附魔·非職業;使用者拍板:意志+套裝給魔抗〔中等〕+ 種族魔抗下修防堆免疫)。**意志=精神壁壘**:新 `formulas.willpower_magic_resist(wp)`〔`_soft_cap` knee=40·漸近 cap 25·wp100≈15·effective200+→~23〕,加進 `magic.entity_resist` 的 magic 聚合(讀 effective 意志→誓福/附魔抬意志連帶增)。**大法師套裝**:`armor_sets.json` archmage bonus 加 `resist:{magic:25}` 子鍵(**R68 下修 15**)+ `inventory.equipment_bonuses` 一行合併套裝 resist 子鍵(任何套裝可帶·比照 `cast_fatigue_factor`/`disguise` 額外鍵)。**布萊頓 magic 50→25**(唯一免疫驅動;Orsimer 25 不動=非 caster 堆疊路徑且動之會位移戰士 sim)。**🔴 sim_assassin byte-identical**(刺客 khajiit wp30→0 魔抗·非布萊頓·隔離 worktree 證);**sim_builds 法師/戰法 vs 元素 boss 顯著改善**(Altmer 法師 0→40 魔抗:意志15+套裝25)=本輪目的,非 byte-identical(by design)。免疫仍可能但需 Breton+套裝+高意志+多誓福極限堆(R63 接受深投資膨脹),非輕易達成。`run_all` 80(test_attributes +`test_willpower_magic_resist_r65`·test_equipment +`test_archmage_set_grants_magic_resist_r65`·test_daedric 補意志魔抗增量)。調 `WILLPOWER_MAGIC_RESIST_*`/套裝 resist/種族魔抗→動 entity_resist=戰鬥減傷→必跑 sim_assassin;意志魔抗 knee=40 必保(刺客 wp30 零位移) | re-sim, recompute |
| R71 | **隱遁不再無敵:純再潛行(無防禦)+ 命中地板 5%→3%**(承「讓隱遁不再無敵」評估·R69 已讓 vanish 不躲 solo;使用者選 A=隱遁全面去防禦化·知情接受群戰變脆)。**① 隱遁不再跳過敵人攻擊(全敵)**:`main.py` 敵人階段移除 `vanish_success` 的 skip(R69 原只對 solo 不躲→現對任何敵都不躲)·vanish 僅重置偷襲 opening·**無任何免疫**;選項標籤改「重獲偷襲·不閃避」。**② 命中地板 5%→3%**:`formulas.hit_chance` 末 `max(HIT_CHANCE_FLOOR 0.03, min(0.95,...))`(原內聯 0.05·新常數 FLOOR/CEIL)→ 提高 evasion 上限(滿堆閃避→敵 3% 命中)。**效果**:**boss 勝率無變化**(vanish 早 R69 對 solo 不閃·此項對 boss 無額外效果·floor 3% 杯水車薪;刺客 dagon 仍 25%·弓 34%·27王均 96/97%);**真正衝擊群戰生存**(vanish 原為圍毆逃命)→ **apex 4-bandit 死亡 22.6%→60.6%·2b2w 10.1%→40.8%**。刺客=真潛行刺客(boss 不變·群圍很危險·隱遁無逃命)。⚠ 目標雖削 boss 但 boss 早被 R69 處理→實際只重削群戰(可日後軟化:隱遁對非 solo 仍閃/給 evasion buff)。**🔴 sim_assassin 全綠**:solo 偷襲秒殺 0%·R63 追擊 0%(vanish/floor 不碰偷襲 cap)。三 sim 同步(`_round` vanish 不再 return-skip·sim_builds skip_boss=False·sim_duel skip=False)·零新存檔欄·無測試破。`run_all` 80。floor 在 hit_chance(全域)·vanish-skip 全移除(main 敵人階段)·動任一→必跑 sim_assassin(守 solo 0%)+sim_builds | re-sim |
| R72 | **無聲披掛也免「偷襲傷害」護甲折扣(對稱命中端 + 修 R70 過時註解)**(評估「輕甲里程碑無聲披掛是否真有作用」:5-agent 對抗驗證揪出此里程碑〔`light_armor_75`/`featherweight`/`armor_sneak_relief` relief 1.0〕**唯一**讀取點在 `combat.stealth_approach_chance` → 只抵**接戰端**護甲噪音〔搶開場偷襲機率·含龍鱗 −23.8pp〕,**不抵傷害端**偷襲倍率折扣 `armor_sneak_mult_factor`〔R70 後輕甲也吃·龍鱗 −21.6% 不可免〕;使用者拍板讓它**也全免傷害端**〔relief 1.0·對稱命中端·玩家側依穿戴總重〕)。**① 傷害端 relief**:`formulas.armor_sneak_mult_factor(armor_weight, relief=0.0)` 加參數 → `max(0,1−W×(1−relief)×0.012)`(relief 縮放等效重量);relief=1.0→factor 1.0(穿龍鱗滿偷襲)·relief=0.0(預設/非里程碑)**逐位元組同 R70**;兩呼叫端 `resolve_attack`(:479)/`estimate_sneak_damage`(:1034)各傳 `mastery.armor_sneak_relief(...)`（`_param` 走 `getattr(char,'mastery_choices',{})`→怪物安全回 0.0·與同塊 `sneak_mult_bonus` 一致)。**② 修 R70 過時 doc bug**:`formulas.py` 折扣註解區塊原仍寫「門檻 18·≤18 不打折·夾 [0.45,1.0]·armor_relief **不抵此折扣**」(R70 早刪 grace/floor·本輪又讓 relief 抵之)+`combat.py:479`「輕甲 W≤18 不罰」全改寫對齊。**🔴 sim_assassin BYTE-IDENTICAL**(隔離 worktree vs HEAD 證):**無 fixture 選 featherweight 且 apex 不穿甲**(`armor_worn_weight=0`)→ relief 全 0.0 代數恆等;**紅線天然守**:全免=等效無甲,而**無甲 apex 正是 sim 已驗上界**(solo 全 0%·精英 oneshot·4-bandit 60.6%)→ 龍鱗 build 至多打平、不可能超出。`run_all` 80(`test_assassin` +`test_featherweight_relieves_sneak_damage_penalty_r72`:公式層 relief 0/0.5/1 + 整合龍鱗 4 件經 `estimate_sneak_damage` 折扣全免)。零新存檔欄。🔴 此里程碑現**同時**抵接戰+傷害兩端(R07/R25「兩端各自獨立」由本輪刻意打破·僅限此玩家里程碑);傷害端 relief 走 `armor_sneak_mult_factor` 的 `relief` 參數(預設 0=back-compat)·動→必跑 sim_assassin | re-sim |
| R73 | **冰系法術補控場:凍麻 `benumb`(純減命中軟控)**(承「法師三系效果平衡」4 維 workflow+對抗 critic 評估:冰=陷阱元素〔單體無核彈·最被抗 119 中 44·27 王 0 弱點·招牌控場 chill 減速/弱化**只活武器附魔路徑、法術全無**〕→ 純冰法師吃全缺點卻無控場上界=唯一真缺陷;使用者拍板補控場且指定**「純減命中」**〔`initiative_order` 只決回合內出手序·1v1 持久戰減先攻空轉·真正有價值=減命中〕)。**新軟控 kind `benumb`(凍麻)**(R21 三步):`magic._CONTROL_KINDS`+`benumb`(→`cast()` 既有路由自動走 `apply_control`·R44·零 cast 改)+ 新 `magic.benumb_hit_penalty`(掃 active_effects 取 max·夾 0..0.6·鏡像 slow_factor)+ `combat.py` hit-chance 區一行(`is_slowed` 後·各自夾 0.05 可疊)+ `_status_verb`/`tick` 文案。**刻意不碰先攻**(不重用 slow·不改全域 slow)。**法術**(純 JSON):`frostbite` `damage`→`damage_status`+`benumb 0.20/2t`(單體);`frost_nova` 把可忽略 DoT 換 `benumb 0.12/2t`(AoE 較弱·單體 0.20>AoE 0.12);傷害不動。**平衡**:冰=控場元素(降敵命中→脆皮法師熬消耗戰)·frost-only 法師存活↑(vampire_lord 15.3→17.2 回合);**by-design 取捨** frost_nova DoT 換控場→單體 solo 殺傷略降(frost_giant 78→72%·群戰 AoE 凍麻>DoT);**軟控對 solo 照施**(無免疫·順帶補 frost 對王價值)。**🔴 sim_assassin BYTE-IDENTICAL**(隔離 worktree·碰 combat 但刺客不施法、無 benumb→getter 恆 0→`if 0:` 不擲 rng·逐位元組同)。零新存檔欄(`benumb`=active_effects 暫態·不入檔 R03)。`run_all` 80(test_m8 +凍麻回歸·test_m13 dot→benumb·test_solo_control +benumb·test_spell_schema `_STATUSES`+benumb)。🔴 控場走 `apply_control`(R44)·新 kind 三步·純減命中不碰先攻/傷害·動 combat→必跑 sim_assassin;延後 frost_blade(已有 chill rider)/atronach | re-sim |
| R74 | **冰系高級法術 + 三系終極法術(純 JSON 內容)**(承「列出傷害法術」盤點:破壞系冰系缺整條進階+專家單體線〔最高霜寒 mag16〕、三系無終極階;使用者拍板 終極=**單體大核彈**〔補法師最弱對王單體·AoE 已有〕+**不穿透**〔sim 證完全穿透 magic 元素也破不了滿血達貢→終王=offense+survival 雙牆·保 R14 抗性設計〕)。**冰系進階「冰錐術 `ice_spike`」**:單體 frost mag30/cost32 +凍麻 0.2/2 →冰系單體 33→62。**三系終極**(保三系識別:電 raw 王/火 burst+DoT/冰 低 raw+強控):`incinerate` 焚世(火 mag50/cost68+火DoT12/3)·`thunderbolt` 天雷(電 mag56/cost70+踉蹌1)·`absolute_zero` 絕對零度(冰 mag46/cost66+強凍麻0.3/3)。**取得閘純 JSON**(無 schema 等級欄):魔耗高需高 int +`spell_stock`(ice_spike→6 法師城·終極→winterhold〔法師學院〕+imperial_city)·`test_every_spell_is_reachable` 守。**平衡 re-sim**:助中高階非終王(ancient_dragon 86→95%·knight_of_order 96→100%)·**不破終王牆**(達貢 0→0% by-design)·無 trivialize;`sim_builds._MAGE_DMG` 加 4 法術(完全體反映·`_mage_act` 按 mag×(1−resist) 自選·can_cast 守魔耗)。**🔴 sim_assassin BYTE-IDENTICAL**(純資料改·刺客不施法)。零新存檔欄。`run_all` 80(schema 驗 kind/status·reachability 守 stock)。🔴 加傷害法術純改 `spells.json`+`world.json spell_stock`(R02/R29)·終極不穿透·DoT/控場不吃法術威力(R73)·動法術數值→重跑 sim_builds+sim_assassin | re-sim |
| R75 | **電系識別:感電易傷 `conduct`(疊層放大電傷)+ 電傷降火/冰中間**(承「評估電系沒附帶效果」:電正典招牌燒魔在本遊戲空轉〔`Creature` 無 magicka 欄·怪攻 bestiary `attack`/`attacks` 不耗魔〕、可用 stagger 又與冰重疊;使用者拍板 效果改**增加被擊傷害+下修傷害**〔電 raw 王→ramp 軸〕)。**新狀態 kind `conduct`(導電·非控場·不走 apply_control)**:電系法術命中疊 1 層(每層+3% 受電傷·夾 10 層=+30%)·**每次電擊刷新 3 回合·3 回合無電擊整組清零**(turns 歸零移除非逐層遞減·**滿層仍刷新**=驗證非 bug)·**只放大電系法術傷害**(`magic.cast` 內·combat/武器/他系不讀)。`magic.py` `CONDUCT_*`+`conduct_stacks`/`conduct_damage_multiplier`/`add_conduct`+cast 單體/AoE 電系分支 hook(讀現層放大→施傷→`not killed` 疊層)+tick 文案。**電傷降同階火/冰中間**(`spells.json`):閃電束40→30·連鎖閃電22→20·天雷56→48(移 R74 stagger)·電花15不動 → 電=ramp(開場持平·持續疊到+30%·獎勵持久單體=打王·懲罰一次性爆發)。**🔴 sim_assassin BYTE-IDENTICAL**(`magic.cast` only·刺客不施法)。零新存檔欄(`conduct`=active_effects 暫態 R03)。`run_all` 80(test_m8 +疊層/放大/清零·schema thunderbolt damage_status→damage)。不破終王牆(達貢0%·易傷抗性後乘)·greedy sim 不疊層→電被低估(法師 ancient_dragon 95→90%·by-design)·27王均96%。🔴 conduct 是 ramp 易傷只電系讀·每擊刷新/3回合清零/夾10層·改層數倍率→sim_builds·動法術傷害→sim_assassin。**流程教訓**:測疊層機制須給大魔力池(低MP法師連casts OOM→stuck 中途·誤判機制 bug) | re-sim |
| R76 | **法術 DoT/HoT 吃法術威力(一致性修復)**(承「評估法術威力套用到 DoT」:直擊/直接治療/護盾/號令都吃 `_power`,唯獨 DoT〔延燒/焚世/毒雲〕與 HoT〔再生 renewal/regen_aura〕不吃〔R73 flat tax〕→ 高階法師 DoT/HoT 淡化;使用者拍板**全額×power+一併含 HoT**)。`magic.py` 新 `_scaled_status(status,power)`:`status`∈`_POWER_SCALED_STATUSES=(dot,regen)`→magnitude×對應學派 power(夾≥1),其餘原樣;**4 個 `make_status_effect` 點各包一層**(`_apply_to_allies`:87 regen_aura·`damage_status`:188 延燒/焚世·`apply_status`:294 毒雲 enemy+再生 self·AoE:331)。**只 cast 路徑**→武器 ench_dot(burn/chill/jolt)/塗毒/撕裂/感染 DoT 走 combat 直接 append·**天然不受影響**。影響(×2.06 對無抗):毒雲+104%·延燒+56%·焚世+27%·HoT renewal 倍增;**不破終王牆**(火 DoT 對達貢 fire85→~0·毒 poison100 免疫→0)。**🔴 sim_assassin BYTE-IDENTICAL**(magic.cast only·刺客不施法)。零新存檔欄。`run_all` 80(test_m8 +DoT/HoT 放大·既有 dot_ticks 只驗存在不破)·greedy `_mage_act` 不主用 DoT/HoT→法師 27王均96%不變(fidelity·DoT/HoT-build 才有感)·apex 0%。🔴 cast 施加 dot/regen 吃對應學派 power(與直擊/heal 一致)·僅 `_POWER_SCALED_STATUSES`·combat 路徑 DoT 不吃·改集合/幅度→sim_builds+sim_assassin | re-sim |
| R77 | **法杖施法焦點(spell focus:持杖強化施法,非只 OOM 近戰棒)**(承「評估法杖作用」:法杖設計正確但邊緣 —— 施法者近戰武器〔魔法技命中96% vs 鐵劍51%+元素/回魔電池〕但**持杖對放法術零加成**、法師少缺魔、sim/meta 不用〔拿 iron_sword〕;使用者拍板給 spell-focus 身份)。**① 法術威力**(乘進 `_power`·與 R68 法袍套裝相加):法力法杖+10%·馬格努斯+20%(`inventory.staff_spell_power`)。**② 元素直擊加傷**(加傷害法術直擊·**吃抗性 ×mult**):馬格努斯+20→全元素·元素法杖由 `weapon_element` 推導同系(烈焰/冰霜+12·風暴+14·魔族+22·腐敗之顱+32)·`inventory.staff_element_flat`→`cast` 單體/AoE 分支。資料:`weapons.json` 只加 `spell_focus` 到 magicka_staff/staff_of_magnus(元素杖推導)。**🔴 sim_assassin BYTE-IDENTICAL**(刺客不施法/持匕首非杖→getter 回 0/{})。零新存檔欄(讀 char.weapon)。`run_all` 80(test_m8 +staff focus 回歸)。不破終王牆(flat 吃抗性·達貢滿血三系 0→0)·馬格努斯助電法古龍 90→96%·火法仍火抗牆;法師現有常駐持杖誘因。🔴 威力走 staff_spell_power(乘 _power·與套裝相加)·元素加傷走 staff_element_flat(同系限定·吃抗性·magnus 全元素)·元素杖 weapon_element 推導·動→sim_builds+sim_assassin | re-sim |
| R78 | **終極法術改任務取得 + 三系完全體大法師 + 合體王頂點(R-arcane)**(使用者四拍板:接點=毀滅 base75→Rumor 引導引路人·結構=3 試煉+合體王·完全體=build 完全體法師 PVP-type·發放=直接獲得〔並驗 R49 讀書機制確實存在但授誓福非法術〕)。三終極(incinerate/thunderbolt/absolute_zero)由帝都/冬堡商店移除 → **唯一取得=三條跨省試煉 boss 獎勵**。**引擎(quests.py 三處·向後相容)**:`_complete` 加 `reward.spells`(去重 append char.spells)·`available_quests` 加 `requires_skill`〔**base_skill** 判定·R21〕+`requires_quests`〔複數全完成·合體王用〕。**pickup**:`main.action_arcane_trials`〔比照 action_shrine:地點 `arcane_trials` 標籤配對任務 `arcane_site`·複用 _accept_and_brief〕掛法師公會子選單;**發起點分散各省**(使用者拍板):火=黑光城·冰=冬堡·電=吉迪恩·合體王=帝都〔三系畢方現〕;Rumor=純 message flavor 泛指各省法師公會〔events apply_effects 無 world_flag 效果型→不設硬前置·gate 靠 requires_skill/level〕。**4 完全體大法師(bestiary·solo·raw)**:fire/frost/shock_archmage〔HP200/200/210·d5·min17·**高抗自身元素80**·魔抗30-32〕+ fused_archmage 合體王〔HP300·d6·min18·三系皆抗70·HP<<達貢720〕·豐富元素 attacks 曲目模擬法師〔敵不能真施法〕·**on_hit 控場用 dot/weaken/stagger/benumb·絕不用 conduct〔magic.cast 專用·on_hit no-op〕**·fear≤0.30/1〔R44〕。**內容**:4 地城〔dungeons·daedric 規格 raw boss〕+4 world 節點〔**手加 pos/links·非走已陳舊的 geo_rebuild.py**〔重跑會清掉手加的近期地城〕·守 test_world 雙向/省內/degree≥2/不跨省直連〕+4 任務〔source="arcane"·requires_level18+requires_skill destruction75·每條 6 階跨 2 遠省·collect 用既有可購物 ash_yam/bone_meal/glow_dust·kill 怪 biome 對齊抵達省·reward.spells 授終極;合體王 requires_quests 三系+獎 staff_of_magnus〕。**平衡(sim_builds)**:各元素王只被同系法師擋(0%)其餘 99-100%·合體王擋全三系單系法師(0%)物理/混合 91-100%〔最難·<達貢〕。**🔴 sim_assassin BYTE-IDENTICAL**〔未碰 combat/formulas·刺客不施法·新王不在 sim_assassin 路徑·sim_builds 本就直接給法師終極→移除商店不影響〕。零新存檔欄。reachability 測試掃 reward.spells〔含 branches〕。`run_all` 81(+test_arcane_trials)。**R78b 法師公會 mg5 改制(使用者點名)**:首席法師 mg5 兩分支不再給馬格努斯之杖,改授永久誓福 `archmage_insight`「大法師通悟」(**+10% 法術威力**;馬格努斯之杖改由 trial_fused 提供不成孤兒)。**新誓福 spell_power 層**:boons.json `spell_power` 欄·`boons.apply_to_character` 聚合 `char.boon_spell_power`·`magic._power` 末乘 `(1+set+staff+boon_spell_power)`〔與 R68/R77 相加〕·Character 加 `boon_spell_power:float=0.0`(dataclass+to_dict·舊檔缺欄→0 再 ensure 重算)。🔴 sim byte-identical(刺客不施法·sim_builds fixtures boons=[]→加 0 恆等·+10% 只對完成 mg5 玩家生效)。動王數值/法術→sim_builds(+sim_assassin 確認 byte-identical) | re-sim, save |
| R114D | **長途旅行:BFS 沿路自動趕路(UI/UX 審計 包D·收尾)**(承四包·包D·跨省 5-10 跳痛點·純流程零平衡零存檔)。**BFS 選路** `world.route_to`(最短跳數·只經 is_visible 地點·已在/不可達=[]·**純圖走零 rng→決定性**;`route_hours` 估名目時)·**walkability by construction**(每跳由前跳 links 擴出→world.travel 必可走·親驗 0 非對稱連結/1280 路徑全可走)。**逐跳自走** `_travel_to` 重構委派 `_travel_hop`(**對外契約不變**=dead/None;hop 回 dead/fought/clear)→ `_travel_route` 沿路徑逐跳:死亡止·**本跳真打一場〔fought〕且未達終點→停中間交還控制**(歇息再續)·平安走到終點。**fought = interrupted(懲罰類遭遇)OR run_battle 計數增**(使用者拍板「懲罰類都停」):`interrupted`=賞金獵人/公會打手埋伏 + 衛兵盤查〔賞金·非隱形/武士放行〕**即使逃/繳/嚇退未開打也停**(不繞過因惡名而來的處罰);`battles_fought`(session·不入檔)另涵蓋隨機遭遇真開打+圍捕+事件戰鬥。隨機野獸逃走則續行·良性奇遇/武士放行不停。`guard_confrontation` +'confronted' 回值(單一呼叫端安全)。審查 MAJOR 修(原只認 3 埋伏分支→傷兵被趕入下段)+ 使用者覆核(逃/繳不該繞過處罰)。**🔴 rng 等同手動逐跳**(route 零 rng·每跳 world.travel 抽同序→byte-identical·sim 不旅行)。**入口** action_travel 加「🧭長途前往」→列已到訪+可達+非相鄰城鎮(省/跳數/估時·sort 決定性)→_travel_route。run_all 111(+test_travel_route)·sim byte-identical(未碰 combat/formulas)·煙霧綠。🔴 route_to 只經 is_visible;`_travel_to` 契約=dead/None 不可破;遇襲 fought 即停不硬拖傷兵;長途只列已到訪城鎮;**map 點擊旅行刻意延後**(web 地圖 display view·showInfo/drag 與 data-key submit 衝突);**UI/UX 四包 A/B/C/D 全完成**·剩戰鬥用藥設計題(動平衡需拍板+sim) | |
| R115 | **九神深線:神之選民試煉 + 永久神性誓福(首位 阿卡托什·R45 引擎複用)**(承 R107 前瞻「每神深線」;冷迴圈評估 workflow〔10 survey→adversarial verify→synth〕揪出九神軸最不對稱:16 戴德拉任務 vs **1** 神性任務·17 親王誓福 vs **0** 神性誓福;使用者三拍板:方向=九神深線+永久誓福·回報=**線性單一誓福**〔Agent of Mara 式·非分支〕·規模=**各配專屬 d5 boss+地城**〔戴德拉線同重量〕。**🔴 使用者鐵令**:加成**一律按原典神格設計·破 R45 紅線僅通知由使用者定奪·不擅作更換**〔記憶 [[boons-follow-canon-notify-on-redline]]〕)。**引擎 100% 現成零改**:`grant_boon` 早由 `quests._complete` 派發 `boons.grant`·祭壇 `divine` 欄+`available_quests("divine")` 已支援 requires_level →**純資料 + 1 個 main.py 選單鉤子**。**首位阿卡托什(九神之首·湮滅危機共鳴)**:誓福 `akatosh_covenant`「時龍之契」= end+10/will+6/魔抗+15/magicka+20〔**按原典:堅忍+龍血抗魔+Skyrim 魔力再生·刻意無 skill〔阿卡托什無招牌技能·不憑空捏造〕·無 sneak/武器技/strength → 守紅線 byte-identical**〕;boss `time_forsaken`「時之背棄者」〔d5 solo raw·205HP·截停己死背棄時龍律令的不死祭司·物理+frost/shock+控場 fear/stagger≤0.30-1/weaken 走 R43/R44〕;地城 `kvatch_undercroft`「凱瓦奇聖殿地窟」〔焚毀大禮拜堂地下·R28 新節點 pos[18,13] 雙向連 kvatch+gold_coast·degree2〕;任務 `akatosh_trial`〔source divine·divine akatosh·requires_level15·線性 collect魔力藥×2→reach→clear_dungeon→grant_boon〕。**main.py `action_divine_altar`** 加試煉列法(比照 action_shrine·篩 `divine`==本神·**德行閘 `can_bless` 只召無愧信徒**·有惡名/賞金者先朝聖贖罪)+ `trial:` 派發。**🔴 sim_assassin BYTE-IDENTICAL**(隔離 HEAD worktree 證·未碰 combat/formulas·誓福 fixtures boons=[]→attr 恆等·新 boss/地城不在 sim 路徑)。零新存檔欄(char.boons+ensure_boon_fields 既有)。run_all 111(test_divines +5:等級/德行閘·**全試煉內容 FK+控場節制通掃**·端到端授誓福·**紅線守衛掃全 source=divine 誓福**·**九神完成守衛**→未來加神自動把關)·BESTIARY 146。🔴 加新神純改 boons.json(**按原典神格·破紅線先問使用者**)+ bestiary(d5 solo raw·控場≤0.30/turns1)+ dungeons + world(R28 手填 pos/links·degree≥2)+ quests(source divine·`divine` 欄·requires_level·grant_boon);altar 鉤子已通用免改;**已覆蓋**:阿卡托什「時龍之契」、塔洛斯「人皇之佑」、阿爾凱「輪迴之佑」、朱利安諾斯「睿智之佑」、斯丹達爾「慈憫之佑」、澤尼薩爾「豐饒之佑」、凱娜瑞絲「天穹之佑」、瑪拉「慈愛之佑」、蒂貝拉「美之佑」〔per10/will6/speechcraft8/illusion8/魔抗10·boss 蠱惑海妖@阿比西亞海妖窟·**使用者拍板取安全域讀法〔非 Skyrim 單手武器〕→守紅線**〕;**🎉 九神全數完成 9/9**〔八省各神 d5 boss+地城+永久誓福·`test_all_nine_divines_have_a_trial` 完成守衛〕。**附帶戴德拉誓福原典稽核**(16-agent vs UESP·18 誓福 9 符/9 可收緊/**0 破紅線**·神器分支承載武器面)→ 套 7 個 A 組乾淨修正〔莫拉格巴爾 shock→frost·海爾辛 frost→poison·克拉維克斯 magic→mercantile·瘋神 int→luck·諾克圖娜爾+luck/illusion·賈格拉格 scout→mysticism·莫拉力量徑 will→smithing〕byte-identical·B(馬拉凱斯魔抗=平衡需 sim)/C(瓦瑪納 illusion=推翻 build 覆蓋)組暫緩 | save-safe |
| R116 | **幸運功能化:傷害骰下界抬升 + 瓦巴賈克回火化險(天命影響數值浮動與隨機效果)**(使用者點名評估「幸運如何影響數值浮動與隨機效果(瓦巴賈克)」→拍板兩者都做·傷害只抬下界·破紅線只提醒由使用者定奪)。**評估**:幸運只碰戰利(`luck_loot_factor`)/命運加性(`luck_fortune`·撬鎖/逃跑/事件),**不碰**傷害骰(combat.py:516 純均勻 0.85-1.15·唯秩序之劍 R48 覆寫 HI)與瓦巴賈克(`WABBAJACK_TABLE` 純加權·回火 16% R46 自平衡);剛把瘋神/諾克圖娜爾改給 luck→偏轉隨機正是 payoff。**① 傷害骰下界抬升** `formulas.luck_damage_floor`(base-40 中性·漸近 +0.10)→ combat 玩家兩處骰(`resolve_attack`:516 主骰 `_is_player` gate + `_player_counter_damage`:420)LO 0.85→0.85+floor·**🔴 上界 HI=1.15 永不動**(不抬單擊天花板/精英秒殺門檻·骰在偷襲倍率+solo 夾前 → solo 秒殺仍絕對夾)·怪物側 LO 恆 0.85。**② 瓦巴賈克回火化險** `formulas.luck_wabbajack_save`(漸近 0.40)→ 抽中回火(自傷/治敵)時以幸運機率改抽好效果(luck150 回火 15.8%→~9.5%)·**短路守衛**(`_wsave>0 and rng.chance` → luck≤40 不擲 rng)·玩家專屬法杖不碰刺客·動 R46 自平衡但受控(至多 −40% 回火·burst 仍夾)。**🔴 皆 base-40 中性**:sim_assassin BYTE-IDENTICAL(fixture luck=40·隔離 worktree 逐位元組證·**solo 秒殺 oneshot 紅線由此守=恆 0%**);sim_builds make_assassin(luck60)測 floor-lift 可靠性回報=滿血達貢勝率 27%/削弱 97%(全程勝率非 oneshot·floor-lift 只增可靠性 → 勝率單調不降·遠未 trivialize 720-HP 終王牆)。零新存檔欄·run_all 111(test_attributes +2:中性/漸近/**上界不動**)。🔴 改 `LUCK_DAMAGE_FLOOR_*`/`LUCK_WAB_SAVE_*` → sim_assassin(luck≤40 byte-identical)+sim_builds(solo 0%);下界抬升永不觸 HI(恆留變異);瓦巴賈克 save 走短路守衛(luck≤40 不擲 rng)。前瞻:幸運可再擴命中骰/元素爆發偏轉(動 combat 皆須 sim + base-40 中性) | re-sim |
| R117 | **敵方反傷:秩序騎士「灰潮的反噬」(物理反傷·懲罰爆發/秒殺)**(使用者點名評估「將秩序騎士加上反傷系統」→四拍板:只反物理·基數 raw·可致死·磁量 real threat)。**評估**:R42 反傷玩家專屬(受擊→荊棘/重甲/盾反·非遞迴·物理限定)·無怪物反傷;給秩序騎士反傷=新敵方反傷·主題完美(必然反噬·與秩序之劍零變異同構)。**機制**:新 `Creature.reflect` 欄(R43 式資料驅動·`spawn_creature`/`spawn_boss` 皆帶·**戰鬥暫態零存檔遷移**)+ combat 平行反傷區(鏡像 R42):`_is_player(attacker) and not atk_element and dmg_done>0 and defender.reflect` → `_set_hp(玩家, −round(raw/block_factor×reflect))`·**🔴 直接扣血非遞迴**(不觸發玩家自身 thorns → 無環)·**reflect==0 短路 → byte-identical**。`knight_of_order.reflect=0.25`。**🔴 平衡(sim_builds·磁量 0.15→0.25 拍板)**:只懲罰物理爆發 —— 刺客 67%/弓 78%/武僧 92% · 戰士 100%(高血吸收)· 法師 100%(施法穿透物理限定)· 無 build 被 wall;**sim_assassin BYTE-IDENTICAL**(gated reflect>0·不擲 rng·不動 boss 死亡/solo 夾 → oneshot 率不變)。零新存檔欄·run_all 111(+test_enemy_reflect_r117)。🔴 敵方反傷純改 bestiary `reflect`(物理·raw·可致死);調磁量→sim_builds(不 wall);走 `_set_hp` 非遞迴**勿改遞迴**(爆環) | re-sim |
| R118 | **變化系功能化:負重控場線 + 破盾反震(石膚反擊)**(法術學派冷→暖評估揪出變化=六大 magic 唯一 100% 純數值里程碑樹+戰鬥只「套盾」;使用者拍板深化變化·同時參考正典·只 burden/slow〔不加 paralyze〕·負重須對戰況有利·完整兩部分)。**正典**:變化技能描述明列「負重」(skills.json:10)·Burden=Ob/Morrowind Alteration〔feather 敵方對位〕·slow control kind 未被任何學派佔用→distinct 招牌軟控〔與幻術心智控區隔·麻痺留幻術避重疊〕·護膚反震=Ob/Morrowind 元素護盾傷攻擊者的現代化。**Part 1 負重(對戰況有利)**:新 `burden` 負重術(單體 apply_status slow mag0.4/4t)+ `mass_burden`(status_all slow mag0.3/3t)·走既有 apply_control(軟控·**無 solo 免疫→可靠減 boss 命中**)·佈點 imperial_city+漢默法爾三城(R29)純資料;**+ slow 命中懲罰改 magnitude 縮放** `formulas.slow_hit_penalty=min(0.30,max(0.15,sf×0.5))`(combat.py:486)·🔴 校準 ≤0.3 恆等舊 flat 0.15→既有 slow byte-identical·唯**負重 0.40→敵命中 −0.20**(讓高 magnitude 對戰況真有感)。**Part 2 破盾反震**:alteration_100 弱邊 mage_flesh(passive_armor14)→新 kind `shield_recoil`「石膚反擊」(R21 三步·鏡像 armor_stagger)·**作用中護膚盾**(magic.active_shield>0)被物理擊中→30% 震開攻方(apply_control stagger·非遞迴)·把被動 flesh 接主動反噬·opt_id 改走 ensure_mastery_choices 退 pending·100 成「威力 pure_form vs 反震 warding_recoil」真取捨。**🔴 sim_assassin BYTE-IDENTICAL**(刺客不施法/不 slow·slow 縮放 ≤0.3 恆等·shield_recoil gate active_shield+alteration mastery→刺客皆無→getter 0 短路·隔離 worktree 證);變化零 damage kind→天然不破 dagon 720 offense 牆(sim_builds)。零新存檔欄·run_all 111(test_utility_magic +2·test_mastery +1·改 test_capstone 去 mage_flesh 14)。🔴 加變化控場純改 spells.json(apply_status/status_all slow)+world spell_stock;負重命中懲罰走 slow_hit_penalty(≤0.3 恆等勿動地板);反震走 shield_recoil(gate active_shield·apply_control 非遞迴);動 combat/formulas→sim | re-sim |
| R126 | **戰鬥中喝藥:回合制用藥(近戰/潛行終獲中場續戰)**(UI/UX audit Theme 5;使用者拍板:耗一回合 + 全消耗品 + 無硬上限·sim 驗·真破再加閘)。缺口:戰鬥回合選單無用藥·唯一窗口戰前 prep_phase·`use_item` 早支援卻碰不到 → 法系有中場續戰、近戰沒有。**機制(只碰 main.py 選單·未碰 combat/formulas/magic)**:`_choose_combat_action` 加「🧪 用藥(耗一回合)」+picker→`{type:item,item_id}`·`run_battle` item 分支→`inventory.use_item`(治療/魔/體/淨疾/限時強化)·**耗一回合=不攻擊**(主要平衡閘)。**🔴 sim 驗(sim_builds 加用藥 fidelity·近戰 fixture 帶 8 治療·HP<40% 喝)**:**法系 offense 牆完整守**(火/雷 0%·arcanist 10%·聖騎士 0% vs 達貢·帶 8 治療+8 魔力藥仍 out-DPS 不了 720)·近戰 vs 達貢軟化(戰士 14→66%·雙手 46→89%·武僧 51%·刺客 49%·弓 55%)**但在既有吸血鬼附魔 closeable 68-98% 範圍內**〔[[warrior-solo-boss-already-strong]]〕非新破口·治療固定回 50 不放大自限。**sim_assassin 紅線守**(oneshot 0%·選單改不在 sim 路徑)。run_all 114·零新存檔欄。**🔴 使用者拍板**:接受近戰軟化 + 另評估增加 BOSS 強度(補償而非削藥)。🔴 用藥耗一回合(唯一閘·勿免費);法系牆靠 offense(藥給續戰非輸出);調藥→sim_builds 守法系牆 | re-sim, save-safe |
| R127 | **物理抗性正規化 + 達貢真身(秘術對決·pen-免疫物理牆)**(承 R126「評估增加 BOSS 強度」;使用者評估護甲/閃避機制〔皆拍板不改〕後,決定新增「物理抗性」正規抗性類型〔一般化原 planar_ward 提案〕·附魔+煉金同步·玩家 cap 選中等 25%)。**評估**:物理是六大傷害類型中唯一無抗性軸者(fire/frost/shock/magic/poison/disease 皆有 resist·物理只有護甲);護甲天生 pen-可穿(2H 斧破甲 0.45→護甲300 僅 62% 減傷·warrior_2H 反超秘術 57%)→ 秘術對決需 **pen-免疫**牆。**機制(combat.py 物理分支)**:`dmg *= resist_multiplier(entity_resist(defender),"physical")`(physical ∉ MAGIC_ELEMENTS → 不加 magic 抗·**抗性層非護甲層→pen 完全無法穿透**=封破甲流繞牆);玩家側夾 `formulas.PLAYER_PHYSICAL_RESIST_CAP=25`(**boss 不夾**·`_is_player` gate)。**真身 boss** mehrunes_dagon_true(bestiary·resist.physical 60〔⚠R133→70〕/magic 52·solo·HP380·**元素攻擊**〔封 shield_reflect 物理反傷 cheese〕·僅 dagon_true_arena)。**玩家來源**:附魔 resist_physical(`_resist_magnitude` 走**魔抗低階不×2**〔物理已有護甲·保守·甲6/飾9〕)+ 煉金 brew(troll_fat/bear_claw/bone_meal **FAIL-set**·determ brew-diff 證 3 新對零既有配方干擾);顯示 `_RESIST_CN`/`_RES_ELEMS`/`_RESIST_NAME`/`_effect_cn`/`magic._ELEMENT_CN` **5 map** 加 physical(**順帶修 resist_fire/disease 原顯示 raw 缺口**)。**sim_builds**:秘術 70% 最優·近戰硬拼(2H 6-13%·assn/arch 15-24%)·法系/聖騎 0% 牆·shield_reflect 27%(元素攻擊中和不 cheese)。**🔴 玩家 cap 使用者拍板中等 25%**(知情:apex 潛行滿物抗 4-bandit 死亡 60%→25%·2b2w 41%→12%=**軟化 R71 但仍存真實風險**·`test_player_cap_keeps_group_risk_real_r71` 守 ≥15% 不再惡化)。**交付**:deadlands_rift node(visible `after_event` oblivion_crisis_ended·links cheydinhal+bravil·R28)+ skingrad `arcane_trials:true_dagon` + quest trial_true_dagon(source arcane·requires_event+mysticism75+level22·reward `grant_boon` sundered_arcanist)+ boon sundered_arcanist(int/will/mysticism/**physical**+magic resist·**守 R45 紅線**無 strength/sneak/武器技)+ achievement sundered_lord。**🔴 sim_assassin BYTE-IDENTICAL**(隔離 HEAD worktree·物抗 hook `phys_r=0→×1.0`·estimate_sneak_damage 同套物抗〔既有怪×1.0〕·刺客 fixtures 無物抗)。**零新存檔欄**(全走既有 equip/potion/boon_resist·boon 走 char.boons)。run_all 115(新 test_physical_resist 10 測)·BESTIARY 149。**對抗審查 5 維 11 agent→0 blocker/major·4 confirmed(全 minor/nit·已修:recon `magic._ELEMENT_CN` 漏 physical/disease〔R01〕·estimate_sneak_damage 漏套物抗高估·arcane_trials true_dagon level gate 誤報「已了結」·comment 誇大「守 R71」→改「軟化仍存·使用者拍板」)**。🔴 加物理抗性用 `resist.physical`(pen-免疫·玩家夾 25 勿放·boss 不夾);附魔走魔抗低階(不×2);brew FAIL-set + determ brew-diff;動 cap/物抗數值→sim_builds + `test_player_cap_keeps_group_risk_real_r71` 守 R71;新 resist kind 須補齊 5 個顯示 map | re-sim, save |
| R128 | **全怪物物理抗性:按適合程度填入 41 怪**(承 R127;使用者「評估將所有怪物按適合程度填入物理抗性」→ 拍板全範圍〔含 sim 怪〕+ 玩家召喚物也給)。**評估**(workflow 5 agent 分類 149 怪):正典適合度**恰與 sim 安全性對齊**。rubric:**幽體 ethereal**(凡兵穿體·40-50)/**構造 construct**(石金·30-40)/**元素 atronach**(25-28)/**厚甲 tough_beast**(15-25)/**魔神 daedra**(8-10)/**骨系+血肉**(0)。**套用 41 隻**(33 敵+8 召喚·`resist.physical`):幽體 will_o_wisp/imperial_ghost/malyn_varen/twilight_sentinel 45-50·構造 dwarven_centurion 40/gargoyle+knight_of_order 35·元素 storm/flame_atronach 25-28·厚甲 ancient_dragon 25/wamasu 22/frost_giant 20/ogre 18·魔神 dremora_lord/mehrunes_dagon 10/dagon_diminished 5〔⚠R133:主線/削弱達貢→50〕·召喚 ancestral_ghost/summoned_healer 45 等。**🔴 sim_assassin BYTE-IDENTICAL(意外)**:assassin 對 tough solo boss 只量偷襲秒殺·本被 SOLO_SNEAK cap 夾至 0%→物抗不改輸出;群戰 bandit/wolf 全 0。**🔴 R128b 物種一致(使用者拍板必做)**:base dremora→10/frost_troll→20 對齊變體。給物抗使精英 fixture 脫「低HP飽和」→ R37 part②/R92 fortify 的「精英 oneshot 增幅≤2pp」觸發·**探針證實是飽和假象**(0-物抗高HP精英 lich gap +29pp 亦然·非物抗特有)→ 該檢查**降資訊列(非紅線)**;**真紅線=temper/fortify 不破 solo cap → part① 直接守(物抗只讓偷襲傷更小·全0%)**·群戰 R71(bandit/wolf 物抗0)不受影響。**sim_builds 意圖溫和 nerf**:tough boss 物理掉幾 pp+TTK+68-88%·純法系零位移·mehrunes_dagon(物抗10〔⚠R133→50〕)warrior 66→52%〔仍 vampiric closeable〕。**sim_party**:召喚物變耐傷害不變→召喚師 vs dagon 仍 0% 牆守·無 stalemate。**對抗審查 3 維 18 agent→0 blocker/major·1 nit(記載 base dremora/frost_troll 約束·已記)·14 refuted**(幽體強物抗/厚甲韌性/低階可帶附魔破 皆 by-design 駁回)。run_all 115(新 test_monster_physical_resist_by_category_r128)·**零新存檔欄**。🔴 加怪物抗按類別 rubric(幽體/構造/元素高·骨/肉 0);**群戰 R71 fixture(bandit/wolf/skeleton)永守 phys=0**;solo boss + 精英 dremora/frost_troll 物抗自由(R128b·精英 oneshot 增幅已降資訊列);召喚物物抗→sim_party 守 dagon 牆;動 tough/常見敵物抗→sim_builds;改 sim 守衛 ⚠→資訊列須確認非真紅線+記文件 | re-sim, content |
| R129 | **同伴深化:同伴變「類玩家角色」(屬性+技能+裝備驅動戰力)+ 裝備槽戰利品 sink + 戰術傾向**(冷系統審計揪出同伴是招牌系統卻只凍結 JSON 數值·被玩家超車;使用者多輪拍板:同伴類玩家化〔Tier 2 固定屬性+技能·經共用公式導出·技能不成長〕·指揮官軍極致=回報·附魔完全比照玩家·武器傷不設天花板·製作只當 sink·MAX_PARTY 2·**紅線=單一同伴孤立軸〔排除玩家〕vs 達貢 ~0%**)。**關鍵結構發現**:`formulas.attack_damage(w,skill,str,roll,block)` **早已玩家/同伴共用**(combat.py:541)→ 類玩家導出非新戰鬥軸/非重寫·而是**餵更豐富技能/屬性/裝備輸入進既有公式**·唯二 `_is_player` gated 導出(`_weapon_profile`/`_armor_rating`)由**生成時烘焙**重現·零戰鬥時 gate churn。**實作**:① 2 新存檔欄 `companion_gear`(移出背包只存槽=sink)/`companion_build`·技能=companions.json 唯讀模板(非存檔欄)·充能復用玩家 `enchant_charges`·`ensure_*` 遷移(退回背包)·`forget` 退裝備 ② 抽 `formulas.worn_armor_base`(玩家分支同序同 round→byte-identical)·`attack_damage` 已共用 ③ `spawn_companion(...,char=None)`(3-/5-arg 全不動·char=None→模板 byte-identical):`derived_weapon`(裝備傷+flat淬鍊·技能夾 ≤`COMPANION_WEAPON_SKILL_CAP=80`)/`derived_armor`(worn_armor_base+淬鍊·護甲不夾)·裝實體武器→pop element·烘焙護甲 resist_element ④ **附魔完全比照玩家**:生成掛 `_gear_weapon`/`offhand`/共享 `enchant_charges`·combat 兩 gate 由 `_is_player(attacker)` 改讀新 `_gear_weapon_item`(玩家 byte-identical·同伴 gear·怪 None):元素塊(627)+weapon_status 塊(785)·保留 `_is_player` 於 weapon_poison/staff/mana/mastery/offbalance(玩家 perk·平價只限 gear 附魔)·wabbajack luck save 加 `_is_player` 守·**控場走 `apply_control`→solo 抵抗自動守** ⑤ `inventory.remove_item` 加 `keep_temper`+`_worn_by_companion` 守衛(移轉不失淬鍊·賣最後份但同伴穿戴不誤清·`{}`→no-op→byte-identical)·`party.equip_gear/unequip_gear` ⑥ **戰術傾向**(功能性零數值):`party.TACTICS`(bulwark 自施 taunt/skirmisher 集火最低血/vanguard 隨機)·羈絆階解鎖永久·`run_battle` ally 階段讀 `companion_tactic`(support-first·召喚物不適用·未選=隨機=byte-identical) ⑦ UX:`action_party`「檢視/裝備同伴」→`_manage_companion`(`ui.companion_manage_panel` 導出面板+換裝對比+選傾向)。**🔴 sim_assassin BYTE-IDENTICAL**(隔離 worktree diff HEAD 空)·**單同伴孤立牆 0%**(`sim_party.fight_solo_companion`·farkas/rashid 滿裝+vampiric 全 0%·~3-7 回合即死磨不動 720)·整合煙霧 geared veteran 傷 18→50。run_all **117**(新 test_companion_gear/test_companion_build·test_companions +skills≤80 lint)。**🔴 鐵律**:同伴戰力走共用玩家公式生成時烘焙;武器技能夾 ≤80(單同伴孤立牆·動之/加高基礎傷武器→跑 `sim_party.fight_solo_companion` 守 0%);武器傷不設天花板(內容夾);附魔平價走 `_gear_weapon_item`(只限 gear 附魔·控場走 apply_control);裝備移轉走 equip_gear(keep_temper 不失淬鍊);戰術傾向純資料複用 taunt/目標(零新數值·support-first);skills 唯讀模板(無裝→byte-identical);crafting 數值不動;動 combat→sim_assassin(byte-identical)+sim_party(單同伴牆)。前瞻:thrall/pack_warrior 弧·護甲 fortify/thorns 平價·隊伍陣型 | re-sim, save |
| R130 | **同伴收尾:血僕/同窩戰士專屬支線+忠誠頂點(補齊平齊)+ fortify_skill 護甲平價**(承 R129·評估 workflow 3 agent 評三 backlog:同伴弧=do-now·護甲平價=partial·formations=**drop**〔與 R129 傾向重複+無走位模型不合〕;使用者拍板同伴收尾)。**Part A(純資料)**:R129 後 blood_thrall/pack_warrior 仍缺 personal_quest/capstone(vs 9 具名皆有)→ arc_offerable 恆 False/active_capstone 恆 None。補 companions.json(`personal_quest`/`gate:2`/`capstone`·**血僕 ally_regen·同窩戰士 ally_shield**)+ quests.json 2 條 `source:"companion"` 弧(**R52 血族↔獵群宿仇對稱**:血僕 kill werewolf×3→reach bloodmoor_crypt·同窩 kill vampire_fledgling×3→reach moonhowl_den·reward bond+gold+infamy/fame)。引擎全現成·零新存檔欄(推導 completed_quests/bond)·kill 目標 weight>0 可刷。**Part B(小邏輯)**:`party.companion_skill(...,char=None)` 加 char → 掃穿戴 `fortify_skill` 附魔加向技能·**SUM 夾 ≤COMPANION_WEAPON_SKILL_CAP=80**(守單同伴孤立牆)·3 callsite 傳 char·**char=None/無 fortify 裝→byte-identical**;fortify_attribute(1 聖物)/thorns(碰 combat·對元素 boss 無用)**drop/defer**。**🔴 紅線發現**(擴 sim 守衛揪出·[[byte-identical-vacuous-for-new-player-layer]]):R129 的 `fight_solo_companion` **未套忠誠頂點**→單同伴牆對頂點層是空話;擴守衛後揭露 **ally_regen 頂點(+6血/回)讓滿裝+arc 完成的單一同伴熬穿達貢(100%)·既有 drelas/hedge_mage(亦 regen)早 R129 後即如此**(非本輪新增·只是才量到);ally_empower/ally_shield 安全 0%。**使用者拍板:接受=極致投入回報**(arc+頂裝 regen 同伴可單刷·真實 play 玩家恆在場·同伴從不真正單挑)→ 單同伴牆精修:**核心牆〔無頂點/非 regen 頂點/fortify〕~0%·arc 完成 ally_regen 頂點刻意放行**(sim 印「接受例外」非 WALL-BREAK)。**🔴 sim_assassin BYTE-IDENTICAL**(未碰 combat/formulas·companion_skill char=None 對既有呼叫恆等·刺客不生成同伴)·核心牆 sim 全 0%。run_all **117**(test_companion_arcs +弧完成頂點·test_companion_gear +fortify 夾限)。🔴 加同伴弧純改 companions.json+quests.json(`source:"companion"`·kill weight>0·reward.bond·零存檔欄);**capstone kind 須紅線安全**(ally_regen 頂點=極致回報使用者接受·新增 regen 頂點前知會);fortify_skill 走 companion_skill(char) 且 SUM 夾 ≤80;動同伴 sim 守衛須套頂點(否則牆空話)。前瞻:護甲 fortify_attribute/thorns(低值)·formations 已 drop | re-sim, content |
| R131 | **元素抗性算法改分兩段(元素層×魔抗層):修「元素+魔抗疊過100歸零」硬牆 → 元素法師可通關**(承使用者多輪點名「法師+坦克可通關·物理不獨大」;先評估強化 boss〔AoE 反傷脆皮法師·棄〕→ 加物抗降元素抗〔物抗反傷 mage 隊·棄〕→ 使用者一語中的**「不該讓 boss 魔抗+元素抗>100」**)。**根因**:`formulas.resist_multiplier` 火/冰/雷 = `1−(元素抗+magic)/100`(**相加**)→ 疊過100歸零(達貢 fire85+magic50=135→×0·fused 105→×0·古龍 120→×0)→ 元素法師撞高抗 boss **傷害歸零硬牆**(sim solo/隊=0%);物理不吃此(獨立物抗)=「物理獨大 vs 法師被牆」真根源。**評估 3 算法**(硬夾/分兩段×/效益遞減)→ 使用者拍板**分兩段×(保留免疫)**(加入「玩家可達最高抗性」對比:效益遞減連玩家也永不完全免疫;分兩段×**保留單元素≥100 完全免疫 build 回報**·只修 boss 硬牆)。**改動(單函式)**:火/冰/雷 → `min(2.0, max(0,1−元素抗/100)×max(0,1−magic/100))`(相乘·各層夾≥0)·單層≥100→免疫·弱點負抗放大·poison/disease 不吃 magic 層·**元素抗=0→元素層1→恆等舊加法**。**效果(sim_builds solo)**:元素法師對高抗 boss 0%→有感(fused 火法 0→97%·單元素大法師 0→99%·做~13-20% 非0);**🔴 終王牆守**:solo 元素法師 vs 達貢/dagon_true=**0%**(秘術師 solo dagon_true 69% 照舊)→ 終王對 solo 法師仍牆(需配隊·mage+坦 45-68%);物理/秘術師**零位移**。取捨:元素試煉 boss(fused R78)現可元素法師 solo=刻意軟化。**玩家側對稱**:專職單元素抗(火≥100)仍免疫·中低堆疊變弱(dunmer 火75+魔23 由 0.02→0.19)。**🔴 sim_assassin BYTE-IDENTICAL**(刺客元素抗=0→分兩段=加法·物理輸出不吃元素抗)。run_all **117**(test_m8 R120 秘蝕斷言改 `_fire_dmg(5)>_fire_dmg(0)>0`·test_detailing hag_matriarch 電抗 -65→-85 穿透 magic40 層守 moor 弱性軸)。🔴 元素傷=元素層×魔抗層(相乘·永不疊過100歸零)·單層≥100 仍免疫·元素抗=0 恆等舊加法·poison/disease 不吃 magic·動 resist_multiplier→sim_builds(元素法師有感·終王 solo 仍牆)+sim_assassin(byte-identical)·加 boss 元素/魔抗自由(無「疊過100=0」陷阱)。前瞻:元素法師對元素試煉 boss 若過強→函式無旋鈕·需回硬夾/效益遞減變體 | re-sim |
| R132 | **閃避來源重構:雜技+敏捷雙主軸「單一投入遞減」+ 敏捷通用反射(接受更耐打·放寬 R71)**(承連續評估〔各 build 單人強度→潛行傷害算法→閃避來源〕使用者點名「主要來源改雜技、敏捷·單一投入遞減」+ 拍板 敏捷=通用反射〔人人適用〕·接受更耐打〔放寬 R71〕·里程碑閃避=做成有感)。**根因**:舊閃避唯一實質來源=雜技 dodge(`acro×0.0025` 線性·**自身無 cap**·靠全域 R69 sum-soft-cap 兜底)·敏捷二段(R63)只 >100 給〔近裝飾〕·9 里程碑閃避對雜技 build 邊際 <0.07〔重複〕。**改動(`formulas.py` 單檔)**:① 雜技 `dodge_evasion` 線性→`_soft_cap(acro,0,0.00295,0.26)`〔acro100 0.25→0.176·自我封頂〕② 敏捷 `agility_evasion` >100 二段→**通用反射**〔knee 100→30·per→0.0022·cap→0.15·agi65 0.06/agi100 0.096·戰士/法師也少量閃避〕③ 移除全域 `EVASION_DIMINISH_*`→遞減下放每來源自身→總量單一硬夾 **`EVASION_TOTAL_CAP=0.42`**〔里程碑線性相加「有感」·不再被全域遞減吞〕;里程碑 evasion_bonus 值/cap 0.15 **不動**(有感靠結構改)。**效果(sim_builds base→本輪)**:近戰/潛行/混合硬 boss **+10~18pp**〔warrior_1H 達貢 53→71·monk 40→58·assassin 44→61·assassin true_dagon 25→40〕·apex5 均 assassin 70.4→78.2/warrior_2H 78.2→82.6/monk 65.8→72.8;**純法系(mage/arcanist/paladin)~不動**〔agi40 不閃 + 硬 boss 輸出牆非生存牆〕;群戰死亡(R71 刻意放寬)apex 4-bandit **60.6→48.6%**·2熊2狼 40.8→30.3%。**🔴 硬紅線守**:solo 一擊秒殺**全 0%**·R63 追擊 0%(閃避與偷襲傷正交);無 build trivialize(720 達貢最高 warrior_2H 89%)。**⚠ 取捨(知情拍板)**:① 買賣不均→**拉大近戰 vs 純法系差距**(法系零受益·本就 apex 最弱);② 明確放寬 R71 群戰脆弱(−12pp)。run_all **117**(test_attributes neutral 門檻改 30·test_combat `test_evasion_diminishing_returns_r69`→`test_evasion_sources_r132` 硬夾模型·test_mastery evasion_bonus 相對斷言不變)。🔴 閃避二主軸=雜技+敏捷各自單一投入遞減(勿回線性)·敏捷通用反射(knee 30·刻意惠戰士/法師)·里程碑線性相加·總量硬夾 `EVASION_TOTAL_CAP=0.42`(唯一總量旋鈕)·solo 秒殺與閃避正交(恆 0%)·**動任一閃避常數→跑 sim_assassin(群戰死亡)+sim_builds(硬 boss 生存·近戰vs法系差距)**。前瞻:近戰vs法系差距過大→補純法系一個與敏捷無關的生存工具 | re-sim |
| R133 | **達貢三形物理抗性階梯 70/50/30(「物理不獨大」終王)**(承連續評估:各 build 強度→潛行→閃避→現實派 fixture→元素法師弱勢診斷→主線終王物理獨大;使用者拍板)。**診斷**:純元素法師弱=**怪物抗性太高**(隔離實驗:歸零抗性→每格 0→76-100%·+5×HP 修不動硬牆;**非原始傷害**〔零抗每記 115-125·6 記殺 720〕·**非生存**);物理側相反=melee 碾壓主線終王(w2H 94/w1H 87 vs arcanist 18/mages 0)=「物理獨大」。**機制**:物理抗性 pen-免疫(R127)只砍物理輸出·**完全不碰魔法傷害**→加物抗=削 melee、arcanist/元素法師恆不變=精準壓物理獨大。50=削melee曲線拍板(40 仍領先·55 反轉成秘術牆)。**改動**:bestiary `mehrunes_dagon`(720)phys 10→50·`mehrunes_dagon_diminished`(380)phys 5→30〔削弱形每條抗性皆 < 化身〕·真身 `mehrunes_dagon_true` phys 60→70(本體最堅·resist_multiplier 0.40→0.30)。**效果(realistic→R133·主線720)**:melee −44~52pp(w1H 87→35·w2H 94→46·monk 58→12·shdR 88→44);**arcanist 18→18·元素法師 0→0(fire85 牆)·battlemage 71→49·paladin 14→0**;apex5 melee −6~10、魔法系 ±0→頂 ~46-49% 無碾壓;**削弱形 380HP 仍 ~100%(守「削弱=易」)**;三達貢差異化(70>50>30)。**🔴 紅線**:偷襲秒殺全 0%(cap 主導·物抗只讓偷襲傷更小);刻意破 R128「魔神 8-10」rubric=位面之君特例(比照真身·flavor 補);**不幫元素法師(仍 fire85 牆·使用者「不修」)**;sim 排除吸血鬼附魔→實戰 melee 更高(逃生門在)。run_all 117(test_physical_resist:主線=50·削弱=30·真身=70〔0.40→0.30〕)·BESTIARY 149。🔴 加終王物抗純改 bestiary resist.physical·削幅由值定(70=秘術牆)·動→sim_builds(melee 不歸零·魔法不受影響)+sim_assassin(秒殺恆 0%)·三達貢物抗 70/50/30 差異化維持·pen-免疫勿混護甲 | re-sim, content |
| R134 | **BOSS 召喚爪牙:18 王名冊 + 殺王潰散 + 爪牙三重排除(戰利品/擒魂/擊殺計數)**(承特色化評估鏈〔solo-collapse 必要性→召喚 probe→名冊逐對驗證→單人/隊伍框架對照〕;使用者四定案:名冊 14 組+終王 dremora 2/6 循環召喚〔知情接受〕+三重排除+自系牆納入)。**引擎**:bestiary 可選 `summon` 譜 `{id,wave,cap,cooldown}`;**`combat.try_boss_summon`=唯一決策點**(main+sim_builds 共用):冷卻到+無存活爪牙+未達終身 cap→召 wave 隻(`summoned=True` 暫態)入 enemies 本回合參戰·**耗 boss 該回合攻擊**;無譜在 rng 前回 None(byte-identical 基石);`choose_enemy_attack`=taunt fallback(玩家召喚模板嘲諷招無 damage 鍵);**殺王=爪牙潰散** `scatter_orphan_summons`(敵階段前+回合末;依賴「召主皆 solo·一場一召主」不變式);硬控自然停召。**排除四處**:勝利迴圈 `summoned` continue(loot/擒魂/record_kill→kill 任務/成就全斷)+harvest 過濾 token+**reanimate 屍體排除**(審查揪出第四側門:復生爪牙屍體回收 token→補一行);XP 保留(有界≤cap)。**名冊 18 王**:中高階 14(骷髏/僕從/draugr/新血 cap4/幽影/巨蛛 1/3/召喚魔人 1/2/沼蛭/瘟疫鼠/冰元素)+**自系元素牆 3**(fire/frost/shock_archmage+同系 atronach→自系法師 0/2/0%=牆完成·其他不動)+終王 dagon+dremora 2/6;新模板 `summoned_flame_atronach`。**質量鐵則**:輕爪牙(28-48HP)=純 TTK 稅(勝率≤7pp·melee 3→8-10 回合·AoE 法師幾乎免稅=清場峰值首次在 boss 戰內變現);**爪牙抗性軸=法師第二配對考驗**(抗50→−15~22pp·**免疫100 鎖死禁用**除非自系牆);d4 早期王不配(逃跑保護·schema 守)。**🔴 平衡**:名冊王勝率維持;**終王 720 單人全 build 0-8%**(使用者兩見數據拍板=終王重定義為隊伍/吸血附魔向·隊伍+2素體同伴 10-34%);**sim_assassin BYTE-IDENTICAL**(隔離 worktree)+非召喚王 sim_builds byte-identical(審查 800 場實測)。對抗審查 3 維→0 blocker/major(修 reanimate 側門+test nit;記載:召主死於敵階段爪牙仍動一輪·爪牙 infect 保留=主題壓力)。run_all **118**(+test_boss_summon)·BESTIARY 150。🔴 加召喚王純改 bestiary summon 譜(召主 solo+d5↑·爪牙非 solo·wave≤cap≤6·免疫爪牙禁用·d4 不配);召喚一律走 try_boss_summon;**加新 per-kill 資源須同步排除 summoned**(四處全守);破「一場一召主」先加 ownership;配爪牙王必跑 sim_builds·動 combat/main→sim_assassin byte-identical | re-sim, save-safe, content |
| R135 | **召喚爪牙擴充:增援變體 5 王 + 瘋神隨機爪牙表 + BESTIARY【召喚】tag(+重大裁決:配爪牙王=帶隊向)**(使用者拍板三項合一)。**①增援**:`summon` 譜加可選 **`msg` 模板**(`{name}`/`{names}`·預設=R134 原句逐字→既有 18 譜 byte-identical·審查程式化證明);5 王:dire_alpha/blood_moon_beast+wolf 2/6·oathbreaker_warlord/avarice_tyrant+bandit 2/6·malacath_outcast_chief+bandit 2/4(probe 全過≤2pp)。**②瘋神隨機**:譜支援 **`ids` 隨機表**(每隻獨立 rng.choice·僅 ids 分支耗 rng→固定譜 byte-identical);madness_avatar+[skeleton,wolf,giant_rat,imperial_ghost] 2/6;**反樂透牆 lint**:隨機表爪牙對 fire/frost/shock/magic 禁 ≥100 免疫(固定 id 可=自系牆;隨機=樂透鎖死禁)。**③gen_bestiary**:「召喚:xx×wave(總cap)」tag+修 BEHAVIOR「不召喚」陳舊句。**🔴 重大發現+裁決**:名冊王多為試煉/進度王·質量閘原只驗完全體;**適齡單人(強中期戰士 80技鋼板)實測斷崖**(grave 97→35·coven 99→8·arch_mystic 93→2·oathbreaker 100→13;根因=中期清波 4-6 回合×三波磨死);**帶 2 素體同伴→97-100% 全回滿**→**使用者拍板 a:配爪牙王=帶隊向內容**(單人適齡撞牆 by-design·溯及 R134 全名冊·與終王方向一致)。審查 0 blocker/major(修 empty-ids+msg format-safety lint)。run_all 118(test_boss_summon 12 測)·sim_assassin BYTE-IDENTICAL(隔離 worktree)·6 新王完全體 98-100%·BESTIARY 150。🔴 增援=同譜+msg;隨機表用 ids(反樂透牆·不得空);**新召喚王雙層驗:完全體+適齡中期**(帶隊向=裁決預設);要回頭做單人試煉可行=降波(cap=wave)/血量閘並先問 | re-sim, content |
| R136 | **弓手里程碑重造:75 箭雨(AoE)+ 100 雙側救活(連珠箭/獵手之眼)**(承弓手全手段評估〔P0-P4 量化:工具全冷非力量缺·全開 true_dagon 88% 全場最強〕;使用者兩度點名:75 疾矢死填充→箭雨 60% AoE·100 兩側救活〔穿甲 pen 被 R127 廢/穩準狠平淡〕)。**①箭雨 volley(75·bow_technique)**:主動齊射全體·每箭 `VOLLEY_DAMAGE_FACTOR=0.6`·倍耗體·**🔴 永不吃偷襲倍率**(呼叫端硬寫 sneak=False);選單 ≥2 存活敵才顯;引擎=`resolve_attack` 加 **`damage_factor=1.0` 參數**(唯 volley 傳 0.6·預設全呼叫 byte-identical);75=瞄準(單體)vs 箭雨(AoE)→ **弓手成唯一有 AoE 的物理職業**;⚠ 附魔 flat 加傷不縮(審查 F2 記載)。**②連珠箭 rapid_shot(100·取代 penetrator)**:weapon_mod `extra_shot 0.2`——持弓**命中後**(F1 補 hit-gate)20% 追加一箭=普通擊 sneak=False(鏡像 R63 車道)·attack/aimed/crippling 後觸發·不遞迴。**③獵手之眼 hunters_eye(100·取代 steady_aim)**:weapon_mod `exploit 0.2`——目標帶控場(`_EXPLOIT_KINDS`=衰/踉/緩/凍麻/懼/麻痺·**不含 dot**)→ 弓傷+20% 走 power_bonus 車道(偷襲倍率後·solo 夾前)=「先壓制再處決」vs 連珠無條件手速。**🔴 驗證**:sim_assassin BYTE-IDENTICAL(worktree×2·assassin xs=0 不擲 rng)·sim_builds 正典 ~零位移(95.7→95.8·主價值在玩法層)·舊 opt_id 走 ensure_mastery_choices 退 pending·run_all 118(test_hybrids +3)。審查 0 blocker/major(修 F1 hit-gate+F2 註解);**F3 記載:chill 附魔弓可讓獵手之眼近常駐**(+20% 有界·收緊需先問)。🔴 volley/追加箭永普通擊;damage_factor 預設 1.0 勿動;exploit 不含 dot;weapon_mod 新參數加合併鍵;動 0.6/0.2/0.2→sim_assassin+sim_builds;前瞻:弓神器真空·sim 建模主動戰技 | re-sim |
| R161 | **玩家文字用語一致性大掃除:同概念異詞正規化(承使用者「全面檢查 one-off wording bug·我來決定如何改」)**(8 域審計 workflow〔106 候選→驗證〕揪出跨檔用語漂移;使用者逐項拍板正名)。**🔴 純顯示字串+skills.json name·零存檔·combat/formulas 用英文 id → sim_assassin byte-identical(48.6%/30.3% 同 R132 基線)**。**六正名全替換**(tesrpg/+tests/·68 檔):魔力→**法力**(159·留縮寫 回魔·勿碰 魔法/魔法抗性)、復原→**恢復**(30·restoration canonical 更名·skills.json name+_SCHOOL_CN)、秘術→**神秘**(55·排除法術專有名 秘術飛彈/秘力星爆)、耗弱→**衰弱**(13·短 tag 仍「弱」)、名聲→**聲望**(38)、神祕→神秘(1)。**破壞→毀滅 外科手術**(僅 5 學派語境:codex 六學派表/oghma 禁咒/trials·main 試煉閘訊息/events 三系試煉旁白·多處一句混用 破壞+毀滅)·**保留 13 處臥底系統『破壞耳目』sabotage 通用動詞**。先修 蒂貝拉誓福 個性→魅力(personality 全遊戲叫魅力)。**3 判斷拍板**:回魔保留·法術專有名保留·破壞外科手術。殘留掃描全 0(除意圖保留);run_all 131·sim byte-identical。**記憶 [[player-facing-canonical-terms]]**(未來新 desc/message 用正名·勿重引舊同義詞)。**🔴 dev 文件(CLAUDE/handoff/*.md)刻意保留舊詞(歷史 R## 紀錄非玩家面);新玩家面文字一律用正名 | save-safe |
| R162 | **戰鬥動作選單瘦身:內容寬 + 無表頭收一片 + 風味備註 hover / 決策數字 chip**(承「行動完看不到敵我資訊」UI 評估鏈 →「動作選單太長」子問題;**初版純 CSS 密排〔縮字/縮 padding〕遭使用者否決還原**,改抓真因——使用者點名「按鈕太寬·你應該有更好的方法」)。**真因**:`button.opt{flex:1 1 230px}` 的 `flex-grow:1` 令按鈕撐滿整排剩餘空間 → 單顆桶(攻擊/架式/脫戰)被拉成橫貫大條;此即檔內 cardgrid 註解早記的「**flex grow 之疾**」(種族/星座選卡用 `flex:none` 解過的同一坑)。**方法(使用者拍板「拿掉表頭收一片」)**:戰鬥選單改**內容寬 `flex:0 0 auto`+`max-width:100%`**(按鈕只佔標籤寬·並排+自動換行)+ **無表頭單一 `.btns.combat-flow` 流動**(5 桶合一)。**不縮字/不縮 padding/不藏資訊**(異於被否決的密排);寬度不再浪費、列數變少(~500-660px → ~3 列)。**🔴 唯一改 `index.html`·零 Python·零 combat/formulas/存檔 → sim byte-identical·前端不由 run_all 執行(node --check + DOM-mock 契約斷言驗)**。**偵測=grouped spec 含 `flee`/`leave` key**(戰鬥唯一標記;地圖/hub/開局 grouped 不含→照原分組表頭·beast-form flat `ui.menu` 不受影響);`.btns.combat-flow` box **每幀重建 → 無 class 洩漏**(異於初版把 class 掛 promptEl 需每幀 reset)。**守約**:連續編號不變(repeat 仍 #1·R113)·回同 key(dispatch 不變·R156)·font-size 仍 ×--fs-scale·**刻意桌面優先不管手機**(使用者指定)。run_all 131(未碰 Python)·node --check·DOM-mock 驗 combat 單片無表頭/repeat #1/key 序 + 非戰鬥 grouped 表頭保留·web boot HTTP 200 serves 新規則。🔴 戰鬥選單瘦身走 `.btns.combat-flow`(`flex:0 0 auto`·偵測 flee/leave key·box 每幀重建免 reset);**不縮字不藏資訊**;桌面優先(手機另評)。**續作(同 R162)**:使用者質疑「不管手機為何不能 hover」→ 桌面確可(初版反對理由只是手機無 hover)。每項標籤經 main.py `_split_combat_label`(純字串拆·全/半形括號皆可·零 regex/零新 import)拆成 **動作詞 + 決策數字段(含數字/%/∞ → 併成一顆內聯 chip·`.btns.combat-flow .opt-chips` inline-flex)+ 完整原標籤 note**(前端掛 hover `title` + `aria-label`·note=完整原標籤故不失真);`ui.grouped_menu` option 由 `(key,label)` 擴為可帶 `(key,label,meta{chips,note})`(2-tuple 不帶額外鍵 → byte-identical·地圖/hub/開局 grouped 不受影響),終端 fallback 仍餵完整標籤;test_web D2 lambda unpack 由 `k,_ in o` 改 `t[0] for t in o`(唯一測試適配)。守恆測試 `test_combat_label_split_chip_hover_r162`:每個含數字段(去空白)必進 chip(否則決策數字漏到只剩 hover)+ `test_grouped_menu_carries_chips_and_note_r162` 驗真 grouped_menu 帶 chips/note、2-tuple byte-identical。**🔴 桌面優先(使用者指定不管手機·hover 在觸控失效但決策數字已在 chip 上)**;未碰 combat/formulas(view-model + 前端)→ sim byte-identical。🔴 戰鬥標籤瘦身走 `_split_combat_label`(數字段→chip·風味→note/hover);grouped_menu option 3-tuple meta 僅戰鬥用(勿破 2-tuple byte-identical)。**對抗審查(5 維·subagent 遇 session 限全中止 → 主迴圈親審 + 對全 ~30 標籤清單實跑 `_split_combat_label`)**:0 correctness bug;契約全過 —— 唯 combat grouped 帶 flee/leave→無誤判(beast-form/地城的 leave/flee 走 flat `ui.menu` 不進 grouped-branch flow;hub/map/origin 無此 key);2-tuple byte-identical、repeat #1、終端 fallback 原樣完整標籤、未碰 combat/formulas。補強:守恆測試由 3 抽樣 → 全 ~30 標籤清單(未來標籤漏數字到 hover-only 會被攔)。by-design 知情代價:非數字提示 重選目標/不閃避/耗一回合 落 hover-only(桌面可及·觸控失之)。**續 II(全域內容寬)**:使用者點名「其他選單也太長」→ 同根因(flex-grow:1 撐滿整排=全域 `button.opt`·戰鬥只 scoped 到 combat-flow)。使用者拍板全域:`button.opt` 基礎規則 flex `1 1 230px`→`0 0 auto`+`max-width:100%`(內容寬·短標籤=小按鈕並排換行·全 .btns 動作選單〔hub/地圖/地城/城區子選單/確認〕一致·各自分組表頭保留);**views(商店/背包/任務=自繪 renderShop/Inventory/Quests)、`.cardgrid`(種族/職業/星座/開局)、`button.back`(flex-basis:100% 全寬)皆不受影響**;長標籤選單(練習)近全寬故幾乎不變·真正變緊湊=短標籤動作選單;combat-flow 的 button.opt 規則變冗餘(留作 isolation 防禦)。純 CSS 一行·零 JS/Python → run_all/sim 天然不受影響·node --check + web boot HTTP 200 驗。**續 III(行動後聚焦資訊區)**:回到最初「行動完看不到敵我」根因 —— render() 的 R113 捲動每幀 `promptEl.scrollIntoView({block:center})` 置中動作選單(∵故事日誌成長使頁恆高於視窗 → 此支恆觸發)→ 資訊區 `#screen` 被頂出畫面。使用者拍板反轉:`!samePrompt` 改單純 `window.scrollTo(0,0)`(移除置中支)→ 行動後捲回頁頂,**HUD + 資訊區(敵我 HP/狀態)+ 短選單 + 本回合同屏**(選單已短故三者可及·故事日誌歷史落摺線下回看再捲)。純 JS 一處·零後端/存檔·node --check + boot HTTP 200 驗。**大幅緩解原始「行動完看不到敵我」**(每新幀資訊區回頂即見)。**續 IV(本回合搬進資訊區·取代 R112 雙區/R156 D1)**:使用者質疑「為何不把本回合做進資訊區」→ R58「絕不把敘事塞進 view」的理由是『view 每幀清空→敘事消失且缺席日誌』,但 **ephemeral 本回合流水帳(R156)本就退役即丟、從不入日誌** → 該理由對它不成立(短暫 log 配短暫 view 反而吻合)。新架構:新增 `#turnlog`(在 `#screen` 與 `#prompt` 之間),`render()` 依 `b.ephemeral` 分流 —— **ephemeral → `renderTurnlog`(每幀整組替換·生命週期同 #screen)**、**持久敘事 → `appendJournal` 直接歸檔 `#log` 故事日誌**(移除 jlatest 退役邏輯·`#log` 由雙區變單純歷史)。清除規則:`renderScreen` 遇非戰鬥 view(name≠combat)→`clearTurnlog`(離戰/回城清殘留)·戰鬥 view 無 ephemeral(選目標重繪)→保留·有 ephemeral→替換;resend 內容去重移到 narrative 流(turnlog 替換天然冪等)。結果:**資訊區=戰況卡+本回合(現況+這回合發生什麼黏一起)→ 選單 → 故事日誌歷史在底**。**🔴 純前端·零後端(block 協定 kind:log+ephemeral 旗不變 → test_blocks_protocol/test_int_revalidate/test_combat_readability 天然守)·零 Python/存檔·未碰 combat/formulas → run_all 131 綠/sim 天然不受影響**;node --check + DOM-mock 6 情境(替換/歸檔/清除/保留/resend 去重)+ boot HTTP 200 驗。🔴 敘事分流鐵律更新(取代 R112/R156 D1):**ephemeral 戰鬥流水帳 → #turnlog(資訊區·替換)·持久敘事 → #log(歷史·append)·絕不再用 jlatest 雙區**。**續 V(進遊戲隱標題列·⚙ 移右上角小按鈕)**:`#masthead`(流亡者標題+花紋+連線)在 `body.playing`(renderHud 依 HUD 是否顯示切換 class)時 `display:none`(主選單仍顯示標題);⚙ `#gear` 移出 `#status`→`#app` 直屬子、`position:fixed` 右上角小按鈕(`right:max(10px,calc((100vw-880px)/2+8px))` 貼齊內容右緣·`z-index:8` 高於 HUD〔7〕低於設定覆蓋層〔.set-overlay 50〕→ 開設定時藏其下,且 gear 仍在 #app 內故 `#app.inert` 照樣鎖它);HUD `padding-right` 加到 42px 留位不遮時間/chip;斷線改由 HUD `.off` 紅框示(連線 dot/conn 仍留 masthead·JS 靠 id 參照不動)。純前端·零後端/存檔·未碰 combat/formulas → run_all 131/sim 天然不受影響·node --check + DOM-mock(playing 切換)+ boot HTTP 200 驗。**前瞻**:僅剩「捲動途中」常駐可見(HUD 補敵血 / `#screen` 釘頂)為獨立未做子問題 | |
| R163 | **Playwright 真瀏覽器桌面 UI 回歸硬閘門**:`tests/browser_ui.py` 用正式 `_make_handler` 供應 production `index.html`,以決定性 SSE frame 驅動 Chromium,不啟整局/不碰真存檔。三情境守①戰鬥 HUD/資訊→本回合→操作→故事日誌順序、內容寬按鈕幾何/零水平溢出、note+chip、真 `/input` 送出、下一幀捲頂;②設定 dialog/inert/焦點困住、字級/主題/減動態 localStorage 持久化;③地圖服務篩選高亮/所在地豁免/最近 X/真 route submit。`check.sh` 在 run_all 後**無條件執行**,缺 Playwright/Chromium/系統庫即紅燈並印安裝指令,不可靜默 skip。Playwright 僅 `.[dev]`,runtime 仍零新增依賴;uv.lock 鎖定。前端 UI 變更除單元/字串契約外必補/跑此套。 | test-only |
| R164 | **桌面戰鬥動作選單可用性 + 首屏密度**(承 R162/R163 實機評估)。①頁尾依真實選項數動態顯示；10+ 項完整編號 + Enter 確認，空白 Enter 再次；submit/新 frame 清數字緩衝，原生可互動元素保留自己的 Enter。② `_combat_option` 讓武器、定性效果、代價/回合直接進 chip；`_split_combat_label` 對未精簡新動作完整顯示括號效果，CLI fallback 還原完整標籤；**取代 R162「數字段 chip/定性 hover-only」舊規則**；無表頭 flow 不加視覺分隔，`data-group` 與 ARIA label 保留類別語意。③ `min-width:761px` 戰況改敵我雙欄 grid，窄螢幕維持直向；1280×800 壓力案例戰況 394→213px、14 動作末端 738px、提示 777px，未縮字/刪資訊。Chromium 回歸守 14 動作競態、1280×800 首屏、1024×768、800px+特大字級、零水平溢出。**🔴 未碰 combat/formulas/存檔；效果可見、CLI 完整、焦點 Enter 與桌面幾何皆為回歸紅線。** | save-safe |
| R166 | **Web 互動可靠性與精準無障礙**:`pending/connected/synced` 單一狀態控制 prompt `aria-busy`、screen/prompt `inert`、spinner、快捷鍵/submit；HTTP 200 只代表接受，須等新 SSE `seq+prompt_id` 才解鎖，409 重建 EventSource 並以 sync floor 擋舊 `last_frame`，網路/8 秒逾時可重試，response-loss 後新幀會收斂。連線文字移成獨立 `role=status`，遊戲異常時 fixed 留在 viewport 且 HUD 下移；SSE parse/render 失敗可見並實際重連。焦點依 action key 回同控制(含 CTA/back/data-key/map route)，消失回 `.ptitle/#prompt` 且 `preventScroll`；保留舊 screen 時撤失效 data-key 監聽、地圖 direct control 依目前 spec disabled + submit 二次 gate。OS/手動減動態皆關 animation/transition/smooth scroll，spinner 留靜態形。**🔴 HTTP 200 不解鎖；pending 只由更新 seq+pid 結束；pending/syncFloor 存在時任何舊幀不得解鎖；screen submit key 必屬目前 spec；異常狀態必有 viewport 內文字，不能只靠顏色。**純前端+測試，後端原子 submit/存檔/combat 不變。 | save-safe |
| R167 | **本回合/故事日誌可讀性:戰果鏡入本回合(方向2)+ 戰鬥動作分類收斂(方向1)**(承「本回合與故事日誌資訊令玩家困擾」評估;workflow 現況測繪〔前端版面/訊息分類/逐幀重演/沿革紅線〕完成後,診斷/方案 subagent 撞 session 限額→主迴圈親自診斷+方案評估;使用者拍板 **方向1+2**·方向2 取「**鏡入本回合(完整回合視野)**」)。**根因**:ephemeral/敘事二分照「技術生命週期(會不會持久)」切、非「玩家心智(這回合發生什麼 vs 持續的故事)」→ 最重要戰果(擊殺/戰利品/升級/任務·持久→`#log` 在選單**下方**·`scrollTo(0,0)` 後不在視窗內)被藏;單一行動被拆兩區(傷害在 `#turnlog`·技能升/擊殺在 `#log`)。**方向2(純前端·`index.html render()`)**:有戰鬥流水帳(ephem)的幀 → `renderTurnlog(ephem.concat(narrative))` 讓該幀敘事(技能升/擊殺/戰利品)**鏡入本回合**、選單上方一眼可見;`#log` 仍 `appendJournal(narrative)` 完整歸檔(**唯一真相來源**)。**無 ephem(純敘事/導航幀)→ 不 concat**(`if(ephem.length)` gate)→ 休息/旅行敘事不污染本回合、非戰鬥幀照 `clearTurnlog`;順帶消 R162 孤兒致命一擊(併成完整戰報)。**方向1(`main.py` 路由 + `console.py` 刪死碼)**:6 個戰鬥動作訊息 `ui.message`→`_cmsg`(ephemeral·不污染永久日誌·對齊 R156「戰鬥逐回合訊息一律走 `_cmsg`」)—— 用藥/調息/變回人形/盾牆落下/戰地搶救/爪牙潰散(回合末·對齊玩家階段);刪零呼叫死碼 `ally_event`(已由 `combat_event` 取代)/`active_effects_line`。**🔴 未碰 combat/formulas/magic 數學·零存檔欄 → sim byte-identical**(方向2 純前端·方向1 只換訊息去向);block 協定(`kind:log`+`ephemeral` 旗)不變 → test_blocks_protocol/test_int_revalidate/test_combat_readability 天然守;**`loot_report` 仍非 ephemeral**(test 續綠·戰果靠前端 concat 鏡入·非後端 dual-emit)。**知情取捨(使用者拍板)**:即時後果同現本回合+故事日誌=輕微重複(兩區空間分離·日誌在摺線下幾乎不同屏),換完整回合視野。run_all 131·browser_ui **16**(新 `test_turnlog_narrative_mirror_r167` 守戰鬥幀鏡入 + 純敘事幀不污染)。🔴 本回合 concat 走 `if(ephem.length)`(戰鬥語境才鏡入·勿無條件 concat 免污染);戰鬥逐回合訊息一律 `_cmsg`(勿用預設 `ui.message`=進永久日誌);`#log` 為唯一真相來源(`appendJournal` 不變)。**續作(同 R167·三保留項使用者拍板全做)**:① **命名時差** →「⚔ 本回合」改「**⚔ 戰報**」(純前端一字·避時序宣稱·戰鬥/勝利兩情境皆貼);② **戰鬥中技能升灌 `#log`(#6)** → `show_events(events, gamedata, combat=False)` 加旗,戰鬥呼叫端(`combat_event` tail/施法/隱遁)傳 `combat=True` 使 `skill_up` 走 ephemeral(只進本回合、不灌永久日誌);**`★可升級`/`✦里程碑待選` 仍非 ephemeral**(要玩家後續行動)、**非戰鬥 skill_up(煉金/撬鎖等)仍持久**(預設 False·逐位元組不變);③ **地城互動先於地圖更新** → `action_dungeon` 迴圈進格**先重畫小地圖(@ 已到新格)再結算**(撬鎖/機關確認選單顯示新格);戰鬥格例外(combat 接管 `#screen`)改**戰後重畫**;**一格至多一次 `dungeon_grid`**(結算彈提示則被該幀消耗·無提示則與探索選單同幀)→ 不疊 render。**🔴 未碰 combat/formulas/magic → sim byte-identical**;`test_show_events_combat_ephemeral_r167`(戰鬥 skill_up ephemeral·level/mastery/非戰鬥仍持久)·browser_ui 16(mirror 測 +「戰報」標題)·`test_dungeon` 既有 crawl 守地城迴圈。🔴 戰鬥回合內技能升一律 `show_events(...,combat=True)`(勿讓逐擊升級灌永久日誌);地城互動格重畫走「非戰鬥格 top-draw + 戰鬥格戰後重畫」單次不疊。 | save-safe |
| R168 | **任務日誌分桶重造:3→6 桶(source 顯式查表)+ 完成數措辭**(承 UI/UX 人性化審計:source 17 種 vs 3 桶,主線/親王/九神/同伴支線全淹「📜 委託」;使用者拍板 方案B 6 桶·主線桶名「🌋 湮滅危機」〔避 codex「沒有主線逼你走」措辭張力〕·保持最小)。**純顯示層 `console.py`**:`_QUEST_GROUPS` 6 桶(湮滅危機/起手任務/公會/試煉/同伴/委託)+ `_QUEST_SOURCE_BUCKET` 17 source 查表 + `_quest_group` 三層判別(**source 查表 → faction 後備 → other 兜底**;source 優先於 faction → 未來非 guild source 補掛 faction 不誤入公會桶);歸桶:relic/undercover→公會·daedric/divine/arcane/holy→試煉·companion→同伴·board/npc/ruler/rumor/三cure→委託(ruler 名自帶「領主委託:」前綴可讀);「已完成 N 件委託」→「N 項任務」(index.html+console.py 兩處同步)。**零前端/零終端退路改動**(renderQuests/fallback 通用迭代 groups·空桶隱藏保留)·桶內接取序不動·零存檔欄·未碰 combat/formulas → sim 天然不受影響。新 `test_quest_log` 5 測(出貨 source 全登錄/faction⟺guild trip-wire/代表歸桶/未知 source 後備/桶序+entry 形狀);run_all **132**·browser_ui 16。⚠ 環境曾被重置 → 免 root 修復(uv sync --extra dev + playwright chromium + NSS deb→`~/.local/lib/nss` + `~/.bashrc` export)恢復 R163 硬閘門。🔴 加新 source 必登錄查表(test 攔);判別維持 source-first;origin 桶 title 必含「起手」·entry 形狀勿動;陳舊 qid KeyError=既有風險刻意未夾帶修(另行拍板) | save-safe |
| R160c | **服務可發現性 III:世界地圖服務標記/篩選(公會徽 + 篩選高亮 + 最近 X 導航)**(承「評估 地圖服務標記/篩選」設計面板 3 方案→judge 使用者拍板 S2;地圖 65 同字符 marker 把服務藏在逐一點擊後·無徽記/篩選/最近X)。**🔴 純前端(唯一改 index.html renderMap)·零後端/存檔/combat·payload 已備 svc/svc_all/hops 零新欄 → sim byte-identical·前端 JS 不由 run_all 執行(node --check+DOM-mock+契約斷言驗)**。**① 篩選 chip 列**(`.msvcbar`·pan surface 外·role=group):動態衍生自 svc_all(visible 閘地點自動排除)·固定序·單字縮寫·單選(再點同項/全部=清除)·aria-pressed·**無 data-key**。**② 高亮+最近X**:選一項→匹配城 `.svcmatch` 金光·`.filtering` 暗化其餘(★你在此豁免)·`.msvc-find`(aria-live)『全圖 N 處·最近<城>(N 段·約M時)[前往]』(hops 排序·你所在地即有分支先於最近·前往走既有 route:)。**③ 公會徽 pip**(每城 svc 公會單字·**復用既有 .mlabel .prov/.zoomed gate**→總覽純淨·聚焦省份才現;`.gfilter`〔只公會篩選〕匹配城總覽亦強制顯);draw() 尾呼 applyMatch 切省重套。**對抗審查 3 維 15-agent→0 blocker/major·12 nit(8 修·3 by-design)**:公會徽強制顯 .filtering→.gfilter(常見服務篩選不顯無關公會徽)·mi-hint scoped .mapinfo→.msvc-find 補淡化·pip margin 減角標重疊·chip 觸控目標 max(24px)守 WCAG·role=group·toggle-off·svcList→svcKeys 去 shadow·修空洞 test 斷言。by-design:篩選高亮限當前檢視(最近X 說明全域)·公會徽預設省檢視即顯(總覽才純淨=刻意)·單字 chip 靠首點 caption+aria-label。run_all **131**(純前端·未碰 Python)。零新存檔欄。**🔴 地圖服務標記/篩選純前端 renderMap(payload 已備勿加後端欄);控件在 pan surface 外·無 data-key;公會徽復用 .prov/.zoomed gate·強制顯走 .gfilter;最近X 走 hops+既有 route:;新 CSS 用已定義 token+fs-scale·觸控 24px;前端改動走 node --check+DOM-mock+契約斷言。前瞻**:目錄可行動化(R114D 放開拍板)·「何處有售」反查·零散站點 codex | |
| R160b | **服務可發現性 II:馬廄/房產進反查通道 + 地點卡就地易讀性(城鎮專精現場可見)**(承「評估 服務可發現性」survey 使用者拍板 1+2;R159 立反查骨幹但 馬廄/房產不在 services vocab→落在所有通道外·地點卡無 services 欄→地圖比現場清楚)。**🔴 純 UI/唯讀衍生·零存檔欄·未碰 combat/formulas/magic → sim byte-identical**。**① 馬廄/房產進三通道**(`has_stable`/`house_at` O(1) accessor 衍生·非 service key·不破 R159 詞彙守衛):`_map_view` svc_all 尾附 馬廄/房產 + codex 目錄「馬廄與房產」section(馬廄一行·房產逐城帶宅名/等級/價) + 地點卡。**② 就地易讀性**:`_location_view` 新 `services` 欄 = `_town_facility_chips`(公會+服務+馬廄/房產·brief 省算)·前端 `renderLocation`「🏪 設施」chips 行(fs-scale·主題安全 CSS)·**標 R29 專精**(訓練師標宗師技 `訓練師（重甲宗師）`·法師公會標學派 `法師公會（毀滅系）`/`（通才）`→ R29 專精首次現場可見)。新 helper `_city_advanced_schools`(directory+地點卡共用·base 可傳入)抽出 R159 內聯學派衍生·**byte-identical 全 25 城驗**。**對抗審查 3 維 8-agent→0 blocker/major·5 nit(3 修·2 by-design)**:helper 傳 base 仍重掃→只讀本城 stock 復 O(N);`.loc-svc` 未定義 token `--parch`→`--ink`;專精標註全半形括號不配 `（…)`→平衡全形 `（…）`(+多學派文法 future-proof)。by-design:多學派分支現資料不可達;地圖只列馬廄/房產不標專精(速覽 vs 地點卡細節)。run_all **131**(test_web/test_codex +2·e2e·JS node --check)。零新存檔欄。**🔴 馬廄/房產走 accessor 衍生(非 vocab);地點卡專精標註走 `_town_facility_chips`;學派衍生單一來源 `_city_advanced_schools`(directory 傳 base 免重掃·勿兩處漂移);brief 不算 services;前端 chip 用已定義 CSS token(勿引未定義 `--parch`);中文標註括號平衡全形。前瞻**:目錄可行動化(discover→navigate·R114D 放開拍板)·地圖服務標記/篩選·「何處有售」反查·零散站點 codex 批次 | |
| R160 | **名人堂 / 往昔英雄 + 成就補完(誓福按來源 / 試煉 boss / 忠誠弧 · 修 curse-rank 回歸)**(承「後續可做」survey 使用者拍板 #4;R158 開槽位但傳奇死抹槽=英雄永久消失·legacy 結算即丟棄·成就僅 40)。**🔴 純 UI/持久化·零存檔格式變更(hall.json 獨立檔·不加 Character/GameState 欄)·未碰 combat/formulas/magic → sim byte-identical**。**① 名人堂 `systems/hall.py`**:`~/.tesrpg/hall.json`(獨立於槽位·HALL_PATH 模組全域供測試隔離·鏡像 saves.SAVES_DIR)·`record` 由 legacy 結算蒸餾留名→依分數排序 top `MAX_ENTRIES=20`→原子寫(.tmp+replace)·`load_entries` 毀損安全(缺檔/壞JSON/非list/**score:null** 皆回 []·`_score` 防 int(None) 崩)·`is_terminal`=隱退(任模式)或傳奇死(**傳奇死抹槽卻留名**·冒險死=續命非終局→不留名免重複)·`main.end_run` legacy 算一次→is_terminal 則 record·主選單 has_entries 列「🏛 名人堂」→`hall_screen`(web panel+終端 fallback R27)。**② 成就 40→46**:修 R57 curse-rank 回歸(血主/頭狼 index 5·`blood_matron`/`alpha_wolf` faction_rank 3→5)·retarget prince_favored/chosen(boons_count→`boons_source daedric` 名符其實)·**3 新 cond.type** `boons_source`(來源集由 boons.json 新 `source` 欄即時派生·防陳舊)/`loyalty_arcs`(**單調**·數 completed_quests 內同伴 personal_quest·遣散不失效)/`world_event`·**6 新成就**(divine 3/全 9·crisis_ender 走 **world_event oblivion_crisis_ended** 雙結局公平·umaril/archmage kill_boss·忠誠弧)·boons.json 39 誓福加 source(惰性·文字級插入 +39 行)。**對抗審查 4 維 15-agent→0 blocker·1 major+5 全修·2 nit by-design**:major=test_saves 驅動終局 end_run 未隔離 HALL_PATH→污染真實 hall.json(R158 教訓重演)→補隔離(驗 bare run_all 不再污染);load_entries score:null 崩→`_score`;crisis_ender 墮落結局打 diminished→改 world_event;boons 827→39 行;divine「祝福」撞 R107→「選民試煉/誓福」;loyal_bond 遣散失效→單調。接受:隱退續玩再隱退產第二筆(每次隱退=一生篇章·by-design)。run_all **131**(新 test_hall 8·test_achievements 擴)。零新存檔欄。**🔴 名人堂=獨立 hall.json(零存檔格式變更·HALL_PATH 模組全域·驅動終局 end_run 測試務必隔離·鏡像 R158);只 is_terminal 留名;排序走 `_score` 防毀損崩;成就唯讀零欄·新 cond.type 三步;誓福按來源走 boons.json source 即時派生(加誓福須標 source·test 守);crisis 類用 world_event 雙結局公平;boons 改動用文字級插入保格式。前瞻**:backlog C 無障礙·自動存檔(critic #2)·存檔改名 | save-safe |
| R159 | **服務可發現性:城鎮服務/宗師目錄(codex)+ 修 R153 地圖公會大廳遮蔽回歸**(承「後續可做」survey 使用者拍板 #3;R29 專精化零遊戲內指引 + R153 svc_all 回歸)。**🔴 純 UI/唯讀衍生·零存檔欄·未碰 combat/formulas/magic → sim byte-identical·前端零改動**。**修回歸**:`_map_view` svc_all 原只取 `_SERVICE_CN`(缺 companions/knights_nine/dark_brotherhood/mythic_dawn 鍵)而前端 svc_all 非空即優先 → 公會大廳永久遮蔽;修=模組級 `_GUILD_HALL_CN`(顯示名=factions.json 正典名·審查修 聖騎士團→九神騎士團)+`_SVC_FULL` 通用全名·svc_all=公會領頭+通用全名·svc 為其前綴·終端 fallback 不動(R27)。**目錄**:codex `services_directory`(order 115·`dynamic:"services"`→`_services_index_rows`)=質化 sections + 4 段動態反查表(服務覆蓋計數/公會大廳〔≤6 列名·大公會摘要〕/宗師一覽〔上限讀 `formulas.TRAINER_CAP`/`SKILL_CAP`〕/法師學派佈局〔**純推導**:保底集=全 spell_stock 交集·⊇六學派→通才·extra=∅→「僅基礎」顯式列出〕);**visible 閘地點全程排除**(神話黎明聖殿不入)。**對抗審查 4 維 23 confirmed 全處置**(major:「旅店給精神飽滿」false→改文+指路 li;跨省採購 false〔帝都=聯集〕·訓練師漏 skills 覆寫層·通才門檻改⊇六學派全集·泛字樣斷言精確化;黑暗兄弟會列名=專項裁決一致〔無 discovery 規則·R153 前已顯示〕)。**守衛**:test_web 詞彙守衛(services 值集 ⊆ `_GUILD_HALL_CN`∪`_SVC_FULL`·公會名=factions 正典名——R153 回歸成因=鍵集漂移,漏登記即紅燈)+ test_codex R29 不變式(每座 spell_stock 城守一門/通才/僅基礎·城名必現)。run_all **130**(+2 測)·零新存檔欄。**🔴 目錄純推導勿硬寫城名/數值;新增 service 鍵須同步登記兩 CN 表(詞彙守衛攔);公會顯示名=factions.json 正典名;visible 閘地點不入目錄/地圖;svc_all=公會領頭+通用全名契約不變;codex 內容準確性=第一級審查維度(R60)。前瞻**:backlog C 無障礙·自動存檔(critic #2)·存檔改名 | |
| R158 | **存檔槽位 / 多角色:角色名冊主選單 + 遷移 + 原子寫 + 隱退存檔**(承「全面評估 UI/UX」審計 critic 缺口 #1·最大結構性缺口:單一 save.json「新遊戲」即覆蓋·養不了第二個角色)。**🔴 存讀檔格式完全不變(R03)·不加任何存檔欄·from_dict 忽略未知鍵·sim byte-identical·時間戳用檔案 mtime**。新 `systems/saves.py`(純持久化·無 ui):`~/.tesrpg/saves/slotN.json`(MAX_SLOTS=8)·slot_path/used_slots/next_free_slot/slot_meta〔毀損/缺檔安全〕/roster/delete_slot/migrate_legacy·SAVES_DIR/LEGACY_PATH 模組全域(測試覆寫暫存)。main.py:移除 SAVE_PATH→`_active_save`(當前槽)·action_save/隱退寫之·end_run 傳奇死抹**自己**槽 permadeath/冒險死保留/**隱退存最終態**·main() 啟動 migrate_legacy→**角色名冊主選單**(繼續/＋新角色進**空槽不覆蓋**/🗑刪除確認/離開)·新角配 next_free + **初次即存檔**。向後相容:舊 save.json→slot1(冪等·不覆蓋既有)。state.py GameState.save **原子化**(.tmp+replace·中斷不毀存檔·格式不變)。**🔴 對抗審查 4 維 14-agent→0 blocker/major·7 confirmed(2 minor+5 nit)全修**(非原子寫→中斷毀檔·migrate mkdir 在 try 外崩啟動·rename 孤兒→跨fs copy fallback·**隱退載到上次手動存檔非隱退態**→存最終態·滿槽新角仍可選→隱藏·測試未還原 _active_save→復位)。run_all **130**(新 test_saves 8 測·session/webdriver 隔離 SAVES_DIR 到暫存零污染)·sim byte-identical。**🔴 存讀檔格式不變(R03·不加欄·mtime 時間戳);槽位走 saves.py(SAVES_DIR/LEGACY_PATH 測試須覆寫暫存);GameState.save 原子寫(.tmp+replace 勿回直寫);新角進 next_free 空槽絕不覆蓋;傳奇死抹 _active_save/冒險死保留/隱退存最終態;migrate 冪等不覆蓋 slot1·mkdir/rename 納 try·跨fs copy fallback;驅動 main() 測試須隔離 SAVES_DIR+復位 _active_save;純持久化/UI→sim 天然不受影響。前瞻**:自動存檔(critic #2·仍手動)·存檔改名·backlog C 無障礙· F 服務目錄 | save-safe |
| R157 | **設定面板:字級 / 深色主題變體 / 減動態 / 操作說明**(承「全面評估 UI/UX」審計 critic 缺口 #3;使用者拍板深色變體·非完整淺色)。**🔴 純前端·唯一改 index.html·零 Python·client-only localStorage → sim/run_all 天然不受影響·R27 web chrome**。⚙ overlay(masthead):字級(小/標準/大/特大)·主題(經典金/高對比〔對應審計 WCAG-AA〕/羊皮紙)·動態效果(開/關)·操作說明。**字級**:所有 CSS font-size 包 `calc(<orig>*var(--fs-scale,1))`(預設 fallback 1→byte-identical·純文字縮放不動版面免溢出·input.field max(16px,…) 免 iOS 聚焦縮放)。**主題**:`:root[data-theme]` 純覆寫調色盤變數(零元件改動)+ body 漸層三停 var 化(預設=原 hex→byte-identical)·經典=無 attr。**減動態**:`[data-motion=off] *{animation/transition:none!important}`。**無閃爍**:head IIFE loader 首屏套 attr。**🔴 對抗審查 4 維 16-agent→0 blocker/major·10 confirmed 全 minor/nit 全修**(焦點困→`#app.inert`+Tab-wrap+遊戲 keydown settingsOpen 讓位·重繪還焦點·aria-pressed·鎖背景捲動·iOS zoom·safe-area·head 全域洩漏→IIFE)。node --check·DOM-mock 持久化 9 例+畸形安全+無洩漏·serves HTTP 200·run_all **129**(未碰 Python)。零新存檔欄。**🔴 設定純前端·唯一改 index.html·client-only localStorage(不寫後端/存檔);字級走 --fs-scale 乘 font-size(預設 1=byte-identical·非 zoom·input 16px 保底);主題純 :root[data-theme] 覆寫(body 漸層 var 化預設=原 hex)·深色變體(不做完整淺色);overlay #app.inert 困焦+鎖捲+aria-pressed;無閃爍靠 head IIFE loader;純前端→sim/run_all 天然不受影響。前瞻**:完整淺色主題(需重調漸層/陰影)· backlog C 無障礙深化(overlay 已示範 inert/aria)· F 服務目錄· G 存檔槽位 | |
| R156 | **戰鬥/日誌可讀性:戰鬥流水帳與敘事分離 + 選單分桶 + 屍體/本回合收束**(承「全面評估 UI/UX」審計 Rank 4·high/medium)。最高頻迴圈可讀性:逐擊流水帳淹沒 240 上限永久故事日誌、回合選單 12–18 項扁平牆、本回合區無高度上限、群戰屍體洗版。**純 UI/呈現·零碰 combat/formulas/magic·零新存檔欄·sim byte-identical**。**D1**:`backend.add_log` 加 ephemeral 旗(只 True 才加鍵→敘事幀 byte-identical)·console `_emit_log/_emit_or_print/message` 串之·逐回合流水帳標 ephemeral(combat_event/tick/ally/active_effects **+ 審查 MAJOR 修:施法/戰技/威能/援護 37 條 run_battle ui.message 改走新 `_cmsg`**〔melee 清、caster 未清=不對稱〕·保留敘事〔感染/傷兵/逃脫/用藥/升級/戰利品/進度〕)·`appendJournal` 退役丟棄 `.eph`(不進永久 jbody·整回合流水帳不留孤兒分隔)。**D3**:`.jlatest` max-height+overflow(寬 AoE 不頂出輸入區)。**D4**:`_combat_view` 存活敵先列(key 對齊目標選單)·>2 已倒下收束成 `☠ 已倒下 ×N`。**D2**:`_grouped_combat_menu` 分「攻擊/威能·戰技〔審查 nit:非「法術·威能」〕/架式/應變/脫戰」·repeat 攻擊桶首=編號1(守 R113)·**分桶純 web**(`ui._web is None`→退扁平 ui.menu→既有 ~14 combat 測試零改)·grouped/flat 回同 key→dispatch 不變。**🔴 sim byte-identical**(呈現層·sim 走自有 _round·solo 秒殺 0.0%)·零新存檔欄·R27 fallback 保留。run_all **129**(新 test_combat_readability_d_batch_r156)·node --check·**對抗審查 4 維 14-agent→0 blocker·1 major+2 nit 全修**(major D1 漏標施法流水帳·nit 死變數/桶名)·4 refuted。**🔴 逐回合流水帳走 `_cmsg`/combat_event(ephemeral)·敘事(感染/傷兵/逃脫/用藥/升級/戰利品/進度)非 ephemeral·add_log 只 True 加鍵(敘事幀 byte-identical)·動作分桶純 web(ui._web is None 退扁平·守測試)·grouped/flat 回同 key→dispatch 不變·repeat 攻擊桶首守 R113·屍體>2 收束·存活敵 key 對齊 _choose_enemy_target·純呈現→sim 天然不受影響。前瞻 backlog**:C 無障礙· F 服務目錄· G 存檔槽位· H 設定面板 | |
| R155 | **新手清晰度批次:預設職業機制說明 + 創角衍生數值預覽 + 系統開場指路 + 種族/星座圖鑑**(承「全面評估 UI/UX」審計 Rank 5·high/small)。R125/R124 只補冷門路徑:預設(★推薦)職業從不說明專精/偏好/主修;創角做永久抉擇時圖鑑不可開+總覽只列名字;深系統首登只有 flavor 無指路。**純 UI/內容·零碰 combat/formulas/magic·零新存檔欄**。E1 `_explain_build_levers()`(專精+20%/★偏好+5/主修7)在 `_choose_class` 印(含預設路徑)+ `_create_custom_class` 去重複列;E2 `create_character` 開場一句(chip=加成·learn-by-doing·指南何在);E3 `_creation_review` 用丟棄 rng 建預覽角色渲 `character_sheet`(build_character 純函式 R33·try/except 不擋創角);E4/E5 新 `_onset_hinted`(set\|None·**None=game_loop 外→`_hint_once` no-op**·`main()` finally 復位)+ 4 掛點一次性指路(詛咒感染/首里程碑/首煉金/首戰);E6 codex `races_signs` 章(dynamic hook·`_races_signs_index_rows` 掃 gamedata 防陳舊·導語**弱點歸星座非種族**)。**🔴 sim byte-identical**(未碰戰鬥·首戰指路 rng 前 gated ui.message·sim/game_loop 外 `_onset_hinted` 恆 None→no-op·sim 不呼叫 main.run_battle)·零新存檔欄·R27 fallback 保留。run_all **129**(新 test_onboarding·test_codex +races_signs·test_web +main() finally 不變式·test_econ_qol/test_webdriver 補還原)·full-creation e2e·**對抗審查 4 維 15-agent→0 blocker/major·9 confirmed 全 minor/nit 全修**(`_onset_hinted` 未於 game_loop 退出復位·races_signs 謊稱種族弱火·指南章名不符·spec XP int 截斷 19%→round 20%·戰鬥提示引用不存在「↻ 再次」標籤+誇大範圍·chip 窄化·自訂路徑雙重說明)。**🔴 新手指路走 `_hint_once`(game_loop 外恆 None→no-op·main() finally 復位·直呼 game_loop 測試須還原);指南章名須與 codex title 精確一致;顯示數值用 round 非 int;創角預覽走 build_character(純·丟棄 rng·try/except 不擋);codex 速查用 dynamic 鉤子·弱點歸星座非種族;純 UI/內容→sim 天然不受影響。前瞻 backlog**:C 無障礙· D 戰鬥/日誌可讀性· F 服務目錄· G 存檔槽位· H 設定面板 | |
| R154 | **角色卡資訊補完:誓福/疾病/附魔充能/魂 token review + 修 renown 死視圖**(承「全面評估 UI/UX」審計 Rank 1·high/small·最高 ROI)。**現況**:console.py 對 boons **零引用**·39 道永久誓福(17 親王+9 神性+詛咒/收尾)是最大終局回報層卻在 web 看不到;疾病只剩「染病×N」一行;充能型附魔耗盡無提示;魂 token 只在祭壇可見;**R101 renown 稱號因 `_status_view` 死碼從不顯示**(web status_line 直接回 HUD·status 卡從不發送)。**純唯讀 view-model(鏡像 sheet_vampirism 範式)+ 刪死碼·零碰 combat/formulas/magic·零新存檔欄·不 mutate char/state**。console.py:新 `sheet_boons`(char.boons∩gamedata.boons+dagon_boon·`_boon_bonus_parts` 譯 attr/skill/resist/magicka/spell_power)+ `sheet_diseases`(`_worsen_step` 當前懲罰 base+worsen×step / 下次惡化 / DoT·per-disease 即算)+ `_charge_suffix`(閘 char.enchant_charges·耗盡⚠·防毀損存檔守 magnitude)接 weapon_line/sheet_equipment·`_hud_view` 加 renown/souls·**刪死碼 `_status_view`**;main.py:action_character_sheet 加「永久誓福」/「疾病」選單(閘用登錄表交集對齊面板);index.html:renderHud 加 renown(金)/souls(紫)chip+`.h-chip.mag`·**刪 renderStatus + VIEWS status 條目**。**🔴 sim byte-identical**(未碰戰鬥·solo 秒殺 0.0%)·零新存檔欄·R27 終端 fallback 全保留(只刪確認死碼·grep 零引用)。run_all **128**(新 test_sheet_boons_diseases_charges_and_hud_r154)·node --check·**對抗審查 4 維 9-agent→0 blocker/major·3 confirmed 全 nit 全修**(charge magnitude KeyError·選單閘 vs 面板過濾不一致×2)。**🔴 角色卡新 review 走 _emit_panel(鏡像 sheet_vampirism)·唯讀不 mutate·充能讀數閘 enchant_charges+magnitude·誓福/疾病顯有效值(含惡化階)·選單閘對齊面板登錄表過濾·刪死碼前 grep 全域零引用·唯讀 view-model → sim 天然不受影響。前瞻 backlog**:C 無障礙· D 戰鬥/日誌可讀性· E 新手清晰度· F 服務目錄· G 存檔槽位· H 設定面板 | |
| R153 | **互動世界地圖:面板內點擊旅行 + 我的位置聚焦 + 手機縮放/滾動陷阱修復**(承「全面評估 UI/UX」多代理審計 8 維 51 findings→8 主題;使用者拍板做 R114D 延後的旗艦缺口)。**現況**:地圖 marker 帶 `data-id` 只 `showInfo`·從不旅行(旅行是另條 action_travel menu);唯讀 view 之後不接 prompt;手機無法縮放(wheel-only)+`touch-action:none` 滾動陷阱。**解衝突**:前往按鈕放進 `.mapinfo` 面板(pan surface 之外)→ marker 維持選取/拖曳·面板按鈕直接 `submit('route:'+lid)`;marker **不掛 data-key**(免上百節點搶數字鍵);地圖動作由唯讀 view 升級為**互動 grouped prompt**(extra_keys=`route:<lids>`·**menu 不認 extra_keys 只 grouped 認**·backend.py:119-127)。**實作**:①`world.routes_from(char,gd)→{lid:(hops,hours)}` 一次 BFS(複用 is_visible/route_to/route_hours·純圖走零 rng 決定性);②`console._map_view(char,gd,reach=None)`/`world_map(...,reach=None)` node 加 hops/hours/svc_all·**reach=None→全 None→逐位元組同唯讀**(back-compat)·終端 fallback 不動(R27);③`main.action_map(state,gd)` 迴圈 emit map+grouped_menu(route: extra_keys + __list__ 清單退路〔複用 action_travel〕+ __back__)·route:→`_travel_route`(R114D·單跳亦正確)·抵達/中斷後重繪·hub `died=action_map(...)`;④`index.html renderMap` showInfo 可達 node 加真 `<button.mi-go>前往(N段·約M時)</button>`→submit(面板內·零拖曳衝突·惠鍵盤)·◎我的位置+開圖聚焦當前省·＋/－縮放鈕(reuse wheel 數學)+雙指 pinch(pointers Map·tap/pan/pinch 分流)·`applyT` `touchAction=scale>1?none:pan-y`(解滾動陷阱)。**🔴 未碰 combat/formulas/magic → sim_assassin BYTE-IDENTICAL**(route: 只是 _travel_route 另一入口·旅途 rng 同清單旅行 R114D·對齊 R132 baseline)·**零新存檔欄**(route: 暫態·map 中繼唯讀衍生)·R27 web-only(終端 fallback 保留·reach=None back-compat)。run_all **128**(test_web 擴 _map_view hops/hours/svc_all + routes_from 對齊 route_to/route_hours + 新 test_action_map_route_dispatch)·node --check JS 合法·真 WebBackend e2e(hub→map→199 route: extra_keys·__back__ 回 hub)·伺服器 boot HTTP 200。**🔴 點擊旅行走 .mapinfo 面板按鈕 submit('route:'+lid)(marker 不掛 data-key);互動地圖必用 grouped prompt(menu 不認 extra_keys);reach=None 保 back-compat;終端 fallback 不刪;routes_from 語意對齊 route_to/route_hours;動 UI 導航 sim 天然不受影響。前瞻 backlog**:A 角色卡資訊補完(誓福/疾病/附魔充能/魂石+修 renown 死視圖·ROI 最高)·C 無障礙打底·D 戰鬥/日誌可讀性·E 新手清晰度·F 服務目錄·G 存檔槽位/多角色·H 設定面板(字級/主題) | |
| R152 | **幻術「狂亂」frenzy:借刀殺人的攻擊向控場(illusion 招牌未交付)**(survey #3·skills.json illusion 自述「隱身/魅惑/狂亂」但 grep 全空只 flavor·機制缺席·R118/R104 前瞻;R31「不做 frenzy」是毒劑脈絡·重定向版不受限)。**新控場 kind `frenzied`=心智劫持**(hard control·鏡像 fear:solo 機率抵抗+去重+**無心智免疫 R140**)但**不 incapacitate** —— 狂亂敵**仍行動**只是攻擊隨機另一存活同類敵(借刀殺人);孤身無同類→空轉自限(≈paralyze 跳過·**不破終王牆**)。**benumb 三步**:magic.py `_CONTROL_KINDS`/`_HARD_CONTROL`(:909)+`frenzied`·無心智閘(:927)+`frenzied`·新 `is_frenzied`·`_status_verb`/`tick` 文案(不動 `is_incapacitated`/`resisted_mind`);main.py 敵方階段(:1322)`if is_frenzied(e):` 選隨機同類 `kin=[o≠e·alive]`·**空 kin→continue(🔴 守 rng.choice([]) IndexError)**·非狂亂走既有 pick_player_side_target;console `_STATUS_TAG` frenzied→狂亂;spells.json frenzy(26·enemy·apply_status)+mass_frenzy(54·all·status_all·R17);world.json spell_stock 4 illusion 城。**🔴 敵 vs 敵純傷害**(resolve_attack 全 _is_player-gated·無 on_hit/附魔/反傷·無 A→B→A 環·殺召主照 scatter_orphan_summons)。**🔴 frenzy 不給玩家傷害→不碰 solo 夾/dagon 牆·sim_assassin BYTE-IDENTICAL**(隔離 worktree·刺客不施法+sim 各自迴圈硬鎖玩家·非狂亂 is_frenzied False 短路在 rng 前)·**免 re-sim 平衡**(CC-on-enemy·群戰 by-design·mass_frenzy cost 54 唯一旋鈕)·零新存檔欄。run_all **128**(新 test_frenzy 7 測:cast/整合重定向〔非空測 revert→FAIL〕/孤身空轉/mass R17/**cast 路由守 solo+mindless**/**祭司完全劫持**;test_solo_control/mindless 擴 frenzied)·端到端煙霧·**對抗審查 4 維→0 blocker·1 major+2 minor 全修**(major:cast `_CONTROL_KINDS` 路由無測守 solo/mindless 紅線→補 cast-path 測;minor:狂亂祭司/召主 frenzy 須置於 support/summon 之前=完全劫持;minor:單體 apply_status 控場讀 apply_control 回傳·免疫/抵抗不印假成功)。**🔴 狂亂=hard 控場但不入 is_incapacitated;重定向必守空 kin;敵敵傷害靠 _is_player-gate 勿放寬 on_hit;不給玩家傷害;is_frenzied 短路在 rng 前保 byte-identical | re-sim |
| R151 | **地城 FEATURE 內容擴充:模板 6→26 + 鋪進 26 座泛用地城(依 biome 分味·依 danger 分 loot 階)**(survey #6·R146 引擎只 3/66 座宣告 features→95% 機制同質;使用者拍板純 v1 內容+全 26 座泛用)。**純改 JSON·零碰 combat/formulas/引擎·零存檔欄·免 re-sim**(shrine 安全池由 test_dungeon_features 靜態強制)。20 新模板(`dungeon_features.json`):8 biome lore + 6 shrine(補齊安全池 frost/poison/disease/shock/magic 抗 + agi/speed/luck + block/illusion/light_armor·risk=damage 或暫態 curse)+ 6 puzzle(新檢定 alchemy/scout/illusion/mysticism·low/mid/high loot 階);26 座泛用地城(27 有 reinfest 除 cedernoc_cave 控制組)加 features:[…]·升級 ashfall/volenfell(只加不刪)·特殊地城不鋪。**對映=biome lore+biome shrine+danger 階 puzzle**(∵ 每層 rng.choice(pool)→池內每 puzzle 須階配:d1-2 low/d3-4 mid/d5 high)。**內容準確性審查 3 維→0 blocker/major·8 findings 全 minor/nit 已修**(中和艾雷德→上古精靈·腹地→ayleid〔imperial_relief 移除〕·rourken→dwemer·bangkorai→yokudan·hag_rock→lost_expedition·叢林新增 shrine_greenpact 綠約取代虎人月井·tier 單調 alchemists 46→44)。run_all **127**(test_dungeon_features +2:泛用全鋪+cedernoc 空、puzzle 階配)·20 模板 resolve 煙霧·免 re-sim(不在 sim 路徑)。**🔴 加特色格純改 dungeon_features.json(shrine 守安全池)+dungeons.json features opt-in;puzzle 階配 danger(守 test);lore↔地城正典契合(biome 只是分類);cedernoc 永不鋪;隨地城重生可重複勿加持久旗標 | content |
| R150 | **修 R129 同伴戰術傾向:消費端 build-id 誤比對(招牌功能死機制)+ 游擊先點治療者**(survey #2·唯一確認真 bug)。**根因**:`party.companion_tactic` 回 tactic-kind(taunt/focus),但 main.py ally 階段(1280/1285)比對 build id(bulwark/skirmisher)→ 恆 False → 鎮守自嘲諷從不觸發·游擊集火退回 `state.rng.choice` 隨機;舊 test_companion_build **只驗 getter 未驗消費** → 漏抓。**修**:兩處比對改 taunt/focus;順帶兌現 desc「先點治療者」= 游擊改用新 `party.focus_target`(優先 R87 敵方支援施法者〔`is_support_caster` 讀 bestiary spells∈heal_other/ward_ally/regen_aura〕·其次最低血·決定性 min·空→None)。**🔴 byte-identical**:未碰 combat/formulas·sim 不設 companion_build→tactic 恆 None→走既有 rng.choice·sim_assassin BYTE-IDENTICAL(隔離 worktree)·召喚物 tactic 恆 None·零新存檔欄。run_all **127**(新 test_companion_tactics:focus_target 單元 + run_battle 消費端整合;**逐分支獨立 revert→FAIL 證非空測**)·對抗審查 2 維抓到 1 confirmed(初版游擊測 RNG(5)+雙敵等血=空測〔buggy 隨機退路恰命中〕·且 run() 字母序遮蔽 → 改 RNG(1)+治療者血高於雜兵+逐分支獨立驗)。**🔴 companion_tactic 回 tactic-kind 非 build id→消費端比對 tactic-kind;加戰術須同步消費分支+消費端整合測(getter-only 漏抓死機制);游擊走 focus_target·support-caster 敵人自動被優先 | save-safe |
| R149 | **元素刃三術招牌狀態:焰刃引燃/霜刃凍麻/雷刃踉蹌**(survey #6·spells.json:362-364 三刃結構相同只差 element·皆 weapon_imbue flat8·combat 讀只加元素傷無 rider=自我重複填充·選哪系刃零差異)。**純資料+combat 小改·複用既有 kind(dot/benumb/stagger)無需 R21 三步**:三刃各掛元素招牌 rider 給「選哪系刃」戰術身份 —— 焰刃 fire burn DoT(mag4/t3·傷害流·吃火抗)/霜刃 benumb(mag0.15/t2·R73 冰軟控·減命中生存流·別於霜附魔 weaken)/雷刃 stagger(chance0.3/t1·conduct 是 magic.cast 專用武器路徑不可用→用 stagger)。spells.json effect 加 rider 子鍵·magic.py copy 進 active_effect+文案·combat.py 命中迴圈呼叫新 _apply_imbue_rider(DoT 同源 imbue_dot 刷新取 max·控場走 apply_control〔R44 同源去重·benumb_hit_penalty 取 max 非 sum〕)。**🔴 rider 非 burst**→不碰 solo 偷襲夾(專測 imbue 單擊夾 10<<410)/終王牆(probe 戰法師持焰刃 vs dagon720=0/20·fire85→DoT~0)·soft 控對 solo 無免疫但僅減命中非 lock·零新存檔欄。run_all **126**(test_magic +5)·sim_assassin BYTE-IDENTICAL(隔離 worktree·刺客不施法→rider path 不可達)·sim_builds byte-identical(_battlemage_act 不施刃)·對抗審查 4 維→0 findings。**🔴 元素刃 rider 純改 spells.json effect.rider(status∈dot/benumb/stagger/weaken/slow)+_apply_imbue_rider;DoT 走同源刷新取 max·控場走 apply_control 非 burst·勿放進 burst 路徑(不碰 solo 夾/終王牆);動 magnitude/chance→sim_assassin+終王 probe | re-sim |
| R148 | **血族↔獵群宿仇接進 R96 敵意引擎(選邊詛咒血脈的世界後果)**(「下一步」survey #5·對抗查證確認 ⚠「curse opt-out」僅 Phase2 據點消長〔R96 明列·非 Phase1 敵意〕→接活死敘事;R52/R57 寫滿「千年宿敵」敘事卻只停任務文字)。**核心=近純資料·零存檔欄**:factions.json `coven_vampire↔werewolf_pack` 互列 rivals → 既有 R96 `faction_hostility`(衍生自 char.factions·**對舊存檔自動生效**)立刻對「選哪一邊詛咒」產世界後果;**吸血鬼↔狼人互斥**(vampirism.py:70/lycanthropy.py:260)→ 敵意自洽·rivals 入會互斥冗餘無害。**membership-gated + rank ramp**:只有*加入*詛咒公會者被獵殺·路途伏擊 `_GUILD_HOSTILITY_AMBUSH_MIN=2` → rank 1+ 才出動(rank-0 僅對話冷待)=深化承諾才招真敵;bounty>guild 不雙觸·與 R50 城內圍捕分屬路途/抵城。**主題化打手**(避「獵群派人類打手」違和):`_guild_enforcer_ambush` 加 per-faction `enforcer_creature`/`avenger_creature`/`ambush_msg` 覆寫(**無欄→通用 guild_enforcer/avenger·既有 4 公會逐位元組同**);4 新 spawn-only 主題怪 血族派吸血鬼(undead·R139 poison50)/獵群派狼人·戰力≈通用打手(伏擊難度不偏移)。**對抗審查 4 維→0 blocker/major·1 confirmed minor**:coven_avenger 凝視 on_hit fear 漏 `element:magic`→破 R145「板甲擋不住凝視」(荊棘/盾反可反心智攻擊);修=加 element:magic + 補「懾魂凝視」入 test_ranged_logic_r145._PSYCHIC 守衛〔原硬編名單漏之〕。run_all **126**·sim_assassin BYTE-IDENTICAL(未碰 combat/formulas·刺客不觸伏擊/不生成新怪·R132 baseline)·BESTIARY 重生 154。**🔴 加公會宿仇純改 factions.json rivals(對稱雙向);主題打手走 enforcer_creature/avenger_creature/ambush_msg(無欄=通用 byte-identical);新凝視/心智攻擊必標 element:magic(R145)+入 _PSYCHIC 守衛;敵意 membership-gated(勿改「純詛咒者即敵」) | content |
| R147 | **運動樹身份化:「調息」續戰(耗一回合換回體·主動招牌招式)**(盤點 #6·athletics 唯一 spec=combat 零主動招式樹〔連姊妹 acrobatics 都有 on_evade〕·R34-R43 冷技能功能化最後漏網;plan-mode 多輪·**重大轉折**:初版被動回體被使用者點名破口〔回體+6>攻擊成本→無限格擋姿態·且「揮擊永不累」違反現實直覺+是使用者的平衡取捨非我擅斷·我一度越界已還原重來〕→ 改「用動作換回體」)。**機制**:新戰鬥動作**「調息」**(鏡像 R126 用藥耗一回合·選單+run_battle handler):回體=`formulas.rest_fatigue_amount(athletics)`+`mastery.rest_bonus`(開源里程碑 SUM)·**🔴 自動解除 guard_stance/shield_wall**(不能持盾喘息=防無限姿態關鍵閘)·耗一回合=不攻擊=唯一成本閘。**樹重造**(開源調息 vs 節流省體·零 stat-dump):25 勻息·50 健步 vs 節氣·75 深息 vs 省勁·100 二次呼吸 vs 無盡之軀;新 kind `rest_bonus`(3步 SUM)·`fatigue_cost_bonus` 改 SUM+cap 0.35(R35)·flee_bonus 隨 escape_artist 移除·opt_id 走 ensure_mastery_choices 通用防呆(不需 RELOCATED)。**🔴 結構性零無限**(調息⇔攻擊互斥+自動解除姿態→攻擊淨流失·姿態煞車天然守·零輸出自限)·**不碰傷害層**→solo cap/終王牆結構不變。run_all **126**(+test_athletics_r147)·sim_assassin BYTE-IDENTICAL(隔離 worktree·sim 不走選單)·gauntlet probe 有界(18→20%·調息在 apex 加 0 不 trivialize)。🔴 調息必自動解除姿態(防無限·勿拆);續戰走動作換回體非被動(被動≥攻擊成本破 R138b);rest_bonus/fatigue_cost SUM-capped;動磁量→gauntlet probe | re-sim |
| R146 | **地城互動深化:第三種互動格 FEATURE(祭壇/碑文/機關)**(「下一步」8-agent 盤點 #4=最大探索缺口:66 座地城機制全同質·只遇怪/寶箱/陷阱;plan-mode·使用者五拍板:三 kind 全做·祭壇=賭博〔增益+代價〕·回報只限時增益守 byte-identical·進格自動觸發·可重複跟隨地城重生〔roguelike 風味·原子探索·零存檔〕)。**架構=占格特色格**(新 cell type `FEATURE`·開放格模型零連通風險·非層級旗標);opt-in `dungeons.json features:[id]`(既有 66 座 falsy→generate skip→**不耗 rng byte-identical**);三 kind 全走既有效果層**絕不碰 combat/formulas/character**。新 catalog `data/dungeon_features.json`(模板池)+ gamedata 載入;dungeoncrawl `FEATURE` 常數+佈格(1 格/層·rest[used]·不撞 boss/stairs);main.py dispatch(自動觸發)+`_resolve_feature/shrine/puzzle/risk`(復用 potion_buff.apply_buff/dungeon.open_container/ui.menu/luck_fortune/_dungeon_curse);console minimap ✧;3 座示範 wire(定點 Edit)。**🔴 shrine 安全池**(fortify_attribute 排除 strength/fortify_skill 排除武器+sneak/resist_element·≤單瓶上界·鏡像 boons+sim 包絡·guard 強制)·風險走傷害/暫態 curse(負層只減·任意技能安全);永久回報 boons 保留未來手擺格(v1 不用·需知會+sim)。run_all **125**(+test_dungeon_features)·sim BYTE-IDENTICAL(隔離 worktree·sim 不 crawl)·smoke 綠·**零新存檔欄**(FEATURE=local grid·增益走 potion_buffs 自動過期·curse 走 _dungeon_curse)。🔴 加特色格純改 dungeon_features.json+dungeons.json features opt-in;shrine 守安全池;隨地城重生可重複勿加持久旗標;擴 boons 須知會+sim | content |
| R145 | **現實邏輯審計批次 7(群 4·方案 C):板甲擋不住凝視·荊棘反彈不了弩矢**(探針證零平衡代價·使用者拍板 C·戰吼=物理)。**心智攻擊→magic ×14**(凝視/咒/尖嘯·元素閘自動排除反傷/盾反/重壓+修「板甲擋凝視」·on_hit fear rider 照走);**物理遠程→`ranged:true` ×8**(弩/銀箭/投石);**引擎 3 gate**:反傷區+盾反擊樹 `not atk.ranged`·**on_evade 反擊補元素+遠程雙閘**(原無閘=閃過龍息可反擊;落空分支 atk_element 未綁定→直讀 atk.element);R117 騎士反噬刻意不 gate(秩序魔法回沖)。run_all **124**(+test_ranged_logic_r145 5 測)·sim_assassin BYTE-IDENTICAL·canonical 零位移·BESTIARY 重生。**🎉 審計 8 群全了結(R139–R145)**。🔴 新怪:凝視/咒標 magic·投射標 ranged·戰吼=物理;三 gate 勿拆 | re-sim, content |
| R144 | **現實邏輯審計批次 6:世界/行為邏輯(達貢不在路上散步·巨狼真的沒穿甲)**(群 8 收官·審計六批完結)。**具名劇情王退隨機池 ×37**(weight:0·物種精英白名單 7 隻保留遊蕩·guard 掃全 solo);**獸形真的脫裝**:單一接縫 `stats.recompute_equipment`(beast→裝備 skills/attrs/resist/fortify 全空·transform/revert 過 recompute 自動切換)+ thorns/set/equip_* beast gate;**獸形服務閘**(施法/撬鎖/煉金/鍛造/公會/告示板);遭遇率 `max(起點,終點)` 危險度;`habitat_bound` 旗標(魚/元素不離鄉);`steal_chance(+item_id)` 重量扣減 0.015/重夾 0.35;斯庫瑪去 willpower+去回血(R20 續守);wild_beast 閘天際·highwayman 文案中立;石膚 desc 對齊。run_all **123**(+test_world_logic_r144 6 測)·sim BYTE-IDENTICAL·smoke 通過·零新存檔欄。🔴 solo 具名王必 weight:0(白名單 guard);獸形壓制住 recompute_equipment 單一接縫;水生/位面怪標 habitat_bound | re-sim, content |
| R143 | **現實邏輯審計批次 5:煉金材料主題重指派(火鹽不再教你格擋)**(群 7·26 材料重指派·保 R90 FAIL-pair 不變式)。新主題配對:格擋=[蟹殼,珍珠]·重甲=[巨魔脂,骨粉]·輕甲=[魚鱗,焦革]·物抗=[骨粉,蟹殼](軟脂/熊爪不再攜帶)·商業=[珍珠,月糖]·口才=[蝶翼,蛾翼]·開鎖=[火把蟲,夜鶯羽]·偵查=[螢光粉,鷹羽]·煉金=[灰燼薯,奈恩根]·骨粉+=召喚(死靈);canon:ash_yam→int(Morrowind)·bittergreen +毒(生食致死)·藍山花=[回魔,回血]·硫磺=[毀滅,抗火]。**確定性 brew-diff 全 1275 對:90 變動全在涉改材料·零洩漏**;可釀 kind 集合原樣(R90/91/92 測試全綠·smithing/mysticism 永禁);新共有對僅共有該 kind。run_all 122·sim BYTE-IDENTICAL。🔴 改材料效果必跑全配對 brew-diff;物抗載體=硬殼/骨 | content |
| R142 | **現實邏輯審計批次 4:裝備數據曲線(材質階梯不反轉)**(群 6 純 JSON):黑檀盾 AR 10→13/value 400(曾弱於同價矮人盾);長槍倒掛從精靈側修 elven_spear 280→200(抬玻璃撞鍛造反套利 Σ原料 220 → 改降精靈);daedric_war_axe wt 16·daedric_bow wt 12(魔族=同線最重);crusaders_ward 更名「十字軍聖壁」(與 R108 crusader_shield「十字軍聖盾」區隔·id 不動)。run_all **122**(+test_equipment_curves_r142)·sim byte-identical。🔴 材質階梯不反轉;抬 value 先查鍛造反套利;顯示名不重複 | content |
| R141 | **現實邏輯審計批次 3:戰鬥公式裝備前提 gate(裸身沒有可反震的厚甲)**(群 5 六條方向性違例)。盾反 gate 持盾·重甲反震/重壓 gate `wears_heavy_armor`·**輕甲系 perk 穿重甲不生效**(新 `mastery._chosen_options_with_tree`+`_armor_tree_active`:light 樹=無重甲件·heavy 樹=有重甲·block 樹=持盾·其餘恆 True,套 evasion_bonus/on_evade/passive_armor_bonus·魔法系來源不受影響)·力竭也罰防端(`fatigue<=0 → evasion×0.5`)·裸身不練護甲 XP(`dominant_weight_class` gate)·重盾盾擊耗體以盾自身 `speed 0.85` 計(armor.json 新欄·非休眠武器)。run_all **121**(+test_formula_gates_r141 5 測·test_mastery 4 fixture 補穿甲)·sim_assassin BYTE-IDENTICAL·canonical 零漂移(w1H 釘值噪聲內·assassin/monk=HEAD 基線逐位一致·力竭防端 canonical inert)。🔴 護甲/持盾系 perk 一律走 `_armor_tree_active`;力竭防端動之必跑 sim;盾擊速度住 armor.json speed | re-sim |
| R140 | **現實邏輯審計批次 2:心智控場豁免(嚇不動白骨與傀儡)**(使用者確認範圍:群 3+5+6+7+8 全修·**群 4 遠程旗標不動**)。bestiary `mindless:true` ×15(白骨/殭屍/傀儡/機關/水晶騎士/鬼火/元素·**draugr/巫妖/幽靈保留心智**);`magic._is_mindless`(鏡像 _is_undead·含復生 `_undead` 暫態);**`apply_control` 心智閘**:fear/calm 對 mindless 回 resisted(**rng 前 → 非 mindless byte-identical**)·新 `divine=True`=聖光驅散唯一旁路(turn_undead 帶入·神聖斥退非心智作用);paralyze/stagger/slow/weaken 物理性控場不受影響;`powers._control_class_ok` beast=活體野獸(排 undead/mindless·馭獸/林語修正);gen_bestiary【無心智】tag。run_all **120**(+test_mindless_control 5 測)·sim_assassin BYTE-IDENTICAL·sim_party 無殭局·零新存檔欄。知情代價:恐懼 build 對 15 隻失心控軸。🔴 fear/calm 永不作用 mindless(單點閘·divine 唯一旁路);白骨/傀儡/元素類新怪必標 mindless;馭獸=活體 | re-sim, content |
| R139 | **現實邏輯審計批次 1:毒/元素抗性 rubric(無血肉之軀不可被下毒)**(鐵律零號入憲後 16-agent 審計 78→67 confirmed·使用者拍板三批修〔群7/8 不動〕;批次 1=22 隻抗性斷層·近純資料)。**不死毒抗**:dro_mathra_shade -45→100(曾以毒為身卻弱毒·**test_detailing 生態弱性軸永久豁免 undead**)·time_forsaken/ancestral_ghost/grief_shade/twilight_sentinel/deathless_king→100·reanimated_thrall {}→{frost50,poison100}·vampire_patriarch 補 fire:-25(TES 吸血鬼弱火)+frost50;**構造/元素毒免疫**:gargoyle/knight_of_order/atronach 全家族 poison100·**summoned_atronach「火焰元素」fire 0→100**·summoned_frost frost 75→100(自系免疫);**魔族** dremora 系 poison75(對齊 daedroth);**元素直覺**:frost_troll fire-50(TES 招牌)·frost_giant -25·dwarven 機關 frost -25→+25(抗霜原典)·sugar_vine/blighted_matron fire-50·dark_moon_senche frost 60→25;**玩家吸血鬼** vampirism `poison=15×stg`(滿階 60)。run_all 119(+test_monster_poison_resist_rubric_r139 掃全 undead≥50)·**sim_assassin BYTE-IDENTICAL**(sim 零毒投入)·**sim_party 全牆守**(召喚師/死靈 vs 達貢 0%·召喚模板抗性法定檢查)·BESTIARY 重生。🔴 加不死/構造/元素怪必配毒抗(guard 自動把關);弱性軸永久豁免 undead;動召喚模板抗性→sim_party | re-sim, content |
| R138 | **重盾「掩體」:元素卸力係數 + 掩體回氣(重盾防禦端補償·遊戲內可見)+ R138b 元素次序鐵令**(承重盾首次實測〔被 w1H 嚴格支配:gauntlet 0%·火法王 2%·根因=護甲飽和/元素旁路/防禦不產資源〕+ 無限姿態質疑〔回體6≥攻擊成本4.2=無限全效·數學證實〕;使用者定案:回氣+2/回〔<成本→邊打邊回不可能〕+ 元素卸力 + 遊戲內可見;**R138b 鐵令「現實邏輯優先——格擋不可能防元素比物理多」推翻初版獨立 0.70×block 車道**〔元素卸70%>物理38% 字面反轉〕)。**機制(僅 姿態+持重盾;非重盾 byte-identical)**:①元素卸力=姿態卸力×`GREAT_SHIELD_ELEMENTAL_FACTOR 0.70`(一般盾 ×0.5·block100→物理38%/元素~26.6%·**元素恆≤物理** test 鎖·`_guard_stance_factor` 加 gamedata 參數 None=back-compat)②回氣走**`combat.bunker_fatigue_regen` 單一決策點**(main 回合末+sim_builds._regen 共用)+2/回·**🔴 必<攻擊成本**(test 鎖)·刻意不 gate fatigue>0(力竭喘息=脫困)③可見性:選單直標「重盾掩體（卸力-X%·元素-Y%·回氣+2)」動態·立架訊息專版·HUD 武器行·codex 節。**平衡(canonical `great_shield` build 入駐 sim_builds·魔族重盾+荊棘5件+姿態常駐)**:物理王 99-100%·**元素消耗戰知情回落**(初版車道 gauntlet/火法王 100%·真身 44% → R138b 係數制 **0%/36%/0%**·使用者兩見數據拍板=現實邏輯>平衡身份)·720 0%(包絡內)·非重盾零位移·sim_assassin BYTE-IDENTICAL(隔離 worktree ×2)。run_all 119(test_guard_stance +2)。🔴 掩體兩件套勿放寬到一般盾(w1H+回血4 探針 720 8→56.5% 炸包絡);**元素卸力永遠≤物理**(鐵令·勿回獨立車道);回氣必<攻擊成本;動 0.70/2→sim_builds+sim_assassin;⚠ 龜殼施法者(重盾+姿態施法無稅·初版 arcanist 真身 76→100)R138b 關不死→正解=姿態鎖施法/戰技(評估已交付待拍板) | re-sim |
| R137 | **w1H/2H 分流:「格擋姿態」取代整回合格擋動作(續戰特化 vs 爆發特化)**(承分流評估〔w1H 被 w2H 全維度壓制 gauntlet 20/84·block 樹 4 死里程碑=行動經濟死·盾牆 solo 7%/手動格擋 15% 皆負資產實測〕;使用者三拍板:姿態化·w1H 續戰>2H 傷害<2H·**姿態不得減命中**〔荊棘流預期常駐·敵須打得中〕)。**機制**:選單「格擋」→「格擋姿態」切換(立/收耗回合·gate 同舊);姿態中(fatigue>0 閘**整組**):①攻擊稅 ×`GUARD_STANCE_DAMAGE_TAX=0.75`(main `_df()` 5 攻擊點·damage_factor 車道→solo 夾不變)②舉盾卸力 `_guard_stance_factor`(`GUARD_STANCE_MITIGATION=0.38`×block/100·**物理全額·元素折半**·聖化車道位置·+斯丹達爾之佑 0.10 遷入〔原掛手動格擋成孤兒〕·總夾 0.60)③**反擊樹復活**:姿態中被**物理**命中即觸發 block_riposte(元素不觸發=鏡像 R42 荊棘·**反擊傷必套 `resist_multiplier(physical)`**〔riposte+on_evade 齊套;審查 MAJOR:未套時反擊 chip 繞 R127/R133 pen-免疫牆——全觸發版 true 5→67%、物理限定版 720 仍 12% 幾乎全為反擊〕)+block XP。**無回合上繳**(上繳實證力竭螺旋+重立死迴圈→sim 0%;成本=稅)。**調參史全 sim 釘**:盾接機率版遭使用者駁回(減命中斷荊棘糧)→純物理減傷(元素關卡純稅 44%)→全傷卸力(終王越界 21%)→M0.35 反擊未套抗(720 12% 超包絡)→**最終=反擊套物抗+M0.38**:w1H gauntlet **86.8%**(n=500·>w2H 84·基線 20)·true 4→10.8·**720 達貢 0→8.0%(n=1000·R134 包絡 0-8 邊界·越界由套抗解非降 M)**。shield_reflect 設計相容常駐(不減命中)但實測姿態 17%<35 基線→canonical 不常駐(`_STANCE_BUILDS={"warrior_1H"}`)玩家可自選。run_all **119**(test_guard_stance 5 測)·sim_assassin BYTE-IDENTICAL(反擊套抗對既有怪 phys=0→×1.0 恆等)·矩陣只 w1H 行動零漂移。🔴 姿態三件套 fatigue>0 閘整組;勿加上繳(死迴圈)勿碰命中(鐵令);**反擊套物抗勿拆**(拆=繞 pen-免疫牆);斯丹達爾住 _guard_stance_factor;動 TAX/M/折半→sim_builds(gauntlet+終王包絡 0-8)+sim_assassin;defender_blocking 參數保留但遊戲內無來源 | re-sim |
| R125 | **UI/UX 打磨批次:導航/自動趕路 + 戰鬥可讀性 + 新手清晰(Theme 1+3+4)**(承使用者評估「UI/UX 改善」;5 維 audit workflow〔27 發現→6 主題·全 code-verified〕揪出 R113-R114 四包後剩餘缺口·使用者拍板做零平衡三主題打磨批)。**零 combat/formulas/magic 數學→sim 天然不受影響·零新存檔欄·前端 index.html 未動**。**Theme 1 導航(main.py+quests.py)**:長途趕路過去到不了任務目標(引擎 route_to BFS 早支援·純被 UI filter 擋)→ `_action_long_travel` broaden = 已訪城/鎮 ∪ 任務目標地(含地城/未訪·`ui._quest_target_location_set`·◎ 排最前)+ 新 `_action_goal_travel` +「🎯 前往任務目標」捷徑 + 旅行選單標 ◎;`objective_text` clear/purge/kill 補「(在<省>)」(`_dungeon_where`/hunt_location)。**Theme 3 戰鬥可讀(console.py 純顯示)**:`_STATUS_TAG` 8→27 kind 全中文化(緩/凍麻/導電/蝕抗/失衡/烙印/聖化/戰旗/疾避/嘲諷…)+`_BUFF_KINDS` 增益綠+`_HIDDEN_TAG_KINDS`(cd/記帳隱藏)+`_STANCE_MAINTAINED=99`(常駐不顯回合)·`_status_tags`/`_status_tags_list` 雙修**保 {s,good} 形狀前端零改**;`_st()` rider 補 stagger/weaken/slow/benumb/calm/consecration(不再空泛「狀態」)。**Theme 4 新手(main.py 純字串)**:`action_shrine` 門檻提示三態(在肩上/待等級X/已了結·審查修進行中誤報)+`_create_custom_class` 印專精/偏好效果+開局指南入口提示。run_all 114+smoke 綠·對抗審查 2 維:0 blocker/major·2 confirmed(神殿進行中誤報 + ride_evasion/taunt 漏標)已修。🔴 導航走 `_quest_target_location_set`+broaden long-travel(route_to 只經 is_visible);戰鬥標籤保 {s,good} 形狀·turns≥99 不顯數;神殿區隔進行中/完成。延後:map 點擊旅行/聚焦你在這/服務目錄/創角 chip·**戰鬥用藥(需拍板+sim)**·**手機無障礙(觸控地圖/aria)** | save-safe |
| R124 | **試煉指引:門檻觸發提示 + NPC 流言指向 + codex 索引(三層·純衍生)**(承使用者評估「流言系統對試煉任務〔九神/魔神/終極技能〕的揭露或指引作用」;揪出流言〔R81 offered_rumor〕只指向傳說地城〔rumor_quest〕**從不指向 33+ 試煉**〔戴德拉16/九神10/秘術5/聖光1/聖物2〕→ 試煉全靠「走到對地點+達閘」被動觸發·玩家常「夠格卻不知」;既有 arcane_trials_rumor 事件 + codex shrine/divine 索引沒推廣到全體。使用者拍板綜合三層)。**新 `systems/trials.py`(純衍生·零存檔欄)**:FAMILIES 登錄(daedric→shrine·divine→divine·arcane→arcane_site·holy→holy_site)·`eligible_families` 複用 `available_quests`(已濾所有閘)+ **要求有 site 地點**(`location_for_trial` 配 world tag·排除無 site 的九神朝聖 → 神性提示在 god-trial〔L15〕才觸發)。**①門檻觸發**(game_loop 鏡像 R55 achievements):`seed_hinted`(載入已夠格→種入不重報)+`update`(新夠格族→一次性「◈ 傳聞」指路·session 暫態 `_trial_hinted` 去重·每族至多一報)。**②NPC 流言**:`offered_rumor` 加 `rumor_trial`(quest/landmark 後·NPC 指向試煉地點·一次性走既有 `dialogue_done`+`_TRIAL_HEARD`)·main rumor 兌現印 `site_pointer`·6 主題 NPC(法師/祭司)加 rumor_trial+rumor_disposition30+teaser。**③codex**:`_trial_index_rows`+`dynamic:"trials"`(掛 magic_schools)→終極真言試煉一覽(秘術+聖光·戴德拉/九神已有各自索引)。**🔴 零 combat/formulas/magic → sim 天然不受影響·零新存檔欄**(session 暫態 + 既有 dialogue_done·舊檔安全)·run_all 114(新 test_trials + schema 守 rumor_trial 合法)。對抗審查 2 維。🔴 加試煉族純改 `trials.FAMILIES`;門檻提示走 session 暫態(每族一報);NPC 指路純改 npcs.json rumor_trial(合法試煉地點·test 守);codex 走 `dynamic:"trials"`。前瞻:relic 聖物(knights 大廳 gate·需 pilgrim_vision 事件)未納門檻提示·可另補 | save-safe |
| R123 | **恢復系反死靈改制:治療傷害不死(取代 R122 聖光攻擊線)**(承 R122 收官後使用者評估「不做聖光攻擊·改治療對不死產生傷害」;評估揪出這是**最正典**〔治療=生命能量·療活物焚亡者〕且**更優雅**〔收斂學派·現有 7 治療自動成反死靈軍火庫·零冗餘攻擊法術〕·平衡數學已對上〔治療值 ×0.5 ≈ 原聖光線〕;使用者拍板改制·**復用 R122 全部基建**〔_is_undead/17 undead/turn_undead/consecration/試煉/里程碑〕→ 只換傷害交付方式)。**機制(magic.py+formulas.py+main.py·未碰 combat.py)**:**治療傷害不死(smite)**——`minor_heal`/`heal`/`close_wounds` 加 `smite_undead:true`;magic.cast heal 分支頂 `elif kind=="heal" and sp.get("smite_undead") and target is not None and _is_undead(target)` → 造傷 = mag×威力×`HEAL_SMITE_FACTOR`(0.5·element magic 吃 magic 抗)·**否則走原自療分支不變**(overheal_ward/restoration_boon/瑪拉全保)·🔴 **只對不死·對活物治療零傷害 → 恢復系對活人零遠程輸出**(靠近戰)。**破曉之光 `dawn_judgment`(id 留·名/kind 改)**=新 kind `radiant`(heal55/mag38/cost66/target all·治 char+盟友 + 灼燒全體不死·活物免傷·enemies/battle 可 None)。**targeting(main.py)**:施 smite 治療且場上有不死→選單「🩹治己/☀️灼燒不死」;**↻ 再施 保留 smite 目標**(審查修:原退化自療)。**移除**:holy_bolt/sun_flare + 兩 holy 傷害乘子(turn_undead 的 holy **控場**閘保留)+ `HOLY_UNDEAD_MULT`(→`HEAL_SMITE_FACTOR`)·world spell_stock 清·console summary 加 radiant·schema `_KINDS+=radiant`。**🔴 sim_assassin BYTE-IDENTICAL**(隔離 HEAD worktree 空 diff·**未碰 combat.py**·formulas 只換刺客未用常數·刺客不施法)。**🔴 sim_builds make_paladin(smite/radiant)**:**不死 boss 100%**(deathless_king/vampire_lord/malyn_varen·showcase)·**非不死 boss 0%**(dagon/古龍——恢復系對活人零魔攻·**達貢牆最硬**·純施法 fixture 無近戰)。run_all 113(test_paladin 改寫 smite/radiant/自療/活物免疫/repeat-smite)。零新存檔欄(smite_undead=spell 資料·radiant=kind)。codex/BUFFS 更新·trial 文字 破曉聖裁→破曉之光。對抗審查 4 維 8 agent:0 blocker/major·2 confirmed(repeat-smite 退化自療 + docs 未寫)已修。**🔴 設計取捨(使用者知情拍板)**:恢復系從此**對活人零魔法遠程輸出**(更正典專精·打活人靠近戰)·達貢牆因此最硬。🔴 加反死靈傷害走 smite(治療 smite_undead·只傷不死·factor 0.5)或 radiant;**治療永不傷活物**(_is_undead 閘勿放開);動 `HEAL_SMITE_FACTOR`/radiant→sim_builds(不 trivialize 不死·守達貢牆)+sim_assassin(byte-identical·未碰 combat);turn_undead holy 控場閘/consecration 皆 R122 不變 | re-sim, save-safe |
| R122 | **恢復系功能化:聖騎士(反死靈聖光 + 守護庇佑)完整弧 — Phase A:聖光反死靈核心**〔⚠ 聖光**攻擊線**(holy_bolt/sun_flare/破曉聖裁·HOLY_UNDEAD_MULT)已由 **R123 改制為「治療傷害不死」**取代;守護/試煉/里程碑/17 undead 仍現行〕(冷系統評估揪出 restoration 是六大魔法學派**唯一無戰鬥身份者**〔8 法術全 heal/regen/cure·里程碑一維「治得更好」·連 ward 都在秘術〕;使用者三度拍板:系統=恢復系·身份=**聖騎士混合**〔反死靈聖光攻擊 + 守護庇佑〕·範圍=**完整弧**〔比照秘術 R119-121 分階段〕)。**Phase A=聖光攻擊身份**(碰 magic.py + formulas.py + 資料)。聖光走 `element:"magic"`(繞火/冰/雷抗·只吃 magic 抗·比照秘術 R119)+ **新 `holy:true` 旗標** → 對不死系 ×`HOLY_UNDEAD_MULT`(formulas·2.0·正典 Sun Damage 雙倍剋不死);**對生者無顯式懲罰**·靠**低基礎 magnitude** 實現「弱泛用 vs 強剋不死」專精(免懲罰分支/邊角)。新 `magic._is_undead(cre,gd)`(讀 bestiary `undead` 旗標 OR R106 復生屍體暫態 `_undead` → 一致)鏡像 `_is_solo`;傷害路徑兩處(單體+AoE)加聖光-剋-不死乘子(gated `eff.get("holy")`);聖光控場(turn_undead fear)只落不死系(damage_status 單體 + status_all AoE 各加 undead 閘·`continue` 為迴圈末安全);turn_undead 訊息三態如實(`repelled`/`undead_present`→驅散成功/不死抵抗/場上無不死·**審查修:原只看 repelled 空→誤報「無不死」·現區隔 solo R44 抵抗〔65%〕與 dedup**)。**3 法術**(spells.json·school restoration·element magic·holy·低 mag):聖光箭 holy_bolt(damage mag12 cost16)·烈陽灼 sun_flare(damage_status mag26 cost34·對不死附 fear repel)·驅散亡者 turn_undead(status_all fear·undead-only·cost30)·佈點 7 恢復系城(reachability 守)。**17 隻 undead 旗標**(bestiary R02):skeleton/draugr/barrow_sentinel/marsh_zombie/lich/imperial_ghost/ancestral_ghost/grief_shade/dro_mathra_shade/reanimated_thrall/vampire_fledgling/vampire_lord/vampire_patriarch/coven_patriarch/molag_bloodlord/time_forsaken/malyn_varen(**排除活人死靈術士 grave_tyrant/meridia_defiler/necromancer_acolyte/xarxes_necromancer·構造 gargoyle·元素 ice_wraith/will_o_wisp·戴德拉召喚 summoned_terror·受詛活人 wish_eaten_sorcerer·活人 carrion_priest/hag_matriarch**·`gen_bestiary.py` 加【不死】tag)。**里程碑不動**(既有 restoration spell_mod / divine_grace +20% 已自動縮放聖光=restoration school)。**🔴 sim_assassin BYTE-IDENTICAL**(隔離 HEAD worktree 空 diff·刺客不施法·holy 分支 gated 於 `eff.get("holy")`→無其他法術帶 holy→非 holy 路徑逐位元組同·未碰 combat.py)。**🔴 sim_builds 新 make_paladin**(restoration 100 純聖光施法·上界 fixture):**達貢牆守 0%**(達貢=戴德拉非不死·magic50→聖光弱且吃 magic 抗)·paladin ≤ arcanist 各處(hard living tail 明顯較弱:古龍 74% vs 89%·umaril 65% vs 88%=「聖光弱剋生者」在 DPS race 顯現)·mid ~95-100%(caster 常態)·不死剋星身份待 **Phase C** 高血不死 boss showcase。零新存檔欄(holy=effect 鍵·undead=bestiary 鍵·皆非 Character 欄)·run_all 112。對抗審查 4 維 8 agent:0 blocker/major·byte/content-accuracy〔17 roster 無偽陽性/漏〕/紅線/schema/reach 全清·1 minor(turn_undead 訊息誤報)已修。🔴 加聖光法術純改 spells.json(element magic·holy:true·低 mag)+world spell_stock;聖光控場只落不死(undead 閘);動 `HOLY_UNDEAD_MULT`/聖光 mag→sim_builds(守達貢牆)+sim_assassin(byte-identical)。**Phase B(碰 combat.py)=守護庇佑 + 里程碑聖騎士身份**:聖化領域 `consecration`(self·apply_status·mag0.20/3t·cost30·**新 combat 減傷機制** `_consecration_factor` gated `_is_player(defender)`·置於物理/元素 if/else 後→物理+元素皆減·與秘術 ward〔magicka 吸收池〕區隔);里程碑 repurpose 2 未鎖節點(50 cleanse→`holy_zeal` spell_mod power0.10 聖光攻擊·75 steadfast→`sacred_bulwark` 新 kind `consecration_boost` 聖化 0.20→0.30 守護頂點·ensure_mastery_choices 退 pending·`regen_on_low` 成孤兒無害);magic.cast 對 consecration status 加 `consecration_bonus` 幅度 + **刷新非疊加**(重施移除舊光環)。sim_builds make_paladin(+consecration·holy_zeal/sacred_bulwark/divine_grace·`_paladin_act`):**達貢牆守 0%**(魔力耗盡+聖光弱)·hard living tail 反降(古龍 30%·umaril 36%:守護分心少攻)·不死 100%·無 boss 勝率升。**🔴 sim_assassin BYTE-IDENTICAL**(三處皆 gated:_consecration_factor/magnitude bump/accessor 皆只 consecration+玩家)。**順帶修 sim_builds._regen 漏 Altmer ×1.5 戰鬥回魔**(對齊 main.py:1214·修 5 個 Altmer fixture·揭露 arcanist dagon 2→10%〔仍牆〕·paladin 恆 0%)。對抗審查 4 維 6 agent:0 blocker/major·2 confirmed(sim regen fidelity minor + consecration 刷新誤報 nit)已修。run_all 113(新 test_paladin·test_mastery steadfast→sacred_bulwark·test_spell_schema +consecration)。🔴 守護減傷走 consecration status + `_consecration_factor`(gated _is_player 勿去 gate)·聖化上限 0.20+0.10=0.30·動→sim_builds(守達貢牆)+sim_assassin(byte-identical)。**Phase C(平行「聖光試煉」site·使用者拍板·零 combat/formulas/magic 碼改)=終極聖光 + 破曉試煉**:終極 `dawn_judgment` 破曉聖裁(restoration·damage·element magic·holy·mag42·cost66·複用 Phase A ×undead·**唯 reward.spells**);boss `deathless_king` 不朽之王(**undead:true**·solo·HP300·resist magic30/frost45·R43 曲目·控場≤0.30/1);dungeon `deathless_crypt` 不朽陵墓(不死怪池·聖光/驅散 playground);world node(高岩·pos[1,6]·雙向 links·R28)+ daggerfall `holy_trials:"dawn"`;quest `trial_dawn`(source holy·requires restoration75+L18·reach 威岩→collect moonstone×2→clear→reward dawn_judgment);main.py `action_holy_trials`(鏡像 action_arcane_trials·法師公會子選單「☀️破曉試煉」)。sim_builds:deathless_king paladin 100%/mage_frost 88%(差異化)·**達貢牆 paladin 0%**(聖光對非不死終王無用)。sim_assassin BYTE-IDENTICAL(零碼改)。run_all 113(test_paladin +4)。codex/BUFFS/BESTIARY 148 同步。對抗審查 4 維:0 blocker/major·2 nit(威岩名·no-avail 訊息)已修。🔴 加聖光終極純 reward.spells·boss undead:true+magic 抗勿過高·world node 手填 pos/links(R28)·action_holy_trials 鏡像 arcane_trials。**🎉 六大魔法學派全有戰鬥身份·恢復系聖騎士完整弧收官** | re-sim, save-safe |
| R121 | **秘術終極試煉:非戰鬥實用法術 + 念力球解謎 + 湮識(魔法核彈 + 加深秘蝕)**(承 R119/R120·使用者多輪拍板;三 phase 分提交 A→B→C)。**Phase A**:靈視術 `scry`(低階·單格)+ 靈識術 `arcane_sight`(中階·中心+四鄰)= 地城對**任一遠格**主動揭露(`_dungeon_scry`「🔮 靈識揭露」動作·cell picker·耗魔·鍊 mysticism·與偵查 25 recon 互補:recon 揭近身·scry 揭遠格→**解鎖 recon 後仍有用**)· 念力術 `telekinesis`(限時 spellfx·隔空化解陷阱)· 走 R104 spellfx/action_cast_self·scry 排除出**所有**cast 選單。**Phase B**:dungeons.json opt-in `orbs:true`→每層念力球·念力術破之(`_dungeon_orb_break`)· 未破離層/決戰→`_apply_orb_curse`:隨機大減一項**非秘術**技能(−`_ORB_CURSE=40`·夾≥0·優先有點數·不重複)· 反噬走暫態 `char._dungeon_curse` 負層(`skill()` 讀·**不入檔**·唯一呼叫端清+進場重置·防滲出)· **resolution-gate 不切連通圖**(守 test_generate_all_valid)· 零新存檔欄。**Phase C**:終極 `arcane_annihilation`「湮識」(damage·magic·mag42·cost66·`deepen_erosion`)命中→`target._deep_erosion`→秘蝕上限 5→10(`EROSION_DEEP_MAX_STACKS`·−15→−30·本場單敵·暫態旗標)·唯 reward.spells(不入商店)· boss `arch_mystic`(**resist magic 75**·solo·raw·HP215·純魔法徒勞→以元素/鋼鐵勝之)· 地城 `soul_sanctum`(ashland·`orbs:true`·raw boss·無 reinfest)· node(晨風 pos[39,11]·雙向 sadrith_mora+ashland_waste·R28)· sadrith_mora 掛 `arcane_trials:"soul"`· 任務 `trial_soul`(source arcane·mysticism 75·reach→collect glow_dust→clear→reward 湮識)· action_arcane_trials fallback 加 mysticism 訊息。**🔴 sim_builds 守達貢牆**:arcanist(含湮識+深蝕)**1% vs mehrunes_dagon**·終極獎勵有感(古龍 77→89%·umaril 70→90%·fused 97→99%)·不 trivialize。**🔴 sim_assassin BYTE-IDENTICAL**(刺客不施法·無 `_deep_erosion`·erosion gated·curse None→skill()+0)。零新存檔欄(湮識=reward·秘蝕上限/反噬/地城 explored 皆暫態)·run_all 112(新 test_orb_curse·test_utility_magic/test_arcane_trials 擴)。對抗審查 4 維 5 agent:1 confirmed minor(scry 漏排除戰鬥 cast→誤選退還但耗回合)已修·餘 balance/紅線/byte/save/topology/reachability 清。🔴 加秘術實用法術走 spellfx(限時)/scry(地城揭露·排除**所有**cast 選單);念力球純 `orbs` 資料+暫態反噬(不入檔·唯一呼叫端清);湮識深蝕走 `_deep_erosion`(per-spawn 暫態·`erosion_max_stacks` 讀);終極只 reward.spells;boss resist magic≥70;動 `EROSION_DEEP`/湮識/boss→sim_builds(守達貢牆)+sim_assassin(byte-identical)。**秘術線 R119-R121 告一段落** | re-sim |
| R120 | **秘術頂點功能化:秘蝕(arcane erosion)—— 破抗輔助頂點(削魔抗·輔助所有傷害魔法)**(承 R119·使用者評估「靈光護壁改秘蝕」多輪拍板:①降敵魔抗非增傷·②輔助所有傷害魔法〔火/冰/雷 MAGIC_ELEMENTS 亦吃通用 magic 抗〕·③純攻頂點〔移除 +15 護甲〕·④磁量 3/層夾5=−15 拍板)。myst_100 冷 `spectral_aegis`(passive_armor15·敘述-程式不符)→`arcane_erosion`(布林解鎖·純攻)vs soul_siphon 真二選一;持頂點者**任何**傷害法術命中削 magic 抗→輔助整個傷害魔法軍火庫(戰法師/多學派)。**機制(R75 conduct 模板·削抗非增傷)**:新 status `erosion`(暫態不入檔 R03)+`EROSION_RESIST_PER_STACK=3`/`MAX_STACKS=5`/`TURNS=3`+helper `erosion_stacks`/`erosion_resist_reduction`/`add_erosion`(鏡像 conduct);cast 兩傷害分支持頂點者(`mastery.has_arcane_erosion`)以 `{**resist,"magic":max(0,magic−3×層)}` **複製** resist(🔴不 mutate boss·proof 證 dagon 施法後 resist 仍 50)計傷+add_erosion(任何傷害元素·**floored≥0** 只蝕既有抗不製造弱點);新 kind 三步(`_IMPLEMENTED_KINDS`+`has_arcane_erosion` 布林 getter 鏡像 has_dungeon_casing·R35 安全短路)+mastery.json 節點;順帶修 AoE add_conduct→shock-gated(對齊單體·R75 latent 不一致·byte-identical)。**🔴 sim_builds 守達貢牆**:arcanist+秘蝕火/冰/雷法師 vs 達貢**全 0%**(fire85 恆牆·−15 削抗絕對傷害仍小·脆皮先死);不解元素大法師牆(fire80 恆牆);輔助幅度小=弱於毀滅(古龍 shock 90→92%·arcanist fused 95→97%/umaril 62→70%/古龍 73→77%)。**🔴 sim_assassin BYTE-IDENTICAL**(刺客不施法·has_arcane_erosion 短路·非頂點 eroding False→resist 不動→mult 逐位元組同)。零新存檔欄·run_all 111(test_m8+秘蝕·test_mastery 2 斷言去 spectral_aegis)。對抗審查 4 維 7 agent:0 correctness/balance/紅線/byte/save 缺陷·2 文字陳舊(節點 desc 磁量·BUFFS/build.md 靈光護壁)已修·AoE conduct-gate 疑慮駁回。🔴 加秘術破抗走 arcane_erosion(布林·削 magic 抗·floored≥0·resist 必 `{**resist}` 複製**不 mutate**);輔助所有傷害魔法靠 magic-resist 共用軸(火/冰/雷亦吃);純攻頂點無殘留防禦;動 `EROSION_*`→sim_builds(達貢全 0%)+sim_assassin(byte-identical)。**延後**:秘術終極(R78 式試煉) | re-sim |
| R119 | **秘術功能化:非元素魔法傷害法術線(consistency 破抗身份)+ 里程碑支撐**(承 R118 法術學派冷→暖·秘術=唯一無戰鬥身份學派〔法術最少 7·里程碑 4/7 冷數值填充·正典招牌靈魂/傳送/探測/吸收/反射未交付·實際 payload 只是「附魔的技能值」〕;使用者三度拍板收斂:①否決反射/秘防→**非元素魔法傷害**·②**純傷害線+里程碑**〔招牌狀態/頂點「之後再改」〕·③**傷害須低於毀滅三系**〔可靠性稅〕)。**身份**:毀滅=配對學派(選對超有效/選錯牆死)·秘術=一致性學派(一律穩定 `magic` 傷害·永不 ×0 也永不 ×2·只吃較稀有/較低的通用 `magic` 抗)=「破抗答案」。**近乎純資料**:傷害分支已預設 `element="magic"`(magic.py:297 單體/476 AoE)·`resist_multiplier`(formulas.py:753)magic∉MAGIC_ELEMENTS→只吃 `resist["magic"]`(不吃單元素抗)·訊息已印「魔法傷害」→ 新傷害法術填 `element:"magic"` 即成(schema `_ELEMENT_KINDS` 要顯式 element·`_ELEMENTS` 已含 magic)。**3 法術**(spells.json·全 element magic·mag 嚴格低於毀滅同階·純傷害無 rider):秘術飛彈 arcane_bolt(damage mag11/cost15<flames14/sparks15)·奧術轟擊 arcane_blast(damage mag24/cost34<fireball/ice_spike30)·秘力星爆 arcane_nova(damage_all mag13/cost42<frost_nova16)。**佈點**(world.json spell_stock·R29):bolt 進全 25 法師城基礎帶(秘術終有隨處基礎攻擊)·blast/nova 進進階帶 imperial_city/senchal/dune(艾爾斯維爾守秘術)。**里程碑**(mastery.json):myst_75 冷 `spectral_ward`(passive_armor15·敘述-程式不符)→`arcane_focus`(spell_mod mysticism power_bonus 0.15·複用既有 kind·零程式)·縮放秘術(較低)傷害·保留對面 arcane_cascade=真取捨·opt_id 改→`ensure_mastery_choices` 退 pending(零存檔欄)·myst_50 ward_focus(power 0.1)現也縮放新傷害(_power 通用·秘術傷害 50+75 兩成長節點·SUM 0.25 對標毀滅)·**myst_100 spectral_aegis 不動**(使用者「之後再改」)。**🔴 sim_builds 新 make_arcanist**:達貢 720 offense 牆守(arcanist **0% vs mehrunes_dagon**·mag 低+只吃 magic50·magnus 杖最壞投資仍 0%)·**利基獨一**(vs fused_archmage〔三元素各70→毀滅全 ×0 牆死·magic35〕arcanist 95%·火/冰/雷大法師〔對己元素 ×0〕98-99%=唯一能傷)·**非 trivialize**(umaril 62%·古龍 73%·knight_of_order 86%·高魔抗 boss 因 mag 低略遜元素法師=可靠性稅可見)。**🔴 sim_assassin BYTE-IDENTICAL**(隔離 HEAD worktree 逐位元組空 diff·未碰 combat/formulas·刺客不施法·`staff_element_flat(magic)=0`·conduct 僅 shock→秘術無 flat/ramp 放大)。零新存檔欄·run_all 111(schema/reachability 自動涵蓋·test_mastery passive_armor 斷言僅引 spectral_aegis〔未動〕→免改)。對抗審查(4 維 fan-out→逐項對抗驗證·7 agent):**0 correctness/balance/紅線/reachability 缺陷**·2 confirmed 皆文字陳舊(ward_focus desc「唯一增幅來源」被 arcane_focus 證偽→改·BUFFS spell_mod 表 mysticism0.10→0.10+0.15)已修。🔴 加秘術傷害純改 spells.json(element:"magic"·mag<毀滅同階·純傷害)+world spell_stock(R29·reachability);里程碑功能化純改 mastery.json(spell_mod 已白名單·opt_id 改走 ensure_mastery_choices);動法術數值→sim_builds(守達貢牆·不 trivialize)+sim_assassin(byte-identical)。**延後**:招牌狀態(秘蝕破抗 ramp/汲取 drain)·myst_100 頂點 spectral_aegis 重設計·秘術終極(R78 式試煉) | re-sim |
| R114C | **經濟 QoL 批次:賣貨/倉庫/連煉/連訓/直達旅店/城區連逛(UI/UX 審計 包C)**(承四包·包C·高頻互動成本小改·純流程零平衡零存檔)。**賣貨/銷贓**預設全部(1→owned)+ 僅一件且 value≥`SELL_CONFIRM_VALUE=150` 才確認(防誤賣精品)。**倉庫** `_stash_transfer` 加 `__all__` 全存/全取(取出以負重閘)。**煉金** `action_alchemy` 抽 `_brew_and_report` + confirm-loop「↻再煉一鍋」。**訓練師**訓完 confirm-loop「↻再訓練+1」(達 cap/錢不足止)。**練習** module global `_last_practice_skill`(session 暫態·不入檔·re-validate)→ 捷徑跳過類別/技能。**丟棄/回爐** value≥`DESTROY_CONFIRM_VALUE=150` 確認。**旅店**抽 `_inn_rest`·廣場加「🛏宿一晚」直達。**🔴 城區連逛**(唯一結構改動):game_loop `_stay_district`——城區服務辦完且 `not died`→下圈直接回該區子選單·返回/該區消失→回 hub;iteration 基數不變(1 dispatch/圈)→ hub-top 更新頻率同 HEAD·`died=="dead"` 照常 end_run。run_all 110(+test_econ_qol·test_m14 _patch_ui stub confirm)·sim byte-identical(純選單流零 rng·零碰 combat)·煙霧驅 main 過連逛。🔴 `_stay_district` 只在 not died 設;`_last_practice_skill` 不入檔;確認門檻純安全閘不碰經濟;刻意不含進食去確認(有被撞見風險);審計餘包 D 長途旅行 + 戰鬥用藥設計題留後續 | |
| R114 | **資訊可見性:限時增益總覽 + HUD 狀態補齊 + 任務追蹤(UI/UX 審計 包B)**(承 R113 四包·做包B·7 條 F1-F7·純讀 view-model/前端·零存檔零 combat)。**F1 限時增益不可查**:新 `console._timed_buffs`(單一資料源·聚合 potion_buffs/spell_effects/well_rested/祝福·帶剩時·祝福自查到期因 blessing_label 不查)+ **修 F1 半 bug** `_blessing_badge` 加 now(原不傳→HUD 永不顯餘N時)。**F2 web HUD≠終端 status_line**:新 `_status_flags`(鏡像 潛伏/獸形/醉/戒斷/染病·排除已 chip 者)+ **接活死碼** diseases.active_labels 進角色卡。落點:`_hud_view` 加 buffs(✨增益×N title)/statuses/encumbered;`sheet_effects` 簽名 +state 經 `_effect_sheet_lines` 全列。**F3 任務目標無常駐**:`quests.tracked_lines`→`_location_view` tracked→地點卡「▶任務」行。**F4** `_shop_view` 加負重(複用背包條)。**F5** `_map_view` node 加 quest(`_quest_target_location_set`)/reinfested(`dungeon.is_reinfested`)→金環◎/紅角⚔。**F6** `housing.garden_ready_notice` 掛 game_loop。**F7** hub「告示板（N）」。run_all 109(+test_infohud)·sim byte-identical(純讀·零碰 combat)·node --check·codex 同步。🔴 增益/狀態走 _timed_buffs/_status_flags 單一資料源;blessing_label 帶 now 且自查到期;HUD buffs 排除祝福;審計餘 C 經濟QoL/D 長途旅行 + 戰鬥用藥設計題留後續 | |
| R113 | **戰鬥/地城操作流:↻ 再次 + 目標記憶 + 方向鍵(UI/UX 審計 包A)**(「評估 UI/UX 便利性」4 維審計 39 發現 0 駁回→四包拍板先做包A=最高頻迴圈)。**戰鬥記憶**:run_battle 場內暫態 cmem{last,target,ally} 不入檔→選單頂「↻ 再次」(僅 attack/cast·cast 需仍 can_cast·死目標重開選單)·目標選單標「↻ 上次」+allow_back(返回=_back() 遞迴保 vanish/charm 場內狀態)·全目標路徑接 None·ally-cast 無同伴 None 照舊。**地城**:離開加 confirm·撬鎖失敗 confirm→選單(再試/連試到開〔靠 no_pick/tired 閘收斂〕/放棄)。**前端**:方向鍵→go:n/s/w/e(curSpec 有鍵才吃)·Esc=返回·智慧數字緩衝(buf×10>總數立即送)·go:* 不被格子接管(保方向標籤+數字鍵)·新敘事幀選單 scrollIntoView(center)(修每幀捲頂但敘事在頁底)。run_all 108(+test_combat_flow)·sim byte-identical(menu 流零 rng)·測試 driver 兩處適配。🔴 cmem 絕不入檔;repeat 只 attack/cast;目標 key=存活索引與 _combat_view 卡片對齊(動一邊必動另一邊);方向鍵只在 spec 含 go:* 攔截;審計餘三包(B 資訊/C 經濟 QoL/D 長途旅行)+「戰鬥用藥」設計題留後續 | re-sim |
| R112 | **故事日誌雙區化:上「本回合」+ 下「順時序歷史」**(使用者問「由下往上更新是否較符合 UX」→ 評估:最新在頂的視線鄰近性真實〔日誌在輸入區下方·現制最新在內捲盒底緣〕但跨回合故事會倒敘;使用者拍板**雙區**)。純前端 index.html:`journalZones()` 雙區 DOM(歷史區首回合退役前 `has-hist` 閘隱藏)·`appendJournal` 退役制(新敘事幀→上區整回合移歷史尾端〔順時序·剝 .jentry 防重播動畫·.jturn 分隔〕→新敘事填上區)·**空回合上區 sticky**·R58 去重雙保險不動·**順帶修 R58 拉回 bug**(錨定守衛:atBottom 才跟捲)。驗證:node --check + DOM mock 功能測試(退役/sticky/去重/上限/錨定)+ run_all 107 + codex 同步。🔴 改日誌呈現動 render/appendJournal/journalZones/#log CSS(.jlatest/.j-hist/has-hist);狀態走 view·敘事走 log 不變 | |
| R111 | **地城再滋擾:重踞變體 + 清剿委託(地城重返迴圈)**(使用者選「地城重返獎勵迴圈」·兩拍板:機制=混合〔週期再滋擾+委託〕·頭獎=變體自帶小寶藏。盤點:回訪管線已存在〔進場全新格局/怪重生/R36 寶箱重刷〕,死的是「回去的理由」與「頭獎」〔boss 寶藏 first_clear 單行閘〕)。**🔴 第一鐵律:`cleared_dungeons` 恆單調不減**(主線門鏈 after/until_cleared·98 條 clear_dungeon 任務·成就/legacy/worldstate 全依賴)→ 重踞=平行衍生狀態:1 新欄 `dungeon_reinfest_at`(dungeon_id→絕對小時·R110 garden 模式)·`dungeon.is_reinfested`=已清+有變體+到期+**無軍營駐守**·`mark_purged` 嚴格推進 now+`REINFEST_CYCLE_DAYS=15`×24·重踞持續到掃蕩(不自行消退)。**變體純資料**:dungeons.json `reinfest` 區塊(desc/rumor/monsters/boss/loot 覆寫;地城怪=spec 平抽→`effective_spec` 疊上即主題翻轉);**27 泛用地城參與**·主線門鏈+親王/試煉/聖物**絕緣**(guard:main/daedric/arcane/relic 的 clear_dungeon 目標不得帶 reinfest)·變體 boss≠原 boss(guard)·參與地城原 boss 全通用怪(kill 任務最壞延遲非死鎖)。**頭獎三分流**(main.py):first_clear 原大寶藏/infested 變體小寶藏(d2 [25,50]+藥水…d5 [80,130]+藥水+紅寶石·絕無神器)+mark_purged/其餘空;首通亦排程。**清剿委託**:新 objective `purge_dungeon`(R21 三步)·達標=`戳>base`·base=接取時快照(`_kill_base`→`_objective_base` 泛化)·零時間存取零新 quest 狀態(嚴格遞增保證);**天然閘取代脈動聚光**(`available_quests` +`now` 參數·board repeatable+purge→只在重踞時現身·掃蕩即消失)→ 不觸 R79 破口;27 條 `dcomm_*`(gold 40-105 按危險度+fame 1-2·邊境掛相鄰真省)。**軍營咬合**:`can_make_camp` +`now`(None=向後相容)·重踞中不可紮營(防「接委託→紮營→駐軍豁免免戰完成」)·已紮營永不滋擾。傳聞:`reinfest_notices`(本省·session seen 去重·每輪一報)掛 game_loop;codex 補「重踞與清剿」修過時句。存檔:ensure(型別矯正含 **Infinity→OverflowError**·未來戳上夾·**舊檔回填 now+一輪**·未知 id inert)。**🔴 sim byte-identical**(隔離 worktree·未碰 combat/formulas)·run_all 107(+test_reinfest)·schema guard。🔴 加重踞地城=reinfest 區塊+對應 dcomm;特殊地城永不參與;cleared 絕不移除;調 CYCLE/寶藏/賞金→先問;purge 判定依賴戳嚴格遞增 | re-sim, save |
| R110 | **家園基地化:房產 tier + 擴建(藥草園/臥房/商誼)+ legacy/成就承認**(三路審計收斂最冷系統=房產〔14 棟同質空殼·購置零承認〕;三視角設計+對抗審查先行;使用者四拍板:**鍛爐砍掉**〔14 房產城全有 armorer·煉金/附魔不限地=死值〕·**管家駐紮延後**〔陷阱選項+4 MAJOR 生命週期破口〕·**tier+商誼第三升級**·**冷卻 24h**)。**tier/格位**:houses.json `tier` 1-3(3500/4000-5000/8000)·`SLOTS_BY_TIER={1:2,2:3,3:4}`(T1 三選二·莊園全裝·T3 空位=目錄擴充預留)。**擴建目錄** house_upgrades.json(**鏡像 R106C necromancy 買斷模式**·嚴格金幣沉永不退款):garden 1200/bedroom 800/trade_pact 1500。**藥草園**:池=房產城 biome→ecology(14 城 1:1 已驗)·`forage_pool_draw` 加嚴格 no-op 參數 `budget/value_cap`(預設=野採逐位元組同·🔴空候選 early-return 恆在 rng 消耗前)·固定預算 6 不吃 scout(守 R93/R94 偵查 niche)·**value cap 10 承重界**(排除 moon_sugar/nirnroot/rare=殺印鈔機·R88 式全池精確最大 ≤40 不變量測試鎖死)·24h 無 banking·購入即冷卻·**零 XP**(專測鎖)。**臥房**:`set_well_rested(char,now,hours=None)` 預設參數(巢穴/藏身處 byte-identical)·36h 只時長不動 `WELL_RESTED_XP_MULT`。**商誼**:`province_discount`(本省有已購 trade_pact 自有房 →0.05·省內布林·無房 ×1.0 恆等)乘進 buy_price **反套利地板前**(頂配疊 0.586×value<賣 1.12×value→地板咬住·全品項掃描測試)·只買價。**承認**:legacy +60/房+20/設施+「家業」label(console 兩渲染點)·成就 `houses_count`(安家立業 1/廣廈四海 4)·codex「家園與置產」。**存檔**:2 新欄 house_upgrades/house_garden_at(dataclass 預設+to_dict)+`ensure_housing_fields`(冪等·型別矯正+時間戳上夾·**未知 id 保留不刪**=inert 不佔格位·比照 stash 寬容)。**🔴 sim byte-identical**(隔離 worktree·未碰 combat/formulas)·run_all 106(+test_homestead)·schema guard(tier 合法+biome 有池)。🔴 加房產純改 houses.json(tier+biome 有池);加擴建=目錄+housing 掛點;動 GARDEN_* 旋鈕→先問+全池不變量續綠;商誼恆地板前;`forage_pool_draw` 參數預設 None 不可動。前瞻:管家駐紮(非經濟價值)·鍛爐(無鍛造城房產)·更多設施 | save |
| R109 | **劇情敵 NPC 化:野外狩獵 + 城內暗殺 + 謀殺目擊制(鐵律廢大廳出擊)**(承 R108 審查揪出 23 條反間/地下/臥底任務因 rogue_thief/blade_agent 無生成路徑死鎖;使用者選「劇情敵設計為一般 NPC 投入世界」多輪拍板·分階段)。**兩層目標**:城內置放具名 NPC 暗殺(assassinate objective·走 action_murder「暗殺此人」·DB全+神話黎明全 humanoid + 一次性反間/臥底)/ 野外·地點狩獵(kill objective·任務條件式生成鉤子·可重複群獵走 provinces·唯一 villain 走精確 hunt_location)。**引擎**:assassinate(npcs 多目標·達標=全在 murdered_npcs);`quests.active_hunt_target`(active kill·weight-0 劇情敵·hunt_location==當前地 或 provinces∋當前省→高機率 HUNT_ENCOUNTER_CHANCE 0.6 生成)接 world.travel/action_explore(無定位→None→byte-identical);謀殺目擊制 crime.murder_witness_chance(城守密度 城>鎮>野×潛行×夜·夾限)+ record_murder(witnessed=)。**道德分軸**:授權暗殺(反間·sanctioned:true)無罰 / 謀殺(DB/神話黎明/地下/自由)目擊制。**聞訊備戰**:多目標殺一逾1日·存活同夥用 *_alerted 強化模板(quest state hit_day·零存檔欄)。**🔴 鐵律**:`action_contract`(大廳出擊生成)**整個移除**·合約目標一律在世界裡·大廳只 `_contract_hint` 指路;knights kn1-6 改 hunt_location 野外狩獵。**內容**:28 置放 NPC(combat_template=原合約怪·部分 alerted)+2 alerted bestiary·kn/db3/md3 hunt_location·移除死 clean_bonus·改寫簡報。**R100 臥底結局因此有出口**。**對抗審查 5 confirmed 全處置**:預先謀殺免費完成→**使用者拍板允許+補敘事**(不發屍體合約會軟鎖階梯·獎勵不膨脹)/clean_bonus 移除/簡報改寫/guard 補 location 檢查(weight-0 須 hunt_location|provinces 防 soft-lock)。**🔴 sim byte-identical**(未碰 combat/formulas·刺客不接社交合約/不謀殺·狩獵鉤子對空回 None)·run_all 105·BESTIARY 137·零新存檔欄(hit_day 存 quest state)。🔴 加暗殺目標=置放具名 NPC(location 合法+combat_template)+任務 assassinate npcs;加野外狩獵目標=weight-0+任務 hunt_location(唯一 villain 精確)或 provinces(群獵整省)·無定位=不可達(guard 攔);授權除敵 sanctioned:true·其餘謀殺目擊制;預先謀殺算數勿加接取基準(否則軟鎖);**公會合約絕不在大廳生成目標**。**潛伏揭發層 B2(✅已做)**:違法身分目標 hit_covert 潛伏(20 個·叛離者潛伏/內圈領袖公開)·接合約(is_active_hit_target)或探查(hit_investigated·scout/口才 vs hit_secrecy)才識破→揭發(處決)/佯裝不知·零存檔欄·公開身分目標非潛伏 | re-sim, save |
| R108 | **九神騎士團聖物戒律:十字軍聖物 + Umaril 弧(KotN 正典)**(承 R107 前瞻·使用者六拍板:完整 Umaril 弧/7 件全套/正典 infamy≤1/朝聖異象+會員 rank≥2/戒律擋吸血鬼+**狼人**/檔位=魔族之上)。**7 聖物**(`requires_virtue:true`·material crusader):盔18 個性+8/鎧32 魔抗+10/護手13 力量+6/靴14 耐力+6/盾16 魔抗+12+十字軍之劍(blade26·speed1.1·**fire26**·sword 零偷襲)+正義之鎚(blunt**28**·absorb_magicka**16**)〔審查實測被既有神器壓制→使用者拍板上調同帶〕;**全 7 件 no_sell 不可賣可存倉庫**(使用者拍板·兩賣出路徑過濾·封最大單次 faucet);套裝(4甲)恢復+20+resist{magic:12};總重~44 天然重砍偷襲。**戒律=R56 鏡像對**:`inventory.VIRTUE_INFAMY_LIMIT=1`·`virtue_violated`=**infamy>1 或 is_vampire 或 is_werewolf**(單一判定點·三墮落三贖罪路:朝聖/解咒/獵巫)·`_virtue_locked` 掛四 equip·`shed_virtue_locked` 共用 `_shed_by_flag` 骨架(物品留背包)·**破戒自動卸下唯一掛點=`divines._relic_discipline`**(infamy 6 寫入點無收口→game_loop 每圈裁決+recompute R05)+邊緣警告(infamy==1·session 一次性·暫態 `_virtue_warned` 不入檔)。**順手修 R56「假成功」UX**(main.py equip 分支原不看回傳值→改看+`_equip_refusal_message` 三種敘事)。**Umaril 弧**:garlas_malatar(pos[16,12]·d5·守 R28)+umaril_the_unfeathered(solo·raw·HP450·fear≤0.25/1t R44·min_level18·寶藏 generic 非聖物);任務鏈 source relic 從騎士團大廳「⚜ 聖物的召喚」接:relic_vigil(requires_event **pilgrim_vision**+knights_nine rank≥2+level18·5 階)→4甲+盾;relic_umaril(requires_quest 鏈·clear_dungeon)→劍+鎚。pilgrim_vision=R107 朝聖 reward 加 world_flags(冪等 once-fire·`_REPEATABLE_REWARD_KEYS` 加 world_flags)。**🔴 sim byte-identical**(隔離 worktree·sim 裝備非 requires_virtue short-circuit);run_all **104**(test_relics **18** 測:鎖裝三路/自動卸/警告/朝聖復裝/**單一來源**掃全發放面/套裝/boss 規格/**kill 目標可達通用 guard**);BESTIARY 135;零新存檔欄。**對抗審查 6/6 confirmed 全處置**:2 BLOCKER 同根(第3階 spawn-only 怪死鎖→換 lich·加 guard)+預清漏洞(relics_recovered 旗+garlas visible 閘)+異象靜默(_complete 回 new_flags·首次才播)+經濟/強度使用者拍板;**guard 掃出既有缺陷**:rogue_thief/blade_agent 零生成→R99/R100 的 23 任務 kill 目標打不到(_KNOWN_LATENT_UNREACHABLE 鎖住·修法留下一輪)。🔴 改容忍線/加墮落條件只動 `virtue_violated`;破戒裁決只掛 `_relic_discipline`;聖物單一來源(test_relics 攔);鎖裝新旗複用 `_shed_by_flag`;動裝備數值→先問使用者 | re-sim, save |
| R107 | **九神信仰:單槽限時祝福 + 德行閘 + 朝聖贖罪**(審計→使用者選虔誠軸·四拍板:Skyrim 單槽/朝聖歸零須考驗/不做深線/不連動騎士團)。**新 `systems/divines.py`**(骨架=potion_buff×spellfx):`BLESSINGS` 登錄表(數值集中模組頂)·`bless` **單槽 dict 整包覆寫=拜新壇即覆蓋**·`recompute`(剔過期+重建 attr/resist 快取+**必呼 recompute_max_resources** R05)·`can_bless` 德行閘(**惡名不高於名聲且本省無賞金**;新角 0/0 放行·他省賞金不擋)·update 掛 game_loop(spellfx 後)·ensure 掛 save_migrations。**Character +3 欄**(divine_blessing 權威{divine,expires_at}+兩快取·dataclass 預設零遷移);attr() 插層 potion 後/disease 前。`BLESSING_HOURS=48`。**九神=正典賽羅迪爾一城一神**(kvatch阿卡托什速度+10/anvil蒂貝拉個性+10/skingrad朱利安諾斯智力+10/bruma塔洛斯**力量+10**〔R92 sim 先例〕/cheydinhal阿爾凱疾病抗+30/chorrol斯丹達爾**格擋−0.10 夾 BLOCK_FLOOR0.25**〔`_is_player`+`if _db:` 雙 gate〕/leyawiin澤尼薩爾**買價×0.90**〔renown 後·反套利地板前·只買價〕/bravil瑪拉治療+15%〔與 restoration_boon 相加·僅 cast heal〕/**great_forest_trail凱娜瑞絲野祠**旅行−0.10〔吃 floor0.5〕;**帝都=朝聖終點不設祭壇**)。祈禱附帶淨疾(diseases.purify)。入口:world.json `divine` 欄(零遷移比照 shrine)→ hub「⛪ 九神祭壇」→ `action_divine_altar`(被拒→就地指引接朝聖);UI 徽記 HUD/status/角色卡+web chips;codex `nine_divines`+新動態鉤子 `dynamic:"divines"`。**朝聖 `pilgrimage_nine`**(source divine·repeatable·13 階:**首階 collect 供品**〔🔴repeatable 首階不可 reach〕→9 reach+2 kill 試煉→帝都):**新 reward key `clear_infamy`**(_complete `infamy=0`·**同登 `_REWARD_KEYS`+`_REPEATABLE_REWARD_KEYS`**)。**🔴 刻意反轉 R84「惡名終身」(使用者拍板唯一例外)**:outlaw_standing/fence/稱號/renown tier 全隨歸零(衍生層·測試釘住);**bounties 不清**(贖罪名不贖法度);無限重複=無限洗白,代價=長征+試煉+途中追殺;⚠惡名成就 live accessor 歸零後回未達成。「朝聖者之恩」威能砍(powers 單槽成本)。**🔴 sim_assassin BYTE-IDENTICAL**(隔離 worktree·fixture 空槽→全 hook 恆等·solo 0%);run_all **103**(test_divines 15 測:單槽/到期兩路/德行閘四象限/六 hook/朝聖端到端+賞金不清/roundtrip/佈點 FK 九神齊)。🔴 祝福走 BLESSINGS 登錄表+divine_* 獨立層絕不寫 base;朝聖=**唯一** infamy 清除途徑(加第二條先問);改祝福數值/HOURS/FLOOR→先問使用者+跑 sim;加祭壇純改 world.json divine 欄(FK 測試守)。前瞻:每神深線→永久小誓福(R45 cadence)·騎士團聖物戒律/rank 服務·斯丹達爾警戒者(危機後) | re-sim, save |
| R106 | **召喚師深化 II:角色擴展 + 里程碑重設計 + 施法/指揮/死靈(分階段·承 R105·使用者拍板全包大里程碑·分階段子提交)**。修 conjuration 兩冷點(里程碑 5/7 通用填充·角色只有坦克+法師玻璃大砲)。**Phase A(角色擴展·✅)**:① 支援召喚 summoned_healer(bestiary spells:["heal_other"])經新 `magic.summon_support_act`(讀 **bestiary** spells·pool=[player]+盟友·固定 SUMMON_SUPPORT_POWER=0.8·複用 R86 _support_act)施治療;main.py ally 階段依 summon_turns is not None 分流(同伴走 companion_support_act 逐位元組同)。② 控場召喚 summoned_terror(on_hit fear/weaken·走 R105 管線·apply_control·不走施法)。③ 束縛兵刃 archetype 差異化:magic.cast bound_weapon 存 archetype·combat:455 archetype=bound.get("archetype")→bound_mace stagger·greatsword mag20·axe(pen 對元素束縛無意義略過)·armor 複用 shield;新 bound_axe/mace/greatsword/armor+conjure_healer/terror+spell_stock(4 召喚重鎮)。全 innate。**🔴 sim_assassin BYTE-IDENTICAL**(刺客不召喚/不束縛·隔離 worktree vs R105 證)+ sim_party(支援召喚師 mid 99-100%·古龍 65%·dagon 720 終王 0% 牆·無 stalemate)。對抗審查 4 維→0 findings。run_all 102(test_summoning +4);BESTIARY 133。**Phase B(里程碑 50 身份分岔·✅)**:conjuration_50 兩死節點→**咒靈共鳴** summon_casting(法術召喚物施法強化:power 0.8→1.0/冷卻 2→1·summon_support_act 讀 summon_casting_mod)vs **束縛精通** bound_mastery(束縛 +15%傷/+2時程·_weapon_profile ×(1+dmg_bonus)+cast turns+=turn_bonus)。兩身份 A 法術召喚/B 束縛死靈。🔴 使用者砍掉「召喚軍師」指揮。sim_assassin byte-identical(無里程碑→mod={}·_support_act cooldown 預設不變)+ sim_party(咒靈召喚師古龍 65→76%·dagon 0% 牆·無 stalemate)。對抗審查 2 維→0 blocker/major(修 1 minor 束縛訊息 turns)。run_all 102。**Phase C(靈魂 token 死靈經濟·✅大幅擴充)**:可管理持久資源迴圈(使用者拍板;平衡數值全由使用者逐項拍板)。**2 存檔欄**(soul_tokens:int + necro_upgrades:dict·dataclass 預設·零 migration·比照 kill_counts)。**給/回收**(新 `systems/necromancy.py` harvest_and_recover·掛 run_battle 勝利塊·只 victory 封逃跑刷):每擊殺+1(亡者收集再+1)+ 戰後每具倖存**真·亡者**+1·**🔴 限召喚系·走里程碑**:`mastery.has_soul_economy`(**召喚 25 里程碑 death_attunement·新 kind soul_economy·取代舊省魔 conj_basics**·鏡像 has_vanish=base_skill≥25)未解鎖→不積 token(非召喚者零漏出)。**只真·亡者**(使用者拍板):raise_thrall 骷髏奴僕 + reanimate 復生帶 `_undead` 暫態旗〔不入檔〕→ 回收/亡者統御+生命/軍團上限;元素/魔人純魔力經濟。**三花處**:①raise_thrall(kind summon·undead:true·token_cost3·新 reanimated_thrall〔HP28·dmg10·**生命隨技能/攻擊隨法術威力(分軸)**〕)②強化 reanimate_thrall(hp_factor1.0〔base0.6 保留〕·turn_bonus1·token_cost2·rider `eff.get()` byte-safe·復活敵屍滿血)③永久升級選單。magic.cast summon/reanimate 加 token 閘(退魔力/體力)+ 軍團上限(undead_field_cap base3)+ `_undead` 旗 + 亡者統御/生命加成。**🔴 真·亡者分軸縮放**(使用者拍板:**技能→生命·法術威力→攻擊**):生命 `undead_conj_scale`(base_skill(conjuration)·FLOOR0.4/封頂1.25·初始更弱)·攻擊 `undead_attack_scale`(智力威力×法袍/法杖/誓福·**不含技能**·夾 UNDEAD_ATK_SCALE_CAP=2.0)·再疊統御/生命——**base 軍團3 守 dagon ~0% 牆**·擴軍團+堆法術威力到極致方大幅磨穿(**極致付出=真實回報**·R63;對抗審查抓到 sim 政策 min(3,cap) 曾**遮蔽**破口→改誠實填滿 cap)。**永久升級**(data/necromancy.json 目錄·buy_upgrade·hub action_necromancy_altar 限 has_soul_economy·**每種費用皆漸增曲線**):**亡者生命**(真·亡者 max HP+6/級·cap+30·平坦·[10,20,40,80,160])/**亡者護甲**(全域護甲+2/級·cap+10·combat._armor_rating 獨立加法層·絕不寫 base·[10,20,40,80,160])/**亡者軍團**(真·亡者上限+1/級·max2·**破牆唯一槓桿**·[100,500])/**喚魂精算**(召 token−1/級·**可降至0**〔raise_thrall 3−2 仍≥1 守牆·便宜亡者可免費〕·[100,300])·全滿~1620 token。**里程碑改制**(ensure_mastery_choices 退 pending·零存檔欄):conj25 death_attunement(soul_economy 解鎖·取代省魔);conj75 conj_efficiency(spell_mod 省魔15%)vs soul_harvest(新 kind·+1 token/kill);conj100 lasting_summon(summon_mod turn_bonus1)vs undead_dominion(新 kind undead_mastery·真·亡者 HP+30%/傷+20%)·三新 kind R21 三步。**🔴 sim_assassin BYTE-IDENTICAL**(刺客不召喚·necro_upgrades={}→護甲/生命+0·無 soul_economy 里程碑→不積·隔離 worktree)+ sim_party(make_necromancer〔base〕/make_necromancer_maxed〔極致〕·_necromancer_act 誠實填滿 cap):**base 死靈師守 dagon 720 ~0% 牆**(純新手 0%·conj100+法袍 fixture 0.2%)·mid 98-100%·古龍95%;**極致滿升級(生命 skill1.25×·攻擊 法袍1.49×·護甲+10·軍團5·省魔·~1620 token+滿法師裝)dagon 66.8% 磨穿**(**使用者拍板:極致付出應得回報·R63**·軍團 L2 500 token + 法術威力雙軸投入)·無 stalemate。對抗審查 5 維→2 confirmed(BLOCKER 終王牆〔政策遮蔽〕+MAJOR 經濟漏出)·修後獨立再審→0 residual;後續使用者重掌平衡(base≥25 code→conjuration_25 里程碑取代省魔·骷髏 dmg 復原10·加亡者生命·陡增費用曲線·分層牆 base 守/極致破)。run_all 102·BESTIARY 134。🔴 加支援召喚走 summon_support_act(讀 bestiary);控場走 on_hit;束縛走 bound active_effect;新法術入召喚重鎮 spell_stock;**死靈經濟解鎖走 conjuration 25 里程碑(has_soul_economy·取代省魔)**;真·亡者不吃偷襲·**分軸縮放**(生命=undead_conj_scale conjuration 技能·攻擊=undead_attack_scale 法術威力〔不含技能·夾2.0〕);永久護甲/生命獨立層絕不寫 base 夾 CAP;**永久升級每種費用漸增曲線**(necromancy.json costs list);**改 combat/magic/bestiary 死靈數值(含 undead_conj_scale/undead_attack_scale FLOOR/CAP)→必跑 sim_party(base 守終王牆·極致回報幅度)+sim_assassin(byte-identical)**;**平衡數值一律使用者拍板·極致付出=真實回報(R63)**。 | re-sim, save |
| R105 | **召喚師深化:召喚物角色定位 + 各自行動模式 + 元素被動 + 隨召喚主成長(冷系統審計 → 使用者逐步拍板)**。死點:召喚物零身份成長(HP 不吃 conjuration/`_power`·練滿與 25 級一樣強)+ 三元素 atronach 行動同質。**① 角色化(bestiary 純資料)**:魔人/魔靈伴=坦克(高血高甲)·火/冰/雷 atronach=法師玻璃大砲(甲降 5-6·高元素傷)。**② 行動模式(R43 attacks 曲目·已接線 choose_attack)**:法師=元素平擊(on_hit)+蓄力大招·坦克=厚重近戰+**嘲諷 action**。**③ 嘲諷(新小機制)**:bestiary attacks 加 `{"taunt":true,...}`→main.py ally 階段掛 taunt effect 跳過攻擊→`combat.pick_player_side_target` 加嘲諷者 aggro(`TAUNT_AGGRO_CHANCE=0.6`·🔴無嘲諷者短路不擲 rng)。**④ 元素 on_hit**:火 dot/冰 benumb/雷 **stagger**(🔴conduct on_hit 不支援);**combat on_hit 條件放寬** `not _is_player(attacker) and (_is_player(defender) or getattr(attacker,"summon_turns")is not None)`→召喚物攻敵也施 on_hit(surgical:怪物→一般同伴不變)。**⑤ 成長(碰 combat/formulas)**:magic.cast `scale=min(SUMMON_POWER_CAP=2.0, power×(1+達貢之佑))`(power 已=_power(conjuration)×力竭)套 HP + `ally.summon_power`;resolve_attack `raw*=getattr(attacker,"summon_power",1.0)`。**🔴 刻意推翻舊「召喚 HP 不吃 _power」(使用者拍板)**·初始弱靠基礎·CAP 防暴衝·reanimate 不設(非本輪)。**🔴 sim_assassin BYTE-IDENTICAL**(刺客不召喚·無盟友→全新分支不可達·隔離 worktree 證)。**🔴 sim_party**:make_summoner(conj100/其他25/法師裝)→ mid boss 97-100%·古龍 71%(vs 電法師 89%)·**dagon 終王 0% 牆守**·無 stalemate=召喚師≈頂級法師。run_all 102(新 test_summoning·test_mythicdawn 達貢之佑改比值斷言);BESTIARY.md 重生。對抗審查 5 維 17 agent→0 blocker/major(reanimate 缺 summon_power 全 REFUTED·修 2 nit)。🔴 加召喚角色/moveset 純改 bestiary(on_hit∈dot/benumb/stagger·雷不可 conduct·taunt 無 damage);成長走 SUMMON_POWER_CAP+power×(1+boon)(調→sim_party 守 conj100≈法師+dagon0%);on_hit 放寬只認 summon_turns。 | re-sim, save |
| R104 | **實用/幻術魔法軸:讓魔法首次在戰鬥外有意義(魅惑/隱形/安撫/羽落/偵知)(冷系統審計 workflow → 使用者拍板方向 + 逐項平衡)**。死迴圈:全 6 學派唯 illusion/alteration 的戰鬥外實用面全空(advertised-but-undelivered;戰外施法只 heal/restore)。**6 新法術**(spells.json 純資料):幻術 calm 安撫/charm 魅惑術/invisibility 隱形術 + 變換 feather 羽落術/detect_life 偵知生物;spell_stock 佈點(charm/feather 廣 25 城·calm/invis 4 illusion 深化城·detect 4 alteration 城)。**增益層 `systems/spellfx.py`(+1 存檔欄 spell_effects)**:charm/invis/feather/detect=限時自我增益 {kind,magnitude,expires_at}(絕對小時·鏡像 potion_buff 但**無推導快取**·helper on-the-fly 讀·**絕不寫 base**);update 掛 game_loop 剔過期·ensure 遷移(save_migrations)。**六接線點全複用聚合點**:①社交(charm 逐面·使用者拍板)persuade/pry+0.12·talk_down+0.10·barter+0.08·steal+0.10 且賞金×0.7(比照 R56 charm_social_bonus);②潛行(invis 完整工具)travel 遭遇×0.4·run_battle 入戰重獲 opening(入戰即破)·guard/manhunt 繞過;③脫戰(calm 只對雜兵·solo 完全免疫·成功率隨敵數非線性 formulas.calm_chance 1敵85%→4敵21%)apply_control 加 calm 分支·is_incapacitated 納 is_calm·全敵 calm→「從容離去」;④探索 feather max_weight+50·detect offer_battle 揭敵情+scouted(消耗);⑤action_cast_self 城鎮放行 4 self kind·magic.cast +state 參;⑥spell_effect_summary 補 5 kind。**🔴 隱形偷襲首擊仍受 SOLO_SNEAK 夾(不秒 solo·專測)·calm solo 免疫·絕不寫 base·所有平衡數值使用者逐項拍板**。**🔴 sim_assassin BYTE-IDENTICAL**(隔離 worktree diff 證:刺客持匕首不施法·spell_effects=[] → 新分支全 append-only 且 gate 在已施法/持增益·helper 對空回 0/1.0/False → 社交/物價/負重/遭遇/opening 逐位元組同·零 recompute)。run_all 101(新 test_utility_magic)·check.sh --smoke 全綠。🔴 加實用法術純改 spells.json(5 新 kind)+world.json spell_stock+test_spell_schema._KINDS;戰外增益走 spellfx(絕對小時·不寫 base);calm 走 apply_control(solo 完全免疫)+formulas.calm_chance;隱形偷襲受 SOLO_SNEAK 夾;**改任一平衡數值(spellfx CHARM_*/INVIS_*/FEATHER_*·formulas CALM_*·魔耗時程)→ 必先問使用者**。 | re-sim, save |
| R103 | **徒手失衡 gated:里程碑解鎖(hand_to_hand 25)+ 輕裝武僧(不穿重甲)(承 R102·使用者點名)**。評估揭露 R102 失衡是 innate 且兩軟點:重甲對徒手戰鬥**零減益**(閃避只看雜技·揮擊耗體/ramp/拳傷都不看護甲)、25 節點 `fist_basics`(+5%)只是冷填充。使用者拍板:失衡改「**25 解鎖 + 僅未穿重甲累積**」。**機制**:① `inventory.wears_heavy_armor`(任一 equipped 件含盾 weight_class==heavy→True·飾品無 weight_class 不計·輕盾不擋)·② 新 kind `offbalance_unlock`(R21 三步·`has_offbalance_unlock` **鏡像 has_vanish**=base_skill≥25 門檻判定·自動授予零遷移)·③ h2h_25 `fist_basics`→`unburdened_stance`(offbalance_unlock·無傷害)·④ combat `_offbalance_active=has_offbalance_unlock and not wears_heavy_armor`·**兩 hook 皆閘**(疊層+ramp)→ 重甲/未解鎖完全不累積(踉蹌/擊倒全無·中途換重甲即停)。skills.json 文案修正。**🔴 byte-identical**(helper 只在 hand_to_hand gate 內呼叫·`wpn_skill_id=="hand_to_hand"` 短路在前·sim 持匕首永不入·隔離 worktree diff 證對 R102 逐位元組同)·門檻只認 base_skill·獸形排除。**存檔**:opt_id+kind 改→`ensure_mastery_choices` 退舊選 pending+舊+5% 快取蒸發(零新存檔欄·無 fist_basics 殘留)。**平衡(sim_builds)**:輕甲 monk 不變(99/90/83/19·終王牆守)·重甲 monk 失衡全失→進攻崩(古龍 3%·dagon 0%·**乾淨 dead 非 stalemate**)→「徒手要靈活=穿輕甲」真取捨。**對抗審查 4 維 12 agent→0 blocker/0 major**(8→5 confirmed:4 nit〔byte/gate/save 全正向〕+1 minor〔BUFFS.md 徒手 weapon_mod 列已移除 +0.05·已修〕·0 uncertain)。run_all 100(test_offbalance +gate 測)·byte-identity worktree·check.sh 全綠。🔴「輕裝才生效」沿用 wears_heavy_armor;自動授予 unlock 走 has_xxx=base_skill 門檻(鏡像 has_vanish);兩 hook 同閘 `_offbalance_active`;改 25 opt_id 走 ensure_mastery_choices。 | re-sim, save |
| R102 | **徒手技能深化:失衡(off-balance)疊層 → 徒手傷害遞增 + 兩段擊倒(使用者三拍板:方向=體力/失衡戰·機制=疊加多層提升傷害·payoff=兩段踉蹌+滿頂罕見真擊倒)**。skills.json 徒手廣告「耗損對手體力」但 Creature 無 fatigue 欄·`_fatigue_ratio` 對怪恆 1.0 → 招牌死機制(R82 式);徒手基傷 4·除虎人爪外無身份;h2h_50 grappler(skill_fortify +8)唯一冷填充。**核心(`offbalance` 疊層·鏡像 R75 conduct·暫態 active_effects 不入檔)**:`magic.offbalance_stacks/add_offbalance/reset_offbalance`+tick 文案;`formulas.OFFBALANCE_*`(MAX 8·STAGGER_THRESHOLD 4·KNOCKDOWN_CHANCE 0.25·DMG_PER_STACK 0.04)+`offbalance_stack_gain(skill,poise_rate)`〔1+skill//50·×(1+poise_rate)〕+`offbalance_damage_multiplier`〔1+0.04×stacks·夾+32%〕。**combat 兩 hook(全閘 `_is_player and wpn_skill_id=="hand_to_hand" and not beast`)**:① 傷害 ramp(物理護甲後·**額外閘 not sneaking**)`dmg*=mult(讀 pre-add 層)`〔疊加越打越痛·偷襲不吃→守 R61+solo 偷襲夾〕;② 疊層+兩段(on-hit 塊)`add`→`stacks≥MAX 且 rng.chance(0.25)`→`apply_control(paralyze,turns=1)`〔R44 solo 機率抵抗+去重〕+reset(防 lock-loop)·`elif stacks≥THRESHOLD`→`apply_control(stagger,turns=1,source="offbalance")`〔軟控·來源去重·不重置 ramp〕。**里程碑冷側 grappler→grapple**(weapon_mod·poise_rate 0.6·**複用 weapon_mod 不新增 kind**·merge tuple 加 poise_rate·SUM-capped R35 安全)·**opt_id 改名→ensure_mastery_choices 自動退 pending**(零新存檔欄·無測試引用 grappler)·skills.json 文案修正。**🔴 byte-identical**(三 hook 全閘 hand_to_hand·sim 持匕首永不入·rng.chance 只 cap 分支內抽·隔離 worktree diff 證 sim_assassin 逐位元組同)·per-enemy 失衡層(R17 非 AoE)·控場全走 apply_control(R44)·ramp 不餵 sneak_mult。**平衡(sim_builds 新 make_monk·warrior 對標)**:solo 與近戰同級(90-99%·古龍 83%≈刺客·**終王 dagon 19% 牆守**)·無 stalemate·**群戰守 R71**(每敵獨立失衡·輕甲 monk 反最脆·4-dremora 死亡 69% vs warrior 1-2%·glass-cannon 決鬥者)·常數 sim 開箱即穩;fight() 加 is_incapacitated(boss) 閘鏡像 run_battle(既有 build 不對 boss 硬控→byte-identical)。**對抗審查 5 維 10 agent→0 blocker/0 major/0 defect**(1 confirmed nit:reset_offbalance 對 solo 被抵抗的擊倒仍重置=by-design 守 anti-perma-lock·非 bug·已加註解;0 uncertain)。run_all 100(新 test_offbalance 14 測)·byte-identity worktree·check.sh 全綠。🔴 改失衡常數(DMG_PER_STACK/KNOCKDOWN_CHANCE/MAX)→必跑 sim_builds(monk)守 solo 不 trivialize+群戰 R71;失衡走 active_effects 不入檔;ramp 閘 not sneaking;真擊倒走 apply_control(solo 機率抵抗);擒拿手複用 weapon_mod poise_rate(非新 kind);獸形排除。 | re-sim, save |
| R101 | **名聲/惡名變活的社交軸(承 R100 後換叢集·使用者拍板:位移態度 + 社交+經濟)**。fame/infamy 到處寫卻幾乎不讀(名聲只在 quests.requires_fame 當閘·惡名犯罪面由 crime R84 已活)→ 變有感聲望軸。**新 `systems/renown.py`(純衍生·零存檔欄·讀 char.fame/infamy)**:renown_tier/renown_title(fame 50/150/350/700→無名…傳奇英雄)·notoriety_social_tier(infamy 20/60/150·**社交面·異於 crime.OUTLAW_THRESHOLDS**)·attitude_shift(civil 階梯 cold/neutral/friendly 內 +fame_tier−infamy_tier 夾·**非 civil〔hostile/comrade/vampire_seen〕原樣·淨 delta 0〔含 tier-0〕→ no-op**)·price_factor(名聲折扣−惡名加價·cap 0.12·tier-0=1.0)。**接線極小**:dialogue.attitude 算 base 後、餵 _guild_attitude **前**插 attitude_shift(公會 comrade/hostile 覆寫仍最終決定)·world.buy_price 乘 price_factor(**只買價·賣價不碰免 faucet**·反套利地板前)·quests 加 requires_infamy(鏡像 requires_fame)·UI renown 稱號 chip。**閘幾乎免費**:events.meets 本已支援 min_fame/min_infamy → dialogue 話題直接 requires 即生效。**內容**:renown_praise(min_fame50)/infamy_dread(min_infamy20)話題 + 3 板委託(requires_fame/infamy·守 reward cap)。**🔴 守住**:零新存檔欄·**sim byte-identical**(不碰 combat/formulas·sim fixtures fame/infamy=0→全 no-op)·**tier-0 嚴格 no-op**(fame=0∧infamy=0→態度不位移·price ×1.0 浮點恆等→既有態度/物價/測試逐位元組同·關鍵相容性)·**不重複 crime 惡名軸 R84**(outlaw/notoriety/fence/bounty/衛兵留 crime·renown 惡名只加社交冷待+買價加價正交·惡名稱號沿用 crime.notoriety_title·只新增名聲稱號)·聲望不造 hostile/comrade(公會 _guild_attitude 覆寫優先)·反套利地板恆守。**對抗審查 3 維 17 agent→0 blocker/major**(byte/tier-0/態度不造敵意/反套利/不重複 crime 全正向·0 uncertain)→修 1 cosmetic(infamy_dread 補 ally say_by_rel)。run_all 99(新 test_renown)·sim byte-identical。🔴 加聲望效果改 renown.py 旋鈕 + 資料(min_fame/min_infamy 話題·requires_fame/infamy 任務);**tier-0 必 no-op**;**只動買價**;聲望位移夾 civil 不造 hostile/comrade;**不碰 crime 惡名軸**。**前瞻**:聲望解鎖謁見/旅店免費/饋贈·惡名招賞金獵人(連 crime active_heat)·更多 gated 內容 | save |
| R100 | **玩家雙面間諜系統:臥底生涯(掩護↔價值觀漂移·殺知情者滅口·真叛變)(承 R96–R99·使用者選完整 Option C)**。R96–R99 的「NPC 臥底」鏡像到玩家;障礙=`join_block_reason` 宿敵互斥擋雙會籍。**新 `systems/undercover.py`(仿 skooma 詛咒模板)+ Character +7 cover_* 欄(ensure_cover_fields 遷移)。🔴 零戰鬥面狀態(無 *_bonus·不碰 attr/skill)+ cover_guild 絕不寫進 char.factions(保 R96 互斥·perk_value(B)=0 自動零戰鬥 perk)→ cover_guild=="" 時全 no-op**。**掩護**:faction_hostility +1 行(cover==fid→0)·_guild_attitude +1 行(cover==apparent→comrade)。**入局(opt-in)**:犯罪公會 A 大廳 rank-gated「間諜事務」(INFILTRATE_PAIRS thieves/db→fighters·mythic_dawn→knights·RANK2·can_infiltrate)→ infiltrate;三犯罪大廳掛入口(_contract_hall 閘 is_criminal_guild→九神排除)。**漂移引擎**:repeatable source:undercover 差事(requires_cover)·reward 僅 cover_deltas{loyalty,secrecy}(無 gold→無 faucet·_REPEATABLE_REWARD_KEYS 納入):取信於敵(+)vs 為我方破壞(−)= 要藏好就得替 B 賣力→生同情=價值觀衝突。**殺知情者滅口**:undercover.update 每日衰減 secrecy(ratchet)→歸零/逾期→expose(清掩護·記 cover_blown_<A>_<B>·仍在 A→faction_hostility(B) 自動復敵);_undercover_detection(抵城 rng)選現有具名 B NPC(pick_knower 優先省·排除 murdered·全滅口回 None 安全)為 cover_knower+期限;action_talk「🔪 滅口」→ run_battle(**新增 flee_after_rounds:int|None=None**·限時決鬥)→ 擊殺保住/逾回合 fled_enemy 曝光。**🔴 flee_after_rounds 預設 None→既有 14 呼叫端逐位元組同·sim 用自有 _round 不經 run_battle**。**結局**:source:undercover+requires_cover+requires_loyalty(互斥):抽身(loyalty≤0→clear_cover+忠誠者誓福+留 A)·叛變(loyalty≥60→defect 棄 A 入 B+叛變者誓福+惡名);三結局皆記了結旗標→can_infiltrate 阻同對重啟。4 誓福守 R45 紅線(無 strength/sneak/武器技)。UI 掩護徽記(HUD/status/終端)。**🔴 對抗審查 3 維 28 agent→0 blocker/major**(byte-identity/R96+存檔/曝光邊界全正向;2 refuted)→修 1 UX nit(識破訊息顯示「限 N 日內」非絕對日)+補 1 測。3 commit 分期·run_all 98(新 test_undercover 24 測)·**sim_assassin byte-identical**(combat/formulas 未碰·刺客 cover_guild=""·solo 0%)。🔴 加潛入對純改 undercover.INFILTRATE_PAIRS+A 大廳入口;加漂移/結局純改 quests.json(source:undercover·requires_cover/requires_loyalty·cover_deltas/clear_cover/defect·誓福守 R45);cover_guild 絕不入 factions;動 run_battle/undercover→必跑 sim_assassin。**前瞻**:更多潛入對·掩護期 B 非戰鬥服務·與反間任務連動 | re-sim, save |
| R99 | **公會諜報層:臥底揭發 + 招募內線 + 反間任務 + 大廳反間服務(承 R96–R98·使用者點名三項)**。R96 敵意/R97 隱蔽身分+臥底/R98 同志服務鋪好舞台但缺反制迴圈→補完。**角色二分**:Mole(掩護 guild+真身 secret_guild)=揭發目標·covert(僅 secret_guild)=招募內線目標。**臥底揭發**:共享話題 expose_mole(req mole_expose:"outsider"·fame+5·吹哨)/betray_mole(req "comrade"·infamy+2·大義滅親)·once·掛各 mole extra;新 meets_dialogue 鍵 **mole_expose**(NPC 須 guild+secret_guild∧secret_guild_revealed·outsider=非真公會員/comrade=是);後果=`_guild_attitude` 頂端 {expose,betray}∩dialogue_done→hostile→topics_for 回[]→連 R98 服務一併斷(once 防再揭·防揭完再交易)。**招募內線**:各 covert deep.recruit_informant(req secret_comrade·once·effects start_quest:cintlead_)+extra→招募得反間線索·**零新邏輯**。**反間任務**:8 cint_ 可重複榜(source:board·repeatable·kill blade_agent/rogue_thief·reward gold+fame)各省 world_pulse 聚光(elsweyr 放 skooma 脈動非 smuggle 脈動)+5 cintlead_(source:npc 一次性·招募派發)。**大廳反間服務**:新 `_counterintel_board`(鏡像 _underworld_contracts)·CINT_RANK=2 rank-gated 掛三犯罪大廳(thieves action_guild_hall cint_ok·DB action_sanctuary·mythic_dawn 經 _contract_hall 閘 is_criminal_guild→**九神排除**);**🔴 cint_ 加入 action_board 排除**(只走大廳·不上公開告示板)。**🔴 對抗審查 3 維 33 agent→1 blocker**(cint_ 漏修 action_board 排除→聚光時洩漏繞過階級閘)→修+補 3 測(公開板排除/九神排除/招募需會員非僅揭露);餘 26 正向確認。**🔴 零新存檔欄**(衍生 dialogue_done/factions/quests)·**sim byte-identical**(未碰 combat/formulas·git diff 證)。run_all 97(新 test_espionage 15·test_data_schema +mole_expose 驗+對話 start_quest 可達性)。🔴 加臥底純改 npcs.json(guild+secret_guild·**不得帶 quest/rumor_quest**=R96 守則);內線純改 npcs.json(僅 secret_guild)+dialogue.json(deep recruit start_quest);揭發走 mole_expose req·翻臉走 _guild_attitude 讀 dialogue_done;反間榜純改 quests.json(cint_ repeatable kill·gold/fame/infamy)+world_pulse 聚光·**cint_ 必入 action_board 排除**;大廳服務閘 is_criminal_guild+CINT_RANK(九神排除)。**前瞻**:臥底反向利用(假情報)·內線循環多線索·更多 mole/covert NPC | save |
| R98 | **犯罪公會服務層:已揭露犯罪同志的會員專屬服務(承 R97·使用者拍板)**。R97 建好隱蔽身分社交圖後揭露無機制回報(guild_intel 指向無獎勵 rumor)→ 把 comrade 網路功能化:聯絡人=服務,全複用既有管線·純社交/服務層·刻意小。**三服務**:盜賊「找門路銷贓」=便攜呼叫既有 `action_fence`(沿用內建防套利地板)·黑兄「接私活」=薄 `_comrade_contract`(複用既有 DB 公會合約池 available_quests("guild",dark_brotherhood)+_active_db_quest+_accept_and_brief+action_contract·無雙重獎勵/無新 source·**刻意不含聖所洗白/五戒**·可戰死回 "dead" 經 action_talk 上拋)·神話黎明「禁術貨源」=薄 `_comrade_supply`(buy-only·`_MYTHIC_SUPPLY` 廣義黑市稀材·world.buy_price 內建地板·排除 _LOOT_ONLY_MATERIALS)。**🔴 可見性閘**:comrade 態度由兩路徑(犯罪 secret_guild 揭露+會員 / 公開 guild 相認)·extra/template 話題不受 attitude 過濾 → 新 dialogue 要求鍵 `secret_comrade:<guild>`(meets_dialogue:NPC.secret_guild==值 ∧ is_member ∧ secret_guild_revealed)·三話題各帶不同值 → 無揭露前/公開公會/跨公會洩漏(多會籍玩家在盜賊同志處只見 fence)。**掛載**:3 話題進 attitude_topics.comrade 模板(req 自動篩·**不需編輯 9 NPC**)·action_talk 依 td.action∈{fence,supply,contract} 攔截派發(否則照走 _resolve_dialogue_topic·鏡像 R82 pump action)。**🔴 防套利**:fence/supply 各守地板·跨服務(盜賊+神話黎明非宿敵雙會籍)買入再 fence 銷贓永無正 EV(買價商人加成 ~數倍 >> fence 1.45×·專測守)。**🔴 零新存檔欄**(閘=char.factions+R97 衍生揭露)·**sim byte-identical**(未碰 combat/formulas·git diff 證)。對抗審查 4 維→1 confirmed(minor 反套利測試未涵 sell_bonus)→ 補強跨服務套利測試(subsume);0 blocker/major·logic 全 refuted。run_all 96(新 test_comrade_services·test_data_schema +secret_comrade FK/action 集)。🔴 加犯罪服務:話題進 comrade 模板+secret_comrade:<犯罪公會> req+action 派發(handler 在 main 因需 ui.menu·dialogue.py 純邏輯);供貨守 loot-only 排除+buy>sell+跨服務不獲利;DB 走既有合約池不開新 source。**前瞻**:臥底揭發/招募內線/反間(R97 另列)·大廳更多 rank-gated 服務·更多 secret_guild NPC | save |
| R97 | **公會社交網:隱蔽犯罪身分 + 同會相認 + 調查揭露 + 臥底(承 R96·使用者連續拍板擴展)**。**公會分類**:factions.CRIMINAL_GUILDS={thieves/db/mythic_dawn}+is_criminal_guild。NPC 標 **guild=公開/表面身分**(公開公會·或臥底掩護)·**secret_guild=隱藏犯罪身分**(隱蔽成員/臥底)。**三路徑揭露(dialogue.secret_guild_revealed·🔴 零新存檔欄·衍生 char.factions/npc_disposition/dialogue_done)**:① is_member(secret)② disposition≥75 ③ 調查成功(dialogue_done 含 _GUILD_KNOWN)。**態度(_guild_attitude)**:揭露+你是秘密同會→comrade 相認(內線);否則以表面 guild 處理(同公開會 comrade·公開公會公然敵視宿敵 R96·**臥底以掩護公會行事=佯裝敵視真同志**);犯罪公會即使被看穿仍不公然敵視。**調查融入套話(R82 _do_pump→_maybe_reveal_secret_guild)**:scout/口才檢定 vs secret_secrecy·成功標 _GUILD_KNOWN。**broad**(融入套話·一般人查無所獲·**選項不洩密→無 meta-gaming**)。**同會相認+情報**:comrade attitude(greetings.comrade+attitude_topics.comrade **⊇ friendly+guild_intel**)·guild_intel 話題指向 rumor 無可剝削獎勵。**內容**:8 公開+7 隱蔽(brynjolf/maven 盜賊·db·mythic_dawn·含 2 新 NPC 填隱藏公會)+2 臥底(evermore_warden fighters/thieves·aurelia mages/mythic_dawn 滲透)·secret_secrecy 40-70。**🔴 不碰 combat/formulas→sim byte-identical**·零新存檔欄·R96 通用規則仍守(guild 標不攜一般任務)。對抗審查 3 維 12 agent·9 發現 2 confirmed:minor 臥底揭露抹除掩護關係(原 return base→改 fall-through 到 apparent·只疊加不抹除)·nit secret_secrecy 補 schema 驗;logic 全 refuted。run_all 95。🔴 公開用 guild·犯罪用 secret_guild(FK:secret_guild∈factions 且犯罪·secret_secrecy 0-100);揭露純衍生零存檔欄;臥底揭露只疊加不抹除掩護(fall-through);調查融入套話 broad 不洩密;comrade⊇friendly;guild 標不攜一般任務。**前瞻**:臥底揭發/招募內線/反間任務·犯罪公會服務·更多 NPC 標 | re-sim, save |
| R96 | **公會宿敵生態 Phase 1:派系敵意 + 對立後果(衍生·零存檔欄)(評估「派系生態活化」→ 9 公會原是孤立任務階梯 silo·rivals 只當入會硬鎖;使用者拍板分兩期:Phase 1 宿敵後果 / Phase 2 據點消長)**。**衍生敵意(factions.py·純函式零存檔欄)**:`faction_hostility(char,gd,fid)`=fid 視玩家為仇敵 ⟺ 玩家在 fid 的某 rival 公會有階級(tier=max rank_index+1)·`is_member` 短路·+`is_hostile`/`most_hostile_guild`(sorted 決定性);**前提 rivals 入會互斥→自洽**·零存檔欄(衍生自 char.factions·舊存檔自動生效)。**對立後果**:① 對話態度(dialogue.attitude 加 guild-hostility 分支·只升級不軟化·tier≥2 hostile/tier1 cold)② 路途宿敵打手伏擊(main._travel_to 優先序階梯·**bounty>guild 絕不雙觸**·皆 foe=None·`_guild_enforcer_ambush` 鏡像 R84·spawn min(3,1+tier//3) 隻 guild_enforcer/guild_avenger·存活不加賞金=自衛·bestiary +2 怪 weight0/min99·頻率 min(0.35,0.10×tier)×(1−騎馬)·tier≥2 出鋼·**永久後果 by-design** 無退會=burn-your-bridges)③ 大廳拒服務(既有 join_block_reason rival 鎖已覆·免改碼)。**🔴 通用 NPC-公會內容規則(使用者點名通用·非限定某城)**:guild-標 NPC **只攜帶公會相關內容**(hostile 不發任務是正確的·但 guild NPC 攜一般任務會被永久鎖死那段)→ 一般任務歸一般 NPC(marcus chain_kvatch)·只標無一般任務的公會成員(brand/gaius/sabine)·`test_guild_npcs_carry_only_guild_content` 通用 guard。**主動破壞任務延 Phase 2**(rank ladder 一階一任務·插中段破存檔·配 oust_rival 才有 payoff)。**🔴 不碰 combat/formulas→sim_assassin byte-identical**(solo 0%/精英 Δ0)·零新存檔欄。對抗審查 3 維 19 agent·16 發現 9 confirmed→修 2 major(永久後果 docstring 假稱可規避→改 by-design·chain_kvatch 鎖死→通用規則只標合規 NPC)+nit(BESTIARY 重生·頻率軟化·import 提頂·伏擊補測)。run_all 95·BESTIARY 131 怪。🔴 敵意純衍生 char.factions·零存檔欄;guild NPC 只攜公會內容(guard 通用把關);伏擊 bounty>guild 皆 foe=None 走 state.rng;永久 by-design;不碰 combat→sim byte-identical。**Phase 2**:據點消長(城足跡 base+overlay·oust_rival/establish·同城多公會共存·僅都市公會·lair/cult opt out·不碰 aiwar) | re-sim, save |
| R95 | **煉金材料正典化:生物部位移 creature loot + 2 新怪 + 野採池重建(承 R94·使用者點名「依正典重調所有材料稀有度+取得方式」)**(評估 workflow 4 agent 研究 51 材料 TES 正典+準確性審查 → 生物部位〔脂/牙/爪/趾/鱗/卵/塵/心/鹽〕正典是 creature loot 非野採植物。兩拍板:生物部位 loot-only〔hunt-gated〕·無對應怪者新增怪)。**21 試劑移 creature loot**:troll_fat→troll·ogres_teeth→ogre·bear_claw→bear·giants_toe→frost_giant·hagraven_claw→hag/witch·spriggan_sap+taproot→spriggan·mudcrab_chitin→mudcrab·void_salts→storm_atronach·hawk/nightingale_feather→harpy(加既有怪 loot 0.4-0.6);spider_egg/vampire_dust/charred_skeever_hide/bone_meal/glow_dust/scrib_jelly 本有 loot(只移出池)。**2 新怪**(bestiary.json):flame_atronach(ashland·d5·min8·火·掉 fire_salts·對標 storm_atronach)·slaughterfish(swamp·d2·min3·水掠食·掉 slaughterfish_scale/egg/pearl)·經 random_encounter biome 加權自動進遭遇。**野採池重建**(alchemical_ecology.json):只留植物/菌/昆蟲~29·每池 ≥2 common/≥4 total·canis_root 改溫帶·frost_mirriam 留 snow(乾燥香草無怪=例外)·jarrin_root 留 savanna rare·多數池 rare 變空。**可達性全保**:全 51 材料池∪loot∪shop 可達;alteration/conjuration/resist_poison 變 loot-gated(殺 spriggan/泥蟹/屠魚)·illusion/acrobatics/resist_disease 留野採;元素抗 fire/void_salts/pearl 既有 shop(d0 城 1 級可買→不 level-gate)+新怪 bonus。guard:test_every_ingredient_is_reachable·test_r95_new_kinds_reachable。**🔴 brew effects 零改(只移取得端)→ 確定性 brew-diff 既有 528 對零位移**;sim_assassin combat/formulas 未碰→**byte-identical**(solo 0%/精英 Δ0);**反而收緊野採可達攻擊向 fortify 17→2 對**(生物部位移 loot)。零新存檔欄。對抗審查 3 維 15 agent·12 發現 **0 defect**(全 refuted/by_design)。run_all 94·BESTIARY.md 129 怪。🔴 生物部位走 creature loot(非野採池)·植物/菌/昆蟲走野採;加新怪守 R11(min_level/danger 非 stat 縮放)/R12(biomes);每材料/kind 須可達(池∪loot∪shop·loot-gated 算可達);改 loot/池不碰 combat→sim byte-identical | re-sim, save |
| R94 | **野採稀有度功能化:偵查定值預算 + 原料擴展(承 R93·使用者點名「依稀有度·偵查越高採稀有度越高數量越多·定值分配」+ 擴展原料)**(三拍板:分配=自動隨機湧現·範圍=全四批·稀有度=浮動預算讓低偵查小機率中 rare)。**機制(`events.py`)**:廢 R93 draws+cap → **浮動預算 budget + tier 成本**:`budget=4+scout//8+rng(0,2)`·`tier_cost{common1,uncommon3,rare6}`·預算內加權抽扣成本·單品上限3·**防呆迴圈=`len(cand)*MAX_QTY+1`**(綁池大小不靜默截斷)。「大量低稀有 OR 少量高稀有」=固定預算+異質成本+加權抽的代數後果。實證 scout0 rare≈2%·scout200≈71%·件數 3.6→16。**內容(+18=51 材料)**:6 新可釀 kind FAIL-pair(`fskill_{alteration,conjuration,illusion,acrobatics}` 補零來源技能 + `resist_poison/resist_disease` 引擎 entity_resist 已讀零邏輯)·攻擊向降稀缺(giants_toe/hagraven_claw/slaughterfish_egg/daedra_heart·R92 sim-gated)·rare 招牌(jarrin_root 純毒/crimson_nirnroot)。**daedra_heart 特例(使用者點名兼鍛造)**:既有 items.json 鍛造材料(value500·14 daedric 配方)→ 兼煉金(加 effects·保 value500/weight1·unified ingredient 覆寫 misc)·**loot-only 不入野採池**(原 dremora/dagon 掉落=打稀有怪)。**可達性(使用者質疑「rare 有必要進商店嗎」→ 不加)**:soft 門檻已保可達·賣 rare 稀釋稀有感 → nirnroot 放 uncommon(alteration 不被 rare gate)·6 新 kind 各有「兩材皆 common/uncommon」可達對·rare 招牌皆額外來源無一唯一·guard 改 test_every_ingredient_is_reachable(池∪loot∪shop)。**🔴 brew 邏輯零改(只 ADD 材料)→ 確定性 brew-diff 證既有 528 對零位移·6 新 kind clean FAIL-pair·零 smithing/mysticism 產物**。`sim_assassin` combat/formulas 未動→**byte-identical**(攻擊向新來源只改取得難度·potion_buff 取最強非疊加·solo cap 絕對→R92 fortify-dosed solo 0%/精英 Δ0)。零新存檔欄。**對抗審查 3 維 10 agent·7 發現 0 blocker/major**→ 修 resist_disease 顯示「抗disease」→「抗疾病」(R01)·daedra_heart weight 還原·range(64)綁池大小。`run_all` **94**。🔴 採集量唯一旋鈕 `_FORAGE_*`(scout 軸·jitter 是低偵查 rare 唯一閘·動之先問);加可釀增益用 FAIL-pair+確定性 brew-diff;攻擊向新材料/來源必跑 sim;smithing/mysticism 永不入池·daedra_heart 兼鍛造保 value/weight;forage 防呆綁池大小。**前瞻 R95(使用者已點名)**:依正典重調全 51 材料稀有度+取得(生物部位 23 個移 creature loot·鹽移 atronach·spriggan 產物移 spriggan·乾燥試劑移 shop) | re-sim, save |
| R93 | **野外採集解耦:生態系材料池隨機抽取(動作/物品/數量三者解耦·偵查成長)(承使用者「評估」野採是否固定組合/毒藥幾型 → 拍板解耦)**(舊野採=寫死固定材料束、一次選一束;改成從「生態系材料池」加權隨機抽**任意組合+數量·有上限**)。三道拍板:① 池放**新檔 `alchemical_ecology.json`**(專檔可擴 tier/季節)·② 採集量**隨 `scout`(偵查)成長**(跨技能 synergy·明確平衡軸)·③ 範圍=轉換 9 forage + 把非 forage 材料納入池。**機制(非純 JSON·必動邏輯)**:新 effect type **`forage_pool`**(`systems/events.py`:`forage_pool_draw` 加權抽取〔`_weighted_pick` 整數累積·決定性走 `state.rng`〕+ `apply_effects` 分支)·**唯一旋鈕** `_FORAGE_*`:`draws=2+scout//40`·`cap=4+scout//25`·每抽 qty 1..2·tier 權重 common4/uncommon2/rare1·**採到才練 scout0.4+alchemy0.5〔空手不練·審查 nit 修正〕**。`gamedata.ecology` 載入(+1 行)。9 生態池(ashland/swamp/heartland/snow/desert/moor/jungle/savanna/border)·`_doc`/`_name` 前綴不入抽取·**全 33 材料皆入 ≥1 池**(含舊只靠掉落的 charred_skeever_hide/bone_meal/spider_egg/vampire_dust)。events.json 9 個 `*_forage` 轉 forage_pool(herb_patch/NPC 鉤/check 型特殊事件不轉)。guard:`test_crafting._wild_gatherable`+`test_polish._is_forage` 認 forage_pool 池成員;新 `test_foraging`(決定性/上限/scout 成長/全材料入池/pool id 解析/空池不練/整合)。**🔴 平衡(brew 邏輯零改·只動取得端)**:對抗審查 3 維·9 agent·**6 發現 0 defect**(3 confirmed 皆 minor/nit)——**攻擊向 fortify 對零新共置**(troll_fat+ogres_teeth=str 等舊本就同 option·blade/h2h/sneak 對仍跨池分離→R92 sim-gated 紅線守住)·**smithing/mysticism 無材料**(野採物理不可能產出永久逃逸藥)·但**5 對安全增益首次同池共置**(fattr_speed/fskill_alchemy/fskill_athletics/fskill_security〔desert〕·resist_magic〔jungle〕→ 可單採湊齊·**全在 R90/R91 安全池不破紅線**·FAIL-pair 不變式仍成立)·scout cap 無硬天花板=by-design 豐度(材料非戰鬥·限時 buff/毒個別受 R30/R31 夾)。**🔴 `sim_assassin` BYTE-IDENTICAL**(零碰 combat/formulas·forage 不在 sim 路徑·solo 0%/精英 95.4%/4-bandit 60.6% 同基線)·**零新存檔欄**(forage_pool 效果不入檔·ecology 唯讀·scout 既有·forage 走已序列化 `state.rng`)。`run_all` **94**(新 `test_foraging`)。🔴 加可野採材料純改 `alchemical_ecology.json`(對應生態系某 tier·**每材料須入 ≥1 池**)+ forage_pool 引用(pool id 須存在防孤兒);採集量旋鈕=`_FORAGE_*`(scout 軸·動之**先問**);**安全池共置 OK·攻擊向新共置須先過 R92 sim 閘·smithing/mysticism 永不入池**;forage 走 `state.rng`·空手不練;不碰 combat/formulas → sim byte-identical | save |
| R92 | **攻擊向 fortify 藥開放(力量/武器技能/潛行/毀滅)—— 依 R30「放開→必過 sim」(使用者點名「難道不該有強化力量、毀滅的藥」)**(使用者質疑 R90/R91 安全池排除招牌攻擊藥=平衡實作妥協非設定真理〔ES 正典台柱〕;R30 本就寫「放開→必過 sim」=暫禁非永禁。拍板:開攻擊藥 + 跑 sim 驗·smithing 永久擋)。**機制**:7 新「攻擊材料」(troll_fat/ogres_teeth/hawk_feather/bear_claw/nightingale_feather/brimstone/shade_cap)鏈式配對 → 開可釀 `fattr_strength` + `fskill_{blade,blunt,marksman,hand_to_hand,sneak,destruction}`(可釀池 ~21→~28·只剩 smithing 永久擋)。**🔴 7 新材料與既有 26 材料零共有(全 FAIL)→ 確定性 brew-outcome diff 證零既有配方變動**。取得:3 個 forage 事件各加**獨立**採集選項(7 材料野外可得·守 test_crafting·不犯 R90「一採全拿」)。**🔴 sim 驗(crux·非 byte-identical=刻意平衡)**:① `potion_buff` **取最強非疊加**(R30 `apply_buff`·同 kind+param 留最強單筆)→ 最壞=單瓶(strength~+9/skill~+12);② `sim_assassin` 新增 fortify-dosed 場景(apex_max + strength/sneak/blade +20 保守上界·含 fortify-alchemy 抬 brew factor 後單瓶 ~+15/+19):**solo boss 秒殺仍 0%**(SOLO_SNEAK_DAMAGE_CAP 為絕對夾·任何 fortify 量都夾不破·even +50 測仍 0%)、**精英 oneshot Δ0**(hit-gated 95.4%·非傷害 gated)、群戰死亡率反降(殺更快);③ `sim_builds` 探針:fortify-destruction+12 → mehrunes_dagon 0%→0%(法師 offense 牆不破)·古龍僅 +2pp。**🔴 smithing + mysticism 永久擋**(`_FORBIDDEN={fskill_smithing, fskill_mysticism}`):兩者皆「**永久逃逸**」〔喝藥做工藝→產物永久·藥退仍在〕——smithing→過頂 temper 永久武傷·mysticism→過強附魔永久(`enchant_magnitude` 讀 mysticism)·逃出「限時 + solo cap」結構(審查抓到 mysticism 同 smithing 類·已入禁)。**`sim_assassin` 既有場景 BYTE-IDENTICAL**(combat 數學零動·只新增驗證段·diff 證零既有行變)·零新存檔欄。**對抗審查(2 維·N agent)**:見 commit。`run_all` **93**(`test_alchemy_buffs` _FORBIDDEN→{smithing}·新增 R92 攻擊向可釀測試)。🔴 開攻擊向 fortify 必經 sim(`sim_assassin` fortify-dosed 場景守 solo 0% + 精英 Δ0·`sim_builds` 守法師牆);`potion_buff` 取最強非疊加(勿改成相加)→ 單瓶為最壞;**smithing/mysticism 永久擋不可開**(過頂 temper / 過強附魔皆永久逃逸);加攻擊材料純改 `ingredients.json`(與既有零共有)+ `events.json` forage(獨立選項) | re-sim, save |
| R91 | **煉金材料加厚 + 安全增益池續填(承 R90·使用者點名「先解決煉金深度」)**(R90 後使用者暫停下一步選擇·指出煉金深度不足〔且抓到 R90 野採「一採全拿」瑕疵·已後修拆獨立採集選項〕→ 拍板「材料加厚 + 續填增益池」,純資料最低風險)。**機制**:① 11 個既有材料**加厚至 4 效果(ES 對標)**;② 續填 **9 種安全增益**(`fattr_speed` + `fskill_{mercantile,speechcraft,security,athletics,block,light_armor,heavy_armor,scout}`)→ 可釀池 12→~21。**FAIL-pair 法**:每個新 kind 放在「目前零共有」的一對材料上 → 每 kind 恰一對「只共有它」(無 max-magnitude 遮蔽·無既有配方位移)。磁量 fattr 4/fskill 5。**取得零改**(加厚既有材料·本就在商店/野採)。**🔴 smithing 入紅線守門**(`fortify_smithing`→抬 temper cap→**永久**過頂武傷,比 strength 更狠且喝完藥仍留=非限時取捨)→ `_FORBIDDEN` 加 `fskill_smithing`。**🔴 確定性回歸 diff**(`PYTHONHASHSEED=0`·HEAD vs 工作樹·全 325 對 brew 結果):**恰 9 對 FAIL→buff·零既有 poison/cure/restore/buff 配方變動**(發現並排除一個 hash-seed nondeterminism 假陽性=等磁量 restore 平局 `max()` 依 hash 序·既有 latent·良性〔釀一次〕·已記不修)。**🔴 零新存檔欄**·**`sim_assassin` BYTE-IDENTICAL**(純資料·刺客不釀藥·隔離 worktree 證)·**零回歸**(只加效果·確定性 diff 鎖死)。**對抗審查(2 維 finder〔safe-pool 分類 + forbidden 完整性〕+ 逐發現驗證·9 agent)**:7 發現·**0 defect**(全 nit/駁回:9 kind 皆在偷襲/武傷鏈外·`fattr_speed` 只餵 extra-action 機率非傷害〔R63 sim 已驗 solo 0%〕·smithing 入禁正確·acrobatics 留 R92 sim pass 重分類)。`run_all` **93**(`test_alchemy_buffs` 擴 R91 9 kind 可釀 + smithing 入禁)。🔴 加可釀增益:FAIL-pair 法(每 kind 一「只共有它」對)·加完跑**確定性 brew-outcome diff**(固定 hash seed)證零既有位移;**安全池**=不餵 attack_damage/sneak_mult/temper-cap 者(strength/武器/sneak/smithing 仍 sim-gated→見 R92);brew 等磁量平局 `max()` 非確定性(latent·良性)。**前瞻 R92**:依 R30「放開先驗 sim」開攻擊向 fortify(力量/武器/潛行/毀滅)·sim 驗 solo cap 守住、夾幅度/時長·smithing 永久擋 | save |
| R90 | **煉金增益軸補完:餵飽 R30 限時增益藥引擎(純資料·8 新材料)**(重跑「下一步」評估排 #2、風險最低:R30 蓋了通用限時增益藥引擎〔`synth.brew_buff_id`/`synthesize` 任意 `fattr_/fskill_/resist_`·`potion_buff` 層套 `attr()/skill()/entity_resist()`·R32 揭露〕,但材料只餵得出 4 種可釀增益〔fattr_agility/willpower·fskill_alchemy·resist_magic〕=設計空間 ~35 只填 11%;**單元素抗藥 resist_fire/frost/shock 端到端接好卻零材料攜帶→不可達**〔補法師對元素 boss 太脆〕)。**純資料(R02)·引擎/synth/UI/R32 揭露/遷移全自動涵蓋任意 param·零邏輯改動**。**機制**:8 新正典材料(dragons_tongue/fire_salts/frost_mirriam/snowberries/void_salts/pearl/tundra_cotton/juniper_berries)各 2 效果·**刻意排成「每目標 kind 恰一對共有只它」**〔避 max-magnitude tiebreak 把某 kind 蓋掉=實作時抓到並修的 collision bug〕→ 新可釀:resist_fire/frost/shock + fattr_intelligence/personality/endurance/luck + fskill_restoration(加既有 4 種→可釀池 ~12)。磁量對標既有(resist 10·fattr 4·fskill 5)。取得:各入 ≥2 城 `merchant_stock`(imperial_city/whiterun/sentinel/markarth)+ 各補一省 forage 事件(cyrodiil/skyrim/hammerfell/highrock·過 `test_crafting` 野外取得守門 + 餵省委託)。**🔴 紅線(只靠資料缺席守·新增測試鎖死)**:可釀增益池**絕不含 `fattr_strength` / `fskill_{blade,blunt,marksman,hand_to_hand,sneak}`**(R30:不灌水偷襲/武傷鏈·`combat.py:477` 讀有效 sneak)→ 安全池只放 intelligence/personality/endurance/luck〔避 strength〕+ restoration〔非武器/非 sneak〕+ 元素抗。**🔴 零新存檔欄**(R32 `known_effects` 既有·新材料起始未揭露)·**`sim_assassin` BYTE-IDENTICAL**(純資料·零碰 combat/formulas·刺客不釀藥·隔離 worktree diff 空證)·**零回歸**(只新增材料·既有 brew/毒藥/restore/cure/揭露全不動)。**對抗審查(3 維 finder + 逐發現驗證)**:**0 發現·0 defect**(紅線/collision/byte/reachability 全清)。`run_all` **93**(新 `test_alchemy_buffs`:🔴 可釀池排除 strength/武器/sneak·新增益各可釀·既有 4 種無回歸·resist_fire 藥真降火傷·材料 ≥2 城可達)。🔴 加可釀增益純改 `ingredients.json`(材料配對·**每目標 kind 須有「只共有它」的一對**防 tiebreak 遮蔽)+ `world.json merchant_stock` + `events.json` forage(野外取得守門);**安全池絕不含 strength/武器技能/sneak**(放開→必跑 sim_assassin);新材料 byte-identical 免 sim(確認跑) | save |
| R89 | **戰士/法師公會功能化:rank-gated 招牌動詞(承 R88·完成核心公會冷階級梯三部曲)**(R88 讓盜賊公會拿到招牌動詞〔走私〕後,戰士/法師公會仍只有 flat % 折扣=冷;使用者「下一步」拍板補齊,**沿用 R88 已在 code 的 `action_guild_hall` rank-gated 服務模式**〔`smuggle_ok` 鏡像〕;動詞先前 AskUserQuestion 已定案)。範圍**只 fighters/mages**(thieves R88 已做·companions/dark_brotherhood 早有功能 perk 不碰);**比 R88 更小**:無新地點/陣營/任務/委託/脈動·純 `action_guild_hall` 服務 + 1 smithing 參數 + 1 enchanting helper。**機制**:① **戰士=軍械庫淬鍊**(`FORGE_RANK=2`):公會供料**免材料**淬鍊·新 `_guild_armory_temper`(比照 `action_temper` 但不查 location armorer + `guild_free=True`)·`smithing.temper(...,guild_free=False)` 新參數〔`free=guild_free or rng…`〕+ **`can_temper(...,guild_free=False)` 跳過原料閘**〔🔴 否則 can_temper 的「缺錠」檢查會擋掉公會免費淬鍊·實作時抓到的真 bug〕·**受 `effective_temper_cap` 夾**(無迴圈·temper 零材料產出);② **法師=奧術服務**(`ARCANE_RECHARGE_RANK=2` 回充 / `ARCANE_SUPPLY_RANK=3` 補給):新 `_arcane_services` → 免靈魂石回充充能型附魔武器〔新 `enchanting.recharge_full`+`charge_capacity` helper·`action_recharge_enchant` 的 `_cap` 改呼叫共用〕+ 補滿 `minor_magicka_potion`〔value18〕至 `MAGE_POTION_SUPPLY_N=3`(鏡像 R52 lair supply·**售賣套利嚴格弱於已上線的 R52 治療藥水 value40 補給先例**)。`action_guild_hall` 加 `forge_ok`/`arcane_ok`(faction+rank·**早退守衛擴含 `and not forge_ok and not arcane_ok`** → ranked 戰士/法師無晉升任務仍留選單)+ 選項 + dispatch;常數放 `SMUGGLE_RANK_*` 旁。**🔴 零新存檔欄**(weapon_temper/armor_temper/enchant_charges/factions/inventory 皆既有)·**`sim_assassin` BYTE-IDENTICAL**(`guild_free` 預設 False → temper/can_temper 既有路徑〔含 rng 抽序短路〕逐位元組同·零碰 combat/formulas·sim 是 dark_brotherhood 非 fighters/mages·走自有 `_round` 永不入公會大廳·隔離 worktree diff 空證)·不限省份(一般公會服務)。**反套利**:免費淬鍊受 cap 夾且 temper 不產材料(drop+rebuy 仍付物價+時間+體力·同既有「順手練技能」非金幣泵)·回充由使用消耗·魔力補給有上限低值。**對抗審查(3 維 finder + 逐發現驗證)**:2 發現·**1 確認皆 nit·0 defect**(`_guild_armory_temper` 以 `if res["ok"]` 守 `advance(hours)`〔hours==0 時 advance no-op·與 action_temper 等價〕=無害風格分歧;魔力補給 faucet 經驗證嚴格弱於 R52 先例 → 駁回)。`run_all` **92**(新 `test_guild_services`:rank 閘/免錠淬鍊/正常淬鍊仍需錠〔byte-safe〕/cap 夾/回充至滿只充能型/補給有上限)。🔴 加公會招牌服務沿用此模式:`action_guild_hall` faction+rank 旗標(早退守衛須含之)+ 服務 helper;**`smithing.temper`/`can_temper` 的 `guild_free` 預設 False 不可動**(守 byte-identical);免費服務須有自然上界(temper cap / 充能消耗 / 補給上限)防套利 | save |
| R88 | **斯庫瑪走私生意:整併入盜賊公會(高階解鎖·月糖精煉 sink + 跨省走私委託)**(評估最淺終端冷迴圈=斯庫瑪〔`skooma.py` 只是 dose→high→withdrawal→cure 開關·零陣營/製作/走私〕;使用者選方向後**再點名整併**→ 併入盜賊公會〔非獨立幫派〕,一石二鳥補活「盜賊公會冷階級梯〔只 `sell_bonus` 折扣〕」+「斯庫瑪淺」·**不增第四個犯罪 silo**·亢奮維持戰鬥中性〔只速度/敏捷/意志·不碰武傷/偷襲〕)。**整併三層犯罪**:盜賊公會=組織犯罪主幹〔走私=招牌高階動詞〕·R84=無黨派亡命徒層·斯庫瑪=盜賊公會的艾爾斯維爾走私線(鐵證 `crime.py FENCE_BONUS_BY_TIER` 註解「對標盜賊公會 0.40」=當初刻意平行不互通)。**機制**:① `alchemy.refine_skooma`(新函式非 brew·`SKOOMA_REFINE_COST=5` 月糖→1 斯庫瑪·`SKOOMA_REFINE_SKILL=20` 門檻·練 alchemy·**無產量階級加成**);② `main.action_guild_hall` thieves rank-gated「🌙 斯庫瑪走私生意」子選單(`smuggle_ok`=thieves 會員 + `province=="艾爾斯維爾"` + `rank≥SMUGGLE_RANK_1=2`·**早退守衛含 `not smuggle_ok`**〔ranked thief 無晉升任務仍留選單〕·複用 circle_recruit 旗標模式)→ `_skooma_smuggling`(精煉 + 委託)·`_smuggling_contracts`(列 worldpulse 聚光 `scomm_`·`_SCOMM_MIN_RANK` 把 riften/wayrest 閘 `rank≥SMUGGLE_RANK_2=4`〔兩段 gate〕)·`action_board` 排除 `scomm_`(同 `ucomm_`);③ 4 `scomm_*` 走私委託(`quests.json`·repeatable·source board·**collect moon_sugar×N → reach 遠省 TG 城**〔運出貓人故土·途中複用 R84 賞金獵人風險·collect 階交貨即消耗 quests.py:241〕·reward⊆{gold,infamy});④ `pulse_elsweyr_smuggle`(`world_pulse.json` 聚光 4 scomm_·修審查「pulse 只聚光 generic kills」旗標)。**🔴 反印鈔機(設計階兩位審計獨立抓 BLOCKER→修)**:不加寬銷贓售價(thieves `sell_bonus` 不動)·精煉嚴格 sink〔反套利不等式 `SKOOMA_REFINE_COST×moon_sugar.value(25) ≥ skooma.value(120)` → 5×25=125≥120·而 `world.sell_price` 對兩物同乘 `(0.3+0.5d)(1+sell_bonus)` → 精煉/買進再賣在**任何議價/銷贓階皆非正 EV·即使月糖免費撿**〕·走私窩不設一般銷贓·利潤只走固定報酬委託·**套利回歸測試鎖門**。**🔴 零新存檔欄**(會籍=`char.factions["thieves_guild"]`·成癮=`skooma_addiction`·委託=`char.quests`/`completed_quests`·脈動=`world_pulse_day` 皆既有)·**`sim_assassin` BYTE-IDENTICAL**(零碰 combat.py/formulas.py·走私全在 hub 子選單/委託·sim 走自有 `_round` 永不入·隔離 worktree diff 空證)。**對抗審查(4 維 finder + 逐發現驗證·13 agent)**:9 發現·**3 確認皆 nit·0 defect** —— 精煉 +1 rounding 套利**不可達**(refiner 必 rank≥2 → sell_bonus≥0.14 消除 rounding band·非會員開不了精煉)、constraints clean、修 1 cosmetic typo(柯林斯→科林斯)。`run_all` 91(新 `test_skooma_smuggling`)。🔴 加走私委託純改 `quests.json`(scomm_·collect moon_sugar→reach·reward⊆{gold,infamy})+ `world_pulse.json` 聚光;精煉比例 N 守反套利不等式(調必重跑 test 套利斷言);rank/省份閘在 `action_guild_hall`;**絕不加寬銷贓售價**;前瞻=戰士/法師公會同類功能化(免材料淬鍊/奧術回充)沿用「rank-gated 招牌動詞」模式 | save |
| R87 | **敵方支援施法者:敵陣治療/護盾 AI(對稱 R86)**(承 R86 前瞻③;使用者拍板 **威脅但可破解** + **主題法系/祭司怪散布各階**)。**機制(`magic.py`·把 R86 helper 一般化)**:抽**陣營無關 core** `_support_act(caster, spells, pool, gd, exclude_from_empower=(), power=1.0)`(R86 決策搬入·empower 排除集取代寫死的 `is not player`·新 `power` 幅度因子);`companion_support_act` 改寫為 wrapper(`pool=[player]+allies`·`exclude=(player,)`·`power=1.0` → **R86 行為逐位元組不變**);**新 `enemy_support_act(enemy, enemies, gd)`**(`pool=存活敵人含 self`·`exclude=()`·`power=ENEMY_SUPPORT_POWER=0.35`·spells 讀 `bestiary[tid]`)。**接線**:`main.py` enemy phase(:1015)攻擊前試 `enemy_support_act`·`res is not None` → `ui.message`(cyan)+`continue`(**施法換損該回合攻擊**=自平衡)。**資料(`bestiary.json`·5 caster 怪散各階)**:moor_witch `heal_other`·glenmoril_witch `regen_aura+heal_other`·goblin_shaman `heal_other`·corrupt_priest `ward_ally+heal_other`·necromancer_acolyte `ward_ally`。**🔴 sim 發現:敵方 rally 反效果**(施法者換掉自身 ~13 攻擊·換到的 power-scaled empower 對小隊太弱 → 敵 rally 反讓玩家更易勝〔baseline 15%→施法 37%〕)→ **棄敵方 empower·改 heal/shield**(真「先點治療者」威脅)。**🔴 平衡(擴 `sim_party.py`·中階戰士 vs 含治療者敵群)**:`ENEMY_SUPPORT_POWER=0.35`(敵治療 50×.35≈18·不全補小血量怪);**無 stalemate**(逾時 0%)·trash 仍可殺·**先點治療者破解**(goblin 群 baseline 94%→忽略 59%→先點 84%);敵治療施法因子 sim 調校(0.35)。**🔴 sim_assassin/sim_builds BYTE-IDENTICAL**(隔離 worktree diff 空·含 4-bandit/2b2w 群戰場景·未碰 combat.py/formulas.py·`enemy_support_act` 只由 `main.run_battle` 敵方階段呼叫·sim 走自有 `_round`/1v1 永不呼叫·**sim-touched 怪堅持無 spells**)。R86 一般化後 `test_companion_cast` 全綠(同伴治療仍 50 給玩家)。**零新存檔欄**(`spells`=gamedata·`support_cd`=暫態)。`run_all` 90(新 test_enemy_support:敵治療最低他敵/門檻/冷卻/幅度因子/sim-touched 無 spells/empower 排除集);run_battle 煙霧(敵女巫治療隊友·無 crash)。🔴 加 caster 怪純改 `bestiary.json spells`(**絕不給 sim-touched 怪**·敵方避 empower〔反效果〕重 heal/shield);調 `ENEMY_SUPPORT_POWER`/threshold/cooldown→跑 `sim_party`(守無 stalemate/可破解);動 helper/enemy-phase→跑 sim_assassin(守 byte-identical) | re-sim |
| R86 | **同伴戰鬥施法:輔助型同伴在戰鬥中各施其職(角色感知支援)**(評估揪出最大「死 build」=13 同伴全 `spells=null`·戰鬥中只 `choose_attack→resolve_attack` 純近戰·盟友治療術 7 處有售卻無實體能施·sim_builds 全程 `allies=[]` 從不模擬同伴 → 治療型同伴是死內容;使用者點名「騎士這種輔助型 NPC 也該用他該用的法術」→ **角色感知支援**·拍板**輔助但不破關**)。**機制(`magic.py`·複用 `_apply_to_allies`·🔴 不碰 player-only 的 `magic.cast`)**:新 `companion_support_act(companion,player,allies,gd)` —— 目標池=`[player]+存活同伴`(**含玩家**·治療/護盾照顧英雄;player 是 Character 但同介面);**① 反應式治療**(heal/regen·池中 HP 比 < `COMPANION_HEAL_THRESHOLD=0.55`)優先 **② 主動式增益**(shield/empower·目標缺該 buff);**固定 power=1.0**(不吃智力/裝備·同伴支援不隨玩家成長爆走);冷卻 `support_cd`(active_effects·`tick_effects` 遞減·`COMPANION_SUPPORT_COOLDOWN=2`);**與 capstone 光環/自身去重**(掃 active_effects 同 kind·empower 玩家被 combat `not _is_player` 守門→排除→rally 只益同伴)。**資料**:4 輔助同伴角色感知 `spells`(`companions.json`)—— hedge_mage `heal_other`/drelas `regen_aura+heal_other`(法系治療)·shieldmaiden `ward_ally`(盾衛護盾)·veteran `rally`(領袖激勵)。**接線**:`main.py` ally phase(:994)攻擊前試支援·`res is not None` → `ui.message`+`continue`。**🔴 平衡(新 `sim_party.py` 驗·輔助但不破關)**:**施法 vs 純近戰(同隊伍)邊際 +0~8%**(3 同伴打終王 96% 中 88% 來自 4v1 隊伍規模·施法僅 +8%)·**無 stalemate**(逾時 0%)·單同伴終王仍難(14%→30-51%)。**🔴 sim_assassin/sim_builds BYTE-IDENTICAL**(隔離 worktree diff 空·未碰 combat.py/formulas.py·helper additive sim 不呼叫·ally-phase 改動只在 `run_battle`〔sim 走自有 `_round`·`allies=[]` 從不入 ally phase〕)。**零新存檔欄**(`spells`=gamedata·`support_cd`=暫態 active_effect)。`run_all` 89(新 test_companion_cast:治療含玩家/門檻/冷卻/護盾含玩家·激勵排除玩家/去重/優先序);run_battle 煙霧(同伴治療玩家·無 crash)。🔴 加輔助同伴純改 `companions.json spells` 欄(helper 通用·heal 反應式/shield+empower 主動式自動分流);調 threshold/cooldown/幅度→跑 `sim_party`;支援不碰玩家偷襲(empower 守門·固定 power);動 helper/ally-phase→跑 sim_assassin(守 byte-identical)+sim_party | re-sim |
| R85 | **湮滅危機尾聲:英雄/叛徒的餘暉(依結局分流的傳奇收尾·純 JSON)**(評估「死系統/敘事死路」揪出主線高潮〔弒達貢〕後世界立刻沉默——`oblivion_crisis_ended` 旗標僅被自身 2 條任務 reward 端用、零下游內容,且雙結局〔英雄 `main_oblivion` vs 叛徒 `md7`〕除數值外零敘事差別;使用者拍板主題=英雄/叛徒餘暉〔傳奇收尾〕,刻意不做權力真空新機制/餘孽肅清戰鬥向)。**2 條結局分流收尾任務(`quests.json`·純資料)**:皆 `source:"main"`(勝利後自動上告示板·複用 `available_quests("main")`)+`requires_event:"oblivion_crisis_ended"`(world_events_fired gate)+`requires_quest:main_oblivion`/`md7`(completed_quests gate·**兩結局互斥→每位玩家只見一條**);目標走 **`reach`**(無新戰鬥·敘事張力靠 text)——`epilogue_hero`「救世之後」(reach imperial_city→kvatch·受諸省擁戴)、`epilogue_apostate`「使徒餘影」(reach imperial_city→cheydinhal·挾達貢殘力被畏懼拉攏)。**2 傳奇誓福(`boons.json`·R45 通用層·`reward.grant_boon`→`boons.grant`)**:`kvatch_hero`「救世主之譽」〔personality/endurance+restoration/speechcraft·**補英雄結局原缺的永久誓福**=平衡叛徒的 dagon 誓福〕、`dagon_apostate`「使徒之影」〔willpower/personality+illusion/mysticism·**刻意與 dagon 誓福不同軸**避冗餘〕·**🔴 守 R45 紅線:無 strength/sneak/武器技能**。**敘事橋接第二幕**(完成 text 指向既有 `politics.pledge`/`_pledge_allegiance`/`aiwar` 危機後內戰·**純敘事零機制改動**)。**🔴 零新存檔欄**(複用 world_events_fired/completed_quests/boons/既有 reward 管線)·**sim_assassin BYTE-IDENTICAL**(隔離 worktree diff 空·純 JSON 未碰 combat/formulas·誓福 fixtures boons=[] 加 0 恆等·sim 不跑 quests)。`run_all` 88(新 test_epilogue:結局分流 gate/誓福紅線/grant/schema·`reach` 排序避「接取即過階」靠首階指 imperial_city)。🔴 加結局收尾純改 `quests.json`(source:main·requires_event+requires_quest 分流·reach 階·grant_boon)+`boons.json`(R45 schema 守紅線);誓福幅度超戴德拉對標=平衡取捨**先問**。**前瞻**:結局內二選一終局抉擇(複用 R45 branches)/機制性接入內戰(新 allegiance reward key)/theme C 餘孽肅清(戰鬥向·需 boss 地城)留後續 | save |
| R84 | **犯罪/通緝系統深化:賞金獵人路途追殺(威脅)+ 亡命徒地下世界(回報)·雙軸通緝身份**(評估死系統揪出 crime.py 機制完整但純懲罰〔無路途後果/無亡命回報/惡名近擺設/高賞金不影響商店態度〕;使用者拍板完整切片=威脅+四回報元素·雙軸驅動·替換式可清追殺+保守經濟)。**雙軸純衍生身份(`crime.py`·零存檔欄·只吃 char 純函式·階梯複用 lycanthropy.tier `sum(threshold)`)**:`active_heat`〔由最高省賞金·**可清**·驅動路途追殺·`ACTIVE_HEAT_THRESHOLDS=(80,300,700)`〕/`outlaw_standing`〔由 `infamy`·**終身**·驅動地下身份·`(5,20,50)`〕/`is_outlaw`〔outlaw≥1 或有賞金〕/`notoriety_title`〔通緝犯→惡名昭彰→江湖鬼影〕/`fence_bonus`〔{0,.15,.30,.45}·上限0.45〕。**威脅側**:`_bounty_hunter_ambush`(鏡像 `_curse_manhunt`)掛 `_travel_to`·**替換式**(觸發則 foe=None 取代該趟一般遭遇·一趟一戰)·tier 隨 active_heat〔1-3 名〕·chance=`min(.75,.35+.12*(heat-1))×(1−mounts.encounter_evade)`〔騎馬甩開〕·走 `offer_battle` 保偵查/潛行撤退·**存活不加賞金/惡名**(自衛·保可清契約)·無新冷卻欄;4 新賞金獵人 bestiary〔bounty_hunter/mercenary_tracker/master_hunter/city_captain·**`min_level:99 weight:0` spawn-only**·群體型非 solo〕;城門 T3 升級〔省賞金≥700→`guard_confrontation` 一名換 city_captain·**城門讀省賞金/路途讀全域 active_heat 刻意分流**〕。**回報側(近純資料)**:3 亡命徒藏身處〔`refuge:"outlaw"`·wilderness danger1→guard/manhunt 天然不觸發=內建安全區·**手填 pos/links 守 R28**雙向/省內/degree≥2/biome〕·`action_refuge`〔is_outlaw 閘·休息/密窖 `_stash_transfer`/銷贓/委託〕·`action_fence`〔贓物加價賣 `fence_bonus`+黑市買·**🔴 防套利地板 `buy=max(buy_price,fence_sell+1)>sell`** 比照 R33·賣價加價只在 action_fence 算·**不動全域 `sell_price`**→其餘呼叫端 byte-identical〕;6 `ucomm_*` 可重複地下委託〔source board·reward `{gold55-90,infamy1-2}`·**無 fame**·objective kill·**前綴分流**:action_board 排除 `ucomm_`/refuge 只列 `ucomm_`〕·3 地下脈動聚光之〔複用 R79 board+pulse·反刷免費〕;codex 擴 `crime_bounty` 條目。**🔴 零新存檔欄**(複用 bounty/infamy/house_stash/world_pulse_day/completed_quests)·**sim_assassin BYTE-IDENTICAL**(隔離 worktree diff 空·未碰 combat/formulas·新怪 spawn-only 不進 random 池/不改 bandit·ambush/refuge 非 sim 路徑)·15-agent 對抗審查〔經濟套利/determinism/byte/地理/邏輯〕。`run_all` 87(test_crime 擴雙軸·新 test_crime_depth)。🔴 加賞金獵人純改 bestiary(spawn-only)+ambush 讀 active_heat;加藏身處純改 `world.json`(refuge 欄·**手填 pos/links 非 geo_rebuild**)+action_refuge;加地下委託純改 `quests.json`(`ucomm_` 前綴·source board·reward⊆{gold,infamy})+world_pulse 聚光;**fence 防套利地板恆守**;賞金獵人 tier/chance 動 → 平衡取捨**先問** | save |
| R83 | **出生星座深化:5 個冷星座補主動威能 + 被動天賦(完成 R61 明列「星座留後續」)**(評估 workflow〔7 lens·8 agent〕揪出最強下一步=星座深化〔13 座只 8 座有主動威能·戰士/法師/竊賊/淑女/駿馬 5 座純創角加值零跑時分支〕·dev 親口延後·R61 種族威能藍圖現成;使用者拍板方向=星座深化)。**架構=R61 種族同構**:三大守護星各補一招主動威能填入既有空 `powers: []`(走 `powers.POWERS`/`use()` 既有管線·冷卻共用 `power_last_day`·零新槽);竊賊/駿馬走持續被動(新 `systems/birthsign_ability.py` 即時讀 `birthsigns[sign].passive`·鏡像 `race_ability.py`·非該座回中性·零快取/零存檔/零遷移)。**3 主動(use() 加 3 effect 分支)**:戰士座 `warrior_fury`〔combat·`self_empower 0.20/3t`·**複用 R61 `berserk_buff`**·combat._self_empower 讀→power_bonus 偷襲倍率後相加守紅線〕、法師座 `magicka_surge`〔combat+utility·新 `restore_magicka` 補滿法力〕、淑女座 `ladys_grace`〔combat+utility·heal70+新 `restore_fatigue`+既有 `cure` 淨化 DoT〕。**2 被動**:竊賊座 `stealth_aptitude 0.15`〔潛行系 spec 技能 learn-by-doing ×1.15·`progression.use_skill` spec mult 後讀·獨立於 R62 職業專精〕、駿馬座 `swift_travel 0.15`〔旅行耗時係數減量·`world.travel` 比照運動/坐騎/同伴識途減項·夾 floor 0.5〕。**🔴 by-design 有界 note**:戰士座+獸人/紅衛兩個獨立每日威能各加一道 berserk_buff → `_self_empower` SUM ≤0.40〔只兩 berserk 源·有界·gate 兩道每日冷卻·非 lever 變更〕。**🔴 sim_assassin BYTE-IDENTICAL**(隔離 worktree vs HEAD 逐位元組證:未碰 combat.py/formulas.py·sim 刺客=陰影座永不用此 5 招·`use_skill`/`travel`/`powers.use` 不在 sim 路徑·被動非該座讀回 0/中性)。**零新存檔欄**(主動冷卻複用 `power_last_day`·被動即時讀 gamedata·星座=gamedata 唯讀非 char 狀態)。`run_all` 86(新 test_birthsign_abilities·test_m8 `test_power_id_per_sign` 更新:warrior/mage/lady 改有 pid·thief/steed 仍 None〔走被動〕)。🔴 加星座主動純改 `birthsigns.json powers[]`+`powers.POWERS`(控場必走 `magic.apply_control` R44)·加被動純改 `birthsigns.json passive`+`birthsign_ability` helper(絕不寫 base·非該座回中性);動 self_empower/combat 讀取路徑→跑 sim_assassin | re-sim, save |
| R82 | **套話/秘密 功能化(修死功能 + 撬出可行動情報)**(承「評估死系統 → 深化社交軸」,使用者**取消派系聲望方向**、改走套話/秘密)。**🔴 揪出更深的死 bug**:秘密存於 `dialogue.json` 的 `npcs[id].secret` 覆寫層,但 `dialogue._do_pump` 誤讀 `gamedata.npcs[npc_id].get("secret")`(=npcs.json·恆 None)→ **既有 8 個秘密永遠讀不到、套話永遠只吐預設旁白(等同死功能,audit 誤判為「功能」)**。本輪正讀 `gamedata.dialogue["npcs"][npc_id]` 覆寫層 → 8 秘密真正揭露。**功能化(小邏輯)**:`secret` 由純 str(flavor)擴為可為 `{text, effects}`;`_do_pump` 成功時套用 effects 走既有 `_apply_topic_effects`→`events.apply_effects`(撬出**藏寶** gold/item·**線索** start_quest),**一次性兌現**靠 `char.dialogue_done[npc_id]` 加 `_PUMP_PAID="__pump_paid__"` 標記去重(再套話只重述不重發·**零新存檔欄**)。**內容(資料)**:15 功能化秘密(10 藏寶 gold 50-70/item lockpick·steel_dagger·ruby·soul_gem·5 線索 start_quest)+ 5 條 `source:"npc"` 秘藏地城任務(複用 R81 未覆蓋的 wendir/kogoruhn/fort_ontus/vindisi/rootwater_grotto·NPC↔地城同省·無 visible gate)+ 8 既有 flavor 秘密。**guards(僅 tests)**:secret schema(str 或 {text,effects})·effect 型別 ∈ `_DLG_EFFECT_TYPES`·start_quest∈quests·item∈items·**reachability 納入 secret start_quests**(秘藏任務防孤兒,擴 R80/R81 guard)。**防刷**:套話付 speechcraft practice(體力+時間)·`_PUMP_PAID` 一次性·reward 走既有不破經濟。**🔴 sim_assassin BYTE-IDENTICAL**(隔離 worktree vs HEAD 證:純社交/內容·sim 不 import dialogue·不套話·零碰 combat/formulas)。**零新存檔欄**(藏寶→gold/inventory·線索→quests·去重→dialogue_done)。`run_all` 85(新 test_secrets:bug 修正/藏寶 once/item/線索/失敗不兌現/schema)。🔴 加功能化秘密純改 `dialogue.json`(`npcs[id].secret`={text,effects}·effects⊆events 支援集·item/start_quest 須 FK 成立)+ `quests.json`(秘藏 source:npc·NPC↔目標同省·無 visible gate);秘密讀 dialogue 覆寫層非 npcs.json;一次性走 `_PUMP_PAID` 標記·動 _do_pump→跑 run_all(免 sim·byte-identical) | save |
| R81 | **NPC 社交叢集復活:role 身份化 + 流言線索(混合)+ 好感回報**(承使用者「評估當前的死系統(ex:流言)」→「評估如何復活·連動系統一起」;死系統審計揪出最大「擺著好看不動」區=NPC 社交叢集:`rumor`〔180 NPC 都有·攀談顯示「傳聞:…」但純文字零功能·註解承諾「指向地城/野外/奇景」卻零兌現〕冷、`role`〔dialogue.py:323 邏輯在但 0 NPC 有 role 值·roles{} 空〕真死)。使用者拍板:範圍=**整個社交叢集**、流言機制=**兩者混合**(部分傳聞→線索任務·部分→即時揭露地標)。**~95% 純資料**(role 已全接線、對話 topic effect 已委派 `events.apply_effects` 支援 start_quest/heal、`landmarks.discover` 可複用),唯一新邏輯=`dialogue.offered_rumor` helper(鏡像 `offered_quest`)+ `main.action_talk` 一個「追問傳聞」分支。**Phase A role 身份化(純資料)**:`dialogue.json` 填 `roles` map(6 role:merchant/blacksmith/priest/guard/scholar/innkeeper)+ role 話題定義(身份 flavor·say_by_rel 按 relationship 上色·部分 min_disposition gate·priest `blessing` heal20/once 為唯一功能話題);`npcs.json` 給 52 個 NPC 補 `role`(`topics_for`:323 已自動併入·main 無需改)。**Phase B 流言線索(混合)**:`offered_rumor`(好感 ≥`rumor_disposition` 時回 `{kind:quest}`〔`rumor_quest` 未 active/done 且 clear_dungeon 未清〕或 `{kind:landmark}`〔`rumor_landmark` 未 discovered〕);`action_talk` 加「追問傳聞」→ quest 走 `_accept_and_brief`、landmark 走 `landmarks.discover`;`npcs.json` 給 42 NPC 補 `rumor_quest`(15 條共享線索·威脅型)、8 NPC 補 `rumor_landmark`(指路型)+ `rumor_disposition`;`quests.json` 加 15 條 `source:"rumor"` 一次性 clear_dungeon 線索(provinces 在地化·modest gold/fame·`available_quests` 依 source 過濾不漏進告示板)。**Phase C 好感回報**:disposition 原幾乎只 gate 任務 → 新增 3 payoff(解鎖流言線索 rumor_disposition·role 身份話題 min_disposition·既有 secret/套話);折扣否決(碰 shop/新 effect/新存檔欄)。**guards(僅 tests)**:放寬 `_DLG_EFFECT_TYPES`=`{faction_standing}∪events 支援集`(容 blessing heal)·role FK(roles 值∈topics·npc.role∈roles)·rumor FK(rumor_quest∈quests 且 source==rumor·rumor_landmark∈landmarks·rumor_disposition 0-100 int)·**source:rumor 可達性**(npc.rumor_quest 回指防孤兒)·線索目標 location 無 visible gate(可接去不了)。**🔴 sim_assassin BYTE-IDENTICAL**(隔離 worktree vs HEAD 證:純社交/內容·sim 不 import dialogue/npcs/quests/landmarks·零碰 combat/formulas)。**零新存檔欄**(rumor 線索→quests/completed_quests·地標→discovered_landmarks·role once→dialogue_done·好感→npc_disposition·新 NPC 欄=gamedata 唯讀)。防刷(追問傳聞一次性鎖·blessing once·線索 reward 只純量)。`run_all` 84(新 test_rumor_clues;test_data_schema 擴)。🔴 加 role 純改 `dialogue.json roles/topics`+`npcs.json role`;加流言線索純改 `npcs.json`(rumor_quest/rumor_landmark/rumor_disposition)+`quests.json`(source:rumor·僅 clear/reach·目標無 visible gate)·NPC↔線索目標須同省·新 source:rumor 任務務必有 NPC rumor_quest 回指(否則 reachability guard 攔=孤兒) | save |
| R80 | **任務生態活化:委託多樣化/在地化 + NPC 一次性任務(城鎮 + 野外)**(承 R79 回饋「任務太少而且太泛用」;使用者拍板 ① 委託加 4 型別 + 強化在地性 ② 不只調聚光,**加非重複任務由 NPC 派發 + 讓野外出現 NPC**)。**評估結論=全可純資料,複用既有管線、零產品邏輯**(唯一 Python 改動=`tests/` guard)。**Phase A 委託大改**(`quests.json`+`world_pulse.json`):8 泛用 kill → **24 個四型別在地委託**(8 kill-雜怪低·8 kill-精英〔複用既有 elite frost_troll/bog_troll/senche_tiger/minotaur/lamia/giant_spider/barrow_sentinel/dremora·賞 80-100 仍<一次性 120-480〕·3 collect-供應〔消耗掉落材料 glow_dust/nightshade/spider_egg〕·3 collect→reach 供應運送〔送達城鎮·reach 只做第二階段〕·2 kill+kill 多階段雜役);綁各省 lore(腹地/雪原/灰原/黃沙/荒沼/綠約/月糖/沼澤);脈動 `spotlight_quests` 1→**3/省**(激增=豐富在地小榜)。**Phase B 城鎮 NPC 一次性**(`npcs.json`+`quests.json`):12 個無任務 NPC 補 `quest`/`quest_disposition` 欄 → `source:"npc"` 一次性(kill/collect·在地·完成進 completed_quests 永久鎖·可帶 items 較豐獎勵);複用 `dialogue.offered_quest`(好感 gate 60)→`action_talk`→`_accept_and_brief`,零邏輯。**Phase C 野外遊蕩 NPC**(`events.json`+`quests.json`):8 個隨機遭遇(travel/explore/rest·`provinces` 在地化·`location_types:wilderness`)= 遇傷獵人/拾荒人/信使/難民/採集人/貓商/亞龍人…→ 選項 `start_quest` 接一次性任務(`requirements:{quest_available}` 去重·複用 merchant_caravan/campfire 範式),8 個 `wq_*` 一次性;零邏輯。**世界靠三管線活化**:告示板委託(可重複·聚光·多型別·在地)+ 城鎮 NPC(一次性·常駐)+ 野外 NPC(一次性·隨機)。**guards(僅 tests)**:repeatable 首階段≠reach(防接取即在目標地自動完成)·`npcs.json` quest 欄 FK·`events.json` start_quest FK·**source:npc 任務可達性**(npc.quest∪event start_quest∪companion recruit_quest·防孤兒永不可接)。**🔴 sim_assassin BYTE-IDENTICAL**(隔離 worktree vs HEAD 證:純內容·零碰 combat/formulas·**無新怪**〔精英複用既有 bestiary〕·新任務走既有 record_kill/spawn/run_battle·sim 不跑 quests/events)。零新存檔欄(複用 char.quests/completed_quests〔一次性鎖〕/world_pulse_day〔R79〕)。反 min-max:repeatable<一次性同難度·collect 報酬≤材料買價·精英報酬中等。`run_all` 83(test_data_schema 擴 guard;test_repeatable_quests/test_worldpulse 沿用)。🔴 加委託純改 `quests.json`(repeatable+低 gold/fame+僅 kill/collect·首階段≠reach)·加 NPC 任務純改 `npcs.json`(quest 欄)+`quests.json`(source npc)·加野外遭遇純改 `events.json`(start_quest)+`quests.json`;sim byte-identical 免跑(動 combat/formulas 才需) | save |
| R79 | **常態世界脈動:動態世界新聞層 + 聚光激增可重複委託(治後期任務泉枯竭)**(評估 workflow 揪出最大未碰洞=靜態內容打完即枯竭〔worldstate once-fire 主線後永久靜默·告示板一次性可窮盡·事件無後期再生〕跨 exploration/quests/events 三 cluster 同根;使用者拍板 方向=常態世界脈動·兩追加:① 可重複委託獎勵=**固定低額**〔非 runtime 遞減,JSON 授低 gold/fame〕② 耦合=**聚光激增 gate**〔委託只在新聞啟動該省情境的 active window 期間現身〕)。**新 `systems/worldpulse.py`+`data/world_pulse.json`(8 脈動·分散八省)**:`update` 掛 game_loop **worldstate 之後**(「四方傳聞」橫幅·cyan)·每日至多評估一次(`pulse_eval_day` 哨兵)·低機率(`GLOBAL_CHANCE 0.5`)·**決定性**(R24:`sorted`+`state.rng`·迭代序不餵 rng·兩 rng 決策點=是否廣播+加權挑選)·**active window 純由 `world_pulse_day` 推導**(`0≤today−last<active_window_days`·零額外狀態欄·鏡像 moons.py)。**關鍵簡化=單一冷卻**:可重複委託**永不進 `completed_quests`**(`is_done` 恆 False)→ 能見度全由聚光 gate 控·脈動 `cooldown_days`(兩激增最短間隔)+`active_window_days`(激增持續期)即唯一冷卻·**不做 per-委託冷卻/不加 repeat_done_day 欄**。**委託(quests.py 極小面)**:`available_quests` +`day` 參數(=`worldpulse.day_index`〔**開局後天數**·與 world_pulse_day 同基準·min_day 防序章洗版才正確〕)+ board 分支聚光 gate(`source=="board"`·repeatable 需 province+day+被聚光否則隱藏·**防 province/day 缺漏 ungated 洩漏**);`_complete` 末尾 `if not q.get("repeatable"): completed_quests.append`(獎勵派發**一字不改**·repeatable 讀 JSON 低 reward 無縮放)。**8 可重複委託**(`comm_*`·`repeatable:true`·**省略 provinces**〔能見度由脈動 province 定〕·**只 kill 目標**〔clear_dungeon 因 cleared_dungeons 永久會即時重複達標=免費金,schema guard 擋之〕·複用既有 creature·低額 40–60 gold/fame≤2)。**schema guard**:`_REPEATABLE_REWARD_KEYS={gold,fame,infamy}`〔絕不帶 items/spells/boons/companion〕+ repeatable objective≠clear_dungeon + world_pulse schema lint(province∈八省·spotlight_quests 指真實 repeatable board 委託·cooldown≥window)。**已接取委託不因視窗過期失敗**(過期只移出可接清單)→ 零 auto-fail。**🔴 sim_assassin BYTE-IDENTICAL**(隔離 worktree vs HEAD 證:零碰 combat/formulas·`_complete` 獎勵路徑不變·sim 不跑 game_loop/quests·新模組不在 sim 路徑)。**2 新存檔欄**(`world_pulse_day` dict·`pulse_eval_day` int·dataclass 預設 + `ensure_pulse_fields` 冪等遷移·R03)。`run_all` 83(+test_worldpulse·+test_repeatable_quests·test_data_schema 擴)。🔴 加脈動純改 `world_pulse.json`(R02)·加可重複委託純改 `quests.json`(repeatable+低 gold/fame+kill 目標)·決定性走 `sorted`+`state.rng`·day 一律 `worldpulse.day_index`·動 worldpulse/委託引擎→跑 run_all(免 sim·byte-identical) | save |
| R70 | **偷襲倍率全面重構:基倍效益遞減 + 各因子下修**(承「偷襲倍率全情境評估」apex ×11.33·多被 cap/overkill 吸收;使用者拍板五項齊改)。**純改 `formulas.py`(4 點)+ `mastery.json`(影刃)·combat 不動**(resolve_attack/estimate_sneak_damage 讀 formulas 自動生效)。**① 基倍遞減**:`sneak_attack_multiplier` 線性 `1+sneak×0.03`(100→×4)→ `1+_soft_cap(sneak,0,0.0277,4.0)`=**sneak100→×3·200→×4·漸近×5**(解兩錨點;治深堆無限放大)。**② 匕首 1.6→1.5**·**③ 夜母每階 0.03→0.0167**(聆聽者×1.18→×1.10)·**④ 影刃 mult_bonus 0.5→0.2**(×1.5→×1.2)·**⑤ 護甲重量係數**`min(1,max(0.45,1−(W−18)×0.012))`→`max(0,1−W×0.012)`(取消 W≤18 grace〔W9→×0.892〕+ 取消 0.45 下限〔W59→×0.292·極重歸0〕)。移除 `SNEAK_ATTACK_SCALE`/`SNEAK_MULT_WEIGHT_FLOOR/_MIN`。**效果**:apex 總倍率 刺客 ×11.33→×5.30·弓 ×9.20→×4.34(−53%)。因 **cap+overkill** 吸收 → 局部:trash 秒殺幾不變(apex bandit 95%);**精英 oneshot 顯著降**(sim_assassin 中階 bandit 74→13%·apex frost_troll→0%/dremora→16%=力量幻想刻意收斂);多數 boss cap 綁定不變;**最硬王明顯削**(sim_builds 刺客 mehrunes_dagon 45→25%·弓 52→34%·疊 R69 上);深堆重砍(sneak138 ×5.14→3.46·200 ×7→4)。**🔴 sim_assassin 全綠**:solo 偷襲秒殺 0% ✓(降倍率更安全)·R63 追擊 0%·4-bandit 22.6%/2b2w 10.1% 不變(群戰死於圍毆非殺速)。零新存檔欄。`run_all` 80(test_assassin armor 係數重寫·test_mastery 影刃 0.2)。偷襲五因子皆 `formulas.py`(+影刃 mastery.json)·動任一→必跑 sim_assassin(守 solo 0%)+sim_builds;基倍錨點調整需重解 SLOPE/CEIL | re-sim |
| R69 | **反風箏:閃避效益遞減(全域)+ vanish 不躲 solo BOSS**(承多輪「刺客打 solo boss 太穩」評估收斂:傷害/護甲/cap 側旋鈕全失效或反效果〔偷襲被 cap 鎖死·堆護甲反誤殺戰士+抬法系·移除 cap 破秒殺紅線〕;唯有動 stealth 兩層防禦〔evasion+vanish〕才對症且 stealth 專屬無連帶。使用者拍板兩項·刻意不含護甲/機率÷2 保零連帶)。**① 閃避效益遞減(全域)**:`formulas.hit_chance` 把總 `defender_evasion`(雜技 dodge_evasion+里程碑 evasion_bonus+敏捷 agility_evasion)過 `_soft_cap(KNEE0.15,SLOPE1.0,CEIL0.35)` 才扣命中。修主破口:`dodge_evasion`=acro×0.0025(acro100=0.25)**無 cap**·線性可疊到命中 5% 地板;≤0.15 線性不動(一般 build 無感)·疊滿封 0.35 → 滿堆刺客對 boss 命中 5%(地板)→~12%。**② vanish 不躲 solo BOSS**:`main.py` 敵人階段 `for e in (enemies if not vanish_success else [])` 改 per-enemy `if vanish_success and not combat._is_solo(e,gd): continue` → 一般敵仍躲過·**solo BOSS 看穿隱遁照常攻擊**(玩家仍重置偷襲);vanish 顯示加「·BOSS 看穿隱遁」。**②是主削**(打第一層引擎=vanish 免疫·刺客 dagon 單此項 84→54)。**效果**(sim_builds n=300):刺客 mehrunes_dagon **84→45%**·弓 85→50%·ancient_dragon 刺客 96→82/弓 99→84;**戰士/法系零位移**(無 evasion 堆疊·不 vanish:warrior_1H 99/13·mage 86/0 不變)→ 終王對 stealth 真挑戰·**無連帶**(勝堆護甲方案);殘留:滿投資 evasion+agi 刺客仍 ~82%(by-design·要再壓加 vanish 機率÷2)。**🔴 sim_assassin 全綠**:solo 偷襲秒殺 0%(evasion/vanish 不碰偷襲 cap)·4-bandit 22.6%/2b2w 10.1% **不變**(diminish 對 acro 群戰 <0.02·群戰 vanish 為非 solo 不觸 no-dodge);全域 diminish 仍動戰鬥常數→必跑 sim(綠)。`run_all` 80(新 `test_evasion_diminishing_returns_r69`)。調 diminish KNEE/CEIL 或 vanish 規則→改 `formulas.hit_chance`(全域)/`main.py` 敵人階段(僅 solo)·重跑 sim_assassin(守 solo 0%)+sim_builds(stealth 終王勝率 by-design 降) | re-sim |
| R68 | **法袍三階套裝再平衡:新增「法術威力」set-bonus 機制 + 魔抗分級**(承「盤點法師套裝效果」評估;使用者拍板把法袍三階做成「魔抗+法術威力」分級身份)。**新 set-bonus 鍵 `spell_power`**(乘進 `magic._power`·與 R67 智力威力疊乘):`inventory.set_spell_power_bonus(char,gd)`=`active_set_bonus().get("spell_power",0.0)`(藏 bonus dict·`_apply_enchant` 不認故安全忽略·比照 `cast_fatigue_factor`/`disguise`/`resist` 子鍵·**零新存檔欄**);`magic._power` 末乘 `×(1+set_spell_power_bonus)`(區域 import inventory 避循環)。**數值**(`armor_sets.json`):學徒布袍 魔抗+5/威力+5%·大法師 魔抗 25→**15**·威力**+15%**·龍祭司 魔抗+25/威力+25%(階梯遞增→龍祭司躍最強法袍〔魔抗25+威力25%+魔力200〕·大法師轉攻防均衡)。**乘性疊乘**(滿練法師大法師袍 int+30%×套裝+15%≈+49.5%·龍祭司 +62.5%);同 R67 及所有學派(傷害/治療/護盾)。**影響**(法師穿 archmage):單擊 mage 93→107·龍祭司袍 122;27 王平均勝率 95.3%→95.3%(威力+15% 與魔抗 25→15 互抵·淨中性·爆發更高·knight_of_order 92→97%)·龍祭司袍法師 96.0%(ancient_dragon 86→95%);**mehrunes_dagon 仍 0%**(終王牆=fire85+magic50 抗性·非傷害不足→未破)。戰法穿 ebony 非法袍→不受影響。**🔴 sim_assassin byte-identical**(刺客穿皮甲非法袍·永不施法→`_power`/set getter 零呼叫·隔離 worktree diff=0);零遷移零 recompute(on-the-fly 讀)。`run_all` 80(test_equipment R65 archmage 魔抗斷言 25→15·新 `test_robe_sets_spell_power_and_resist_r68`)。調法袍 spell_power/resist→純改 `armor_sets.json`·重跑 sim_builds(法系升=by design)+sim_assassin(byte-identical);新 set-bonus 鍵藏 bonus dict + 專屬 getter(勿進 `_apply_enchant`) | re-sim |
| R67 | **智力法術威力大改:int100=+30%(50→100 線性)+ >100 才 softcap(總漸近 +45%)**(承「非潛行單擊評估→法系單擊偏低·智力 ≤100 對法術威力零作用」;使用者三段拍板:int100=+30%·起點上移 50〔≤50 中性〕·>100 才 softcap 收口 +0.15)。**刻意反轉 R63「智力 ≤100 ×1.0 不擾平衡」宣言**(R63 knee=100 不動出廠法系平衡;本輪要讓主屬性對法術威力有感 —— 法系出廠皆 eff int100·今前 ×1.0 零收益)。**只動 `formulas.intelligence_spell_potency`**(單一呼叫點 `magic._power`·乘進所有學派效果:傷害/治療/護盾/結界/灌注;**召喚+束縛兵刃 HP 不吃 power 不受影響**):三段 ≤50→×1.0·50→100 `1+0.30×(int−50)/50`·>100 `1.30+_soft_cap(int−100,0,0.006,0.15)`(0.006=線性段斜率·C1 平滑·總漸近 +45%)。常數 `_KNEE=50/_AT_CAP=0.30/_OVER_SLOPE=0.006/_OVER_CAP=0.15`(取代舊 `_PER/_CAP`);表 int40/50/75/100/138/200=×1.00/1.00/1.15/1.30/1.42/1.45。**影響**(法系皆 eff int100):單擊 mage 72→93·battlemage 法術 69→90〔>劍84·法術躍最大單擊〕;27 solo boss 平均勝率 mage 93.7→95.3%·戰法 94.4→96.9%·集中硬尾(ancient_dragon 戰法 43→89%·knight_of_order 法師 78→92%·**終王法師仍 0%**)。**🔴 sim_assassin byte-identical**(虎人 int≤50 且永不施法→公式零呼叫·隔離 worktree diff=0);無「法術 vs solo 夾」但有界(base int 夾100→固定 +30%·無法一擊·破不了終王);+30% 同及治療(close_wounds 130→169)但受動作+魔力雙閘不可剝削。**零遷移零 recompute**(純讀時公式·max_magicka 不受影響)。`run_all` 80(test_attributes neutral-below-knee 改 50·新 `test_intelligence_potency_r67_curve`)。調 knee/at_cap/over_cap→改此單函式·重跑 `sim_builds`(法系升=by design)+`sim_assassin`(須仍 byte-identical)·**knee=50 必保** | re-sim |
| R66 | **resist 附魔再平衡:soul^0.7 非線性 + 魔抗/單元素分流(承魔抗免疫評估)**(sim 評估揪出元素免疫可由「3 件 magic 附魔」輕易達成〔線性 grand=petty×5·magic=element 同量〕;使用者逐步拍板:① 魂石階改 soul^0.7〔大魂≈微魂×3.1 非 ×5〕② 魔抗基數低·單元素=魔抗×2)。**只動 `enchanting.py` resist 分支**(skill/attr/res/weapon/thorns 不變):新 `_resist_magnitude(slot,param,soul,myst)` = `round(RESIST_MAGIC_ANCHOR[slot]×soul^0.7×(0.5+myst/100)/1.5)`,param=="magic" 回該值否則 ×2;`armor/jewelry_magnitude` 加 `param` 參數、3 個 callsite 傳 param。錨點 soul1·myst100:魔甲2/魔飾3·元甲4/元飾6;大魂:魔甲6/魔飾9·元甲12/元飾18(myst75 ×0.83·myst50 ×0.67)。**效果**:8 槽全魔抗附魔僅 ~68(舊 318)→ **gear 魔抗免疫死透**;單元素需種族+2~3 件(dunmer 火75+1飾22=97 仍差);低階魂石救活(grand=petty×3.1)。**未動非附魔路徑**(誓福 magic142 完成者仍免疫·種族 fire75/frost50)→ 免疫成深完成者成就非附魔速成。**🔴 sim_assassin byte-identical**(刺客無 resist 附魔·隔離 worktree 證;改 enchanting 非 combat/formulas);零存檔欄(synth id 沿存舊磁量·pre-made 神器 resist 數值不受影響=資料端硬寫)。`run_all` 80(+`test_resist_magnitude_r66_nonlinear_and_magic_split`)。調 `RESIST_SOUL_EXP`/`RESIST_MAGIC_ANCHOR`/element×2 → 改 `_resist_magnitude`;單元素=魔抗×2 為構造式(魔抗每點覆蓋三系故每件給半) | save |

## 自動提交閘門 / 換 session 前檢查表

**提交是自動的**:驗證全綠 → `git commit` & `git push origin main`(本專案慣例,**不需使用者明說**;紅燈則不提交、先修)。推送前先過這道閘(一鍵 `/check` = `bash check.sh`):
- [ ] `python3 tests/run_all.py` 全綠(讀結尾印的模組數,勿信文件數字)
- [ ] `python3 tests/browser_ui.py` 真 Chromium 桌面 UI 回歸全綠(不可 skip)
- [ ] 觸戰鬥/施法/刺客/附魔常數 → `PYTHONPATH=. python3 sim_assassin.py`(或 `./check.sh --sim`)
- [ ] 編譯檢查通過(`check.sh` 已含完整 `py_compile`)
- [ ] 若跑過煙霧/實跑 → `rm -f ~/.tesrpg/save.json`(`check.sh --smoke` 會自動清)
- [ ] 改了規則/數量/現況 → 同步 `handoff.md`(§3 鐵律 R## / §1 現況)
- [ ] commit 訊息末加 `Co-Authored-By:` 行 → `git push origin main`

**換 session 前**(= `/sync`):全綠 + sim 穩 + 煙霧通 + 審查確認 bug 修完 + 回歸測試補完 + **handoff.md 已更新本輪里程碑**(且本輪工作已 commit & push)。

## 自動化(`.claude/` 已配置)

- `check.sh` — 單指令驗證鏈(完整 `py_compile` → `run_all.py` → Playwright/Chromium 桌面 UI 硬閘門 → 偵測到改 `formulas.py`/`combat.py` 或 `--sim` 才跑 sim 並標出 `⚠` 行 → `--smoke` 才跑煙霧並 trap 清存檔)。
- `/check` slash-command = `bash check.sh`;`/sync` = 跑 `check.sh --smoke` + 印換 session 檢查表 + 提示更新 handoff.md。
- `.claude/settings.json` 已預許可唯讀測試/sim/git 指令(減少權限詢問)。`git commit`/`push` **不**預許可(刻意保留可見),但依本專案慣例驗證綠後仍自動執行(見上「自動提交閘門」/ handoff §3 R22)。

## 架構 / 重要檔案

- **資料驅動**:規則引擎在 `tesrpg/systems/*.py` + `tesrpg/formulas.py`;內容全在 `tesrpg/data/*.json`
  (races/birthsigns/classes/skills/spells/weapons/armor/recipes/bestiary/world/dungeons/quests/factions/events/origins/rulers/mastery/landmarks…)。
- **核心循環**:行動制(在地點選行動 → 推進時/日 → 觸發事件/遭遇);戰鬥是回合制子迴圈。
- 進入點/主迴圈:`tesrpg/main.py`;狀態/存檔:`tesrpg/state.py`;角色:`tesrpg/models/character.py`。
- 規則:`tesrpg/formulas.py` + `tesrpg/systems/*.py`(combat/magic/progression/mastery/smithing/vampirism/skooma/politics…)。
- UI:**Web-only** —— `tesrpg/web/`(stdlib HTTP+SSE 後端 + `static/index.html` 前端)+ `tesrpg/ui/console.py`(渲染/輸入接層:`_*_view` view-model + rich→HTML 退路 + `_web_prompt`)。平衡工具:`sim_assassin.py`;設計/交接:`DESIGN.md`、`handoff.md`。
